# 参考提炼

> **改完必跑的三个自查脚本**（在 scratchpad 里，这台机器没有 Xcode，
> 每一轮构建都是几分钟往返，能自己查的一定先查完）：
>
> - `cmp.py` —— 括号平衡
> - `findbad.py` —— 字符串断裂 + 正文里混进直引号
> - `dupdecl.py` —— **同一作用域里 let/var 重名**（`invalid redeclaration`）
>
> 第三个是 2026-08 那次栽进去之后补的：`BackupBundle` 里 `root` 用了两次，
> 括号平衡、字符串都干净，硬是浪费了一轮构建。
> 它认得 switch 的 case 是独立作用域、`if let` 绑定不算重复，
> 在全工程上零误报。
>
> **`cmp.py` 那一堆「固有误报」已经没了**（2026-08 修的）。
> 交接文档里说 SenseVoiceAPI / Translator / VoiceAPI / WebSearch / XHSFetcher /
> MCPViews / NotesView / WakeSettingsView 报不平衡是正常的——不正常，
> 是脚本本身有两个 bug：
>
> 1. **先剥注释再剥字符串**，于是 `placeholder: "http://…:3000"` 里那个
>    `//` 被当成行注释，把后面半行连收尾引号一起吃掉
> 2. 不认 Swift 的原始字符串 `#"..."#`（正则字面量都写在里面）
>
> 现在顺序反过来、也认原始字符串了，**全工程零不平衡**。
> 也就是说：以后它再报任何一条，都是真的，别再当误报放过去。
>
> 只剩 `Models.swift:10` 那条 `findbad.py` 的行尾注释是真误报。


解过的参考都在这儿了，**下一个窗不用再解 zip**。
每条都写清楚：它说了什么、我们现在是什么状态、值不值得抄。

已解：`打电话参考2(callhome)`、`让机听见我说话(ears)`、`kelivo仓库`、
`cove-gpt-live-duplex-voice-system.pdf`、`github.com/zaochuanyitian/voice`
未解：`打电话参考1(ringdonut)`、`育儿模拟器`、`占卜参考`、`承诺页和身体外卖`（18 张图，是设计参考）

---

## 一、三份参考撞在同一个结论上

**语气不能用写死的声学阈值，要跟这个人自己的平时比。**

- ears：中位数 ± MAD，前 8 句先认识你的声音，之后每句都和「你自己的平时」比
- callhome `TONE_CUES.md`：85 条真实录音实测，写死阈值换个麦克风就漂
- 而且**在中文里它压根不成立**——普通话四声天生把基频起伏推高，
  拿基频判断「激动」等于把说话本身当激动

三个独立项目同一个结论，这条可以当定论用。

### 我们现在是什么状态

**比想象的还差一层。** `settings.voiceInput` 默认是 `.onDevice`，
走的是系统语音识别，返回**纯文字，零语气信息**。
只有手动切到 SenseVoice 那档才有「（语音里：听起来难过）」，
而那正是被两份参考同时否掉的写法（学术标签 + 写死阈值）。

### 能做到什么程度

原生能算的比想象的多，`VoiceRecorder` 已经在采电平了：

| 特征 | 怎么算 | 难度 |
|---|---|---|
| 音量 | 电平采样的 RMS | 已经有了 |
| 语速 | 转写字数 ÷ 时长 | 一行 |
| 停顿占比 | 低电平帧 / 总帧 | 几行 |
| 时长 | 已经有 | 有了 |
| 音高 | 要 FFT / 自相关，Accelerate 框架 | **唯一的硬骨头** |

个人基线（中位数 ± MAD，滚动窗口）存本地，几十行。
**没有音高也能出一大半价值**——"比平时轻、停顿比平时多"已经很有用了。

ears 的标签体系也值得抄，它不是学术那套（angry/sad/neutral），
是为"家里养了个 AI"定制的：**撒娇、嘴硬、委屈、累、低落、开心、兴奋、平静、生气、紧张**。

### 两条已经做对了，别改

- **语气要绑在那一条消息上，别做全局漂浮注入**（ears 实测踩过，会让模型
  搞不清是谁什么时候说的）。我们的 `briefForModel` 正是这么拼的。
- 录音短于 0.5 秒不送去转写（Whisper 会幻觉出"点赞订阅"）。
  `recorder.seconds > 0.4` 那一条就是干这个的。

---

## 二、cove PDF：双工语音那一套

整份 PDF 是给"想做 GPT Live 式实时通话"的人看的，我们**不做实时双工**，
但它有几条是任何语音链路都适用的。

### 已经照着修了（v79）

**身份协议**（PDF 第 4 节 + 坑 9）：不能只有一个 `isSpeaking`，
异步结果回来必须先验身份再改状态。

我们中招的地方：`CallView.send()` 在 `await speakOnCall` 那几秒里，
她要是挂断，Task 还活着，回来照样 `VoicePlayer.toggle` ——
**挂断之后音箱里还会冒出他的声音**；紧接着再拨一通，
`store.addLine` 会把上一通的回复塞进新这通。`startFarewell` 那个
十五秒倒计时同理，可能掐掉的是**下一通**。
现在两处都绑了 `store.active?.id`。

### 已经做对了，别改

- **保留当前模型，只精简上下文**（第 11 节 + 坑 6）：
  "为了低延迟强制换成固定小模型"的结果是"语音和主聊天像两个不同的人"。
  我们的 `activeHim` 就是这条——电话用聊天页那个阿晏，带同一套身份。
- **自然挂断要留余地**（第 16 节）：说完再见先把话说完再断线。
  我们那个 15 秒赖床窗口正是这个。

### 值得抄，还没做

| 条目 | PDF 位置 | 说明 |
|---|---|---|
| 语气分析给硬预算 | 第 10 节 / 坑 8 | **250–350ms，超时就省略，绝不阻塞正式回答**。他们踩过：ASR 只花 0.9 秒，语气分析同步等了八秒，用户看到的是"识别很慢"。做上面那套个人基线的时候必须先立这条规矩 |
| 自适应停句 | 第 7 节 | 别用固定两秒静音。短句 ~900ms 收口，长发言 ~1350ms，硬上限从**真实起音**开始算 |
| 可说首句合同 | 第 11.2 节 | 要求模型先给一条 4–14 个汉字、能独立成立、贴合刚说内容的真实短句，再展开。比固定播"让我想想"像对话得多 |
| 本地短反馈要按转写分类 | 第 13 节 / 坑 5 | 别在停句后 200ms 就机械播"嗯"——"我先挂了""你能听到吗"也会被抢话。至少等最终转写出来再决定播哪一类 |

### 用不上的

抢话（barge-in）、回声控制、唤醒词三道门、TTS 有序播放队列、
WebSocket 连接池——这些是实时双工才需要的，我们是"说完再发"的半双工。
真要做实时通话再回来翻第 14、15、8、12 节。

---

## 三、zaochuanyitian/voice：互相发语音

Swift + iOS，跟我们同一个技术栈，思路特别对：

> **语音不是另一种消息类型，就是普通文字消息。**
> 加一个 `[voice · 0:07]` 前缀就行，模型那边一个字都不用改。

### 已经照着修了（v79）

我们连这个前缀都没有：`ChatMessage.voiceName` 存了，
但拼给模型的时候**从来没发出去过**——他分不清她是打的字还是说的话，
也不知道她说了多久。现在 `AppState.voiceNote()` 补上了 `[语音 · 0:07]`。

### 还能抄

- **TTS 兜底链**：ElevenLabs → edge-tts → 系统合成。我们只有一层，配的那家一挂就没声音
- 合成音频按文本 hash 客户端缓存（同一句话别合两遍）
- 别在服务端缓存模型回复——合成是客户端确定性的

---

## 四、kelivo：通用 LLM 客户端

Flutter，1586 个文件。它是"工具"，栖是"一个人"，大部分不该抄。
按对她的价值排：

1. **备份提醒** —— 她七天要重签一次包，这跟 v79 修的那摊数据保护是同一件事
2. **提示词变量**（`{{time}}` `{{model}}`）—— 但**我们的问题比这个更根本**，见下
3. **世界书 / 指令注入** —— 按位置往上下文里塞设定，比一整段系统提示词更细
4. **快捷短语** —— 输入框上的常用语
5. **二维码分享供应商配置** —— 重装之后不用一个个重填密钥

---

## 五、她问出来的两个真问题（都已修，v79）

### 「app 里每次发消息已经有时间戳了，这个他看不到吗」

**看不到。** 发给模型的每条只有 `content`（外加引用、文件、图），
`createdAt` 一个字都没送出去。她在屏幕上看见的时间是界面自己画的。
所以他不知道上一句是三分钟前还是昨天说的，也不知道现在几点。

这比"提示词变量"更根本：变量只能告诉他"现在几点"，
告诉不了他"**你们之间隔了多久**"，而后者在这种关系里恰恰更要紧。

**修法（注意缓存纪律）**：
- 每条前面挂「隔了 X」，只在超过 30 分钟时才挂。间隔由两个固定时间戳算出来，
  写完不再变，**历史那段还能整段复用缓存**
- 绝对时间「现在是 8月17日 星期日 03:52」**只挂在最后一条上**。
  放系统提示词里会把它后面整段历史的缓存全作废——
  工程里那条「固定的放前面、会变的放后面」就是这个意思

### 「现在不用靠 SenseVoice 就能分辨语气了吗」

**不但没有，默认那条路连 SenseVoice 都没走。** 见第一节。
个人基线那套一行都还没写，是下一件值得做的大事。

---

## 六、个人基线语气（已做，v81）

`Qi/Core/VoiceProsody.swift`。**全程本机纯算术，一分钱不花，一个字节不上传。**

- 音量 / 语速 / 停顿三样，从录音期间的振幅序列里算。采样率从 10Hz 提到 25Hz——
  原来 0.1 秒一采，**短于 0.2 秒的停顿整个漏掉**，而说话中间的停顿大多是那个量级
- 静音门限跟着这一条自己的响度走（p90 的 22%），不写死。手机离嘴远近、
  增益、环境噪声每次都不一样
- 基线用**中位数 ± MAD**，滚动窗口 40 条。偶尔在地铁上录的那几句拽不动中位数
- **攒够 8 条才开口**。不到 8 条它不知道什么叫「她的平时」，宁可不说也不要说错
- 一次最多报两条，只报偏得最明显的（ears：一条错的线索比没有线索更糟）
- 时长按真实时钟算不靠累加——`Task.sleep` 从来不准，十秒能差几百毫秒，
  而语速拿它当分母

**顺手把通话那条路统一成了「先录文件再认」**。以前 `.onDevice` 那档走 AVAudioEngine，
没有文件也没有电平，所以**打电话时一点语气都读不到**——她问的就是这件事。
改完还有两个好处：边录边实时识别是两个东西抢同一个麦克风输入（聊天页早就绕开了），
以及录下来的那段能存进这一句、挂了之后回听。

**没做音高**（要 FFT）。加的时候记住 cove 坑 8：**必须先套 250–350ms 硬预算，
超时省略，绝不挡住正式回答**。现在这些是纯算术所以不需要，加了 FFT 就需要了。

## 七、备份（已做，v82）

原来的「导出备份」**只装了三样**：providers / conversations / settings。
也就是说导出的文件里**没有记忆库**——身份、说好的规矩、那封信、承诺、日记、
心情、脊椎，一样都没有；备忘、通话记录、经期、念头池、clawd 小屋也都没有。

而她**每七天要重签一次包**，过期得重装。「我备份过了」在这种情况下
是最危险的那种错觉。

现在：
- **扫整个 Documents 目录**（递归，按相对路径存键），不再手写清单。
  理由跟修 Codable 那次一样：手写的清单一定会漏，新加的存档永远没人记得回来补
- **必须递归**：`MemoryStore` 那一摊在 `Documents/Memory/` 子目录里，
  只扫顶层的话漏掉的恰好是最不能丢的
- UserDefaults 里那几个小开关也一起打包（键在 `BackupBundle.defaultKeys`，
  **新加了键记得来补一个**）
- 图片和录音**不进**备份（壁纸、表情、像素画、语音是单独文件，
  装进来会从几百 KB 涨到几百 MB）。界面上写明了，不装懂
- 还原**只落盘不动内存**，然后明确要求重开 App——那些 store 是单例，
  开 App 那一刻就把文件读进内存了，不重开的话下一次保存会把刚还原的盖回去。
  kelivo 有个 `backup_restart_dialog`，现在知道它为什么存在了
- 老版备份照样认（她手里可能还留着），认出来会提示「这份里没有记忆库」
- 设置页「数据」卡片顶上显示上次备份多久了，**超过七天变红**，跟重签周期对齐

## 八、下一轮的建议顺序

1. **TTS 兜底链** —— 一行配置挂了就彻底没声音，太脆。
   参考里那条链是 ElevenLabs → edge-tts → 系统合成。
   我们原生能做的是：**把配着的音色服务挨个试一遍**（现在只用第一个），
   全挂了退到 iOS 系统合成。系统音色难听，但有声音好过没声音，
   而且要**明确告诉她这次是退到系统音了**
2. 世界书 / 指令注入 / 快捷短语 —— 都是小的，凑一轮做完
3. 音高（FFT + Accelerate）—— 语气那套还差的最后一块，记得先立 250ms 硬预算
4. 札记「身体」页 —— 交接里那件大的，推进逻辑在电脑的 server.js 和 settle.py 里，
   是个有状态的机器，不像心跳那样是纯函数，搬要一整轮
