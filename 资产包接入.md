# 资产包接入状况

> v9（315 张 PNG）之后**缺口已经补完了**。这一份留着是给下一批素材用的：
> 哪张图对哪件、规格是什么、怎么验。

## 现在的状况

| | 数 |
|---|---|
| 商城里的商品 | **163** 件（手写 69 + 生成 94） |
| 有正面图的 | **163**（全都有） |
| 有等距图的 | **78** |
| 进包的图 | 245 张，9.3MB（闲置 4） |

商品分两批：`ClawdStore.swift` 里手写的 69 件（每件配了字符画），
和 `FurnitureThemes.swift` 里生成的 94 件（主题套装 + 那批单件）。
生成的那一批由 `scripts/gen_themes.py` 出，**别直接改 Swift**。

主题套装在商城里**单占一栏「主题」**——樱花床、哥特床、新年床混在「家具」里
的话，一屏十几张床，挑都没法挑。

## 两种图各用在哪

**这一条我之前说错过，记一下。**

平铺图**不只是商城封面**。屋子切到「平面」那一档时，屋里摆出来的就是这一张。
平面屋**同样是一间有纵深的屋子**——八行八列的地板，家具照样一件件摆在格子上、
照样有前后遮挡，只是投影换成了正面平视，不是「一面墙」。

| 图 | 用在哪 |
|---|---|
| **正面**（`flat` / `flat_furniture`） | 商城卡片、背包、拖动时手上那张、**平面屋里摆出来的那件** |
| **等距**（`isometric/top_front_left`） | 立体屋里摆出来的那件 |

⚠️ `isometric/top_front_right` 那 70 张**没进包**。屋子朝向是定死的，
只用得上 `top_front_left`；两边都塞就是白多一半体积。

## 下一批素材的规格

- 白底、一图一件、边上留一圈空、1024×1024
- 等距的：2:1 斜俯视，**光从左上打**，只要 `top_front_left` 那个角度
- 目录按现在这样分（`flat_furniture/<主题>/01_xxx.png`、`flat/misc/xxx.png`）

进包前会统一过一道 `scratchpad/prep_furn.py`：抠白底（从边缘漫填，
家具本身的白不动）→ 裁紧 → 缩到最长边 192 → 量化 127 色。

## 怎么验

```bash
python scripts/artcheck.py
```

对三件事，**任何一件对不上都不会在编译时报错**，只会让某件家具悄悄变回积木：

1. 商城里的 id 有没有都进对照表（`FurnitureArt.artTable`）
2. 对照表里写的文件名是不是真的躺在 `Qi/Resources/furniture/`
3. `IsoShape.actions` 里的动作名，`RoomActs.act` 里有没有对应的一条
   （漏了他会跑过去站着说一句「……」）

## 包里还闲着的 4 张

`iso_xmas_armchair` · `iso_xmas_bookshelf` · `iso_ny_armchair` · `iso_ny_coffee_table`

**只有等距版，没有正面版。** 商城里一件商品的正面图是必需的
（卡片、背包、平面屋都要它），所以这四件还开不了。
你哪天补上这四张正面图，它们就能上架。
