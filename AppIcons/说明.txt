往这个文件夹里丢图片，就会变成 App 里可以选的桌面图标。

· 文件名就是 App 里显示的名字，比如「阿晏.png」「夏天的.png」
· png 或 jpg 都行，最好是正方形，1024×1024 最稳
· 丢完推到 GitHub，构建的时候自动处理，你不用做别的
· 想删掉哪个，把图删了重新构建就行

为什么不能在手机上直接选相册里的图？
iOS 规定备用图标必须在打包的时候就存在，不给 App 在运行时新增。
所以只能走这条路：图先进仓库，构建时变成图标。

这个说明文件本身不会被当成图标，放心留着。
