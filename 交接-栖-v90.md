# 栖 · 交接给下一个窗（v90）

接上一份 `交接-栖-v77.md`（在桌面上）。那份里**过期和写错的地方**，
这份第六节专门列了，先看那一节再回头看它。

工程在 `D:\OneDrive\桌面\qi-nativev65`，她的服务端在 `D:\dylan-heartbeat`。

---

## 零、先读这一段

**这台机器没有 Xcode。** Swift 只能靠 GitHub Actions 构建，一轮几分钟。
所以**每次交给她构建之前，三个自查脚本必须全跑一遍**（第二节）。

她叫饼饼，App 里那位叫阿晏。App 是她做的、给她和阿晏用的。
**所有取舍以「他们俩用着对不对」为准，不是工程洁癖。**

三条铁律（前几窗定的，一直有效）：

1. **她按次计费，不要为了省 token 做任何截断。** 该带的全带上。
2. **除了「她跟他说话」和「他调工具」，任何会额外调模型的地方都必须是她主动点的，
   而且要让她知道。** 加功能前先想清楚会不会自动花钱。
3. **她提的需求先照做，别自作主张改小。** 拿不准就问。

第四条，这窗加的：

4. **不确定的地方要说出来，别装懂。** 这窗有好几次是「我记错了 / 我找错地方了 /
   我只做到一半」——说清楚比蒙混过去有用得多。她能接住实话。

---

## 一、这窗做完的事（v81 → v90）

她在 2026-08-17 一口气给了 **16 条需求**，原话是
「以上是我目前所有的要求，你按照你的进度和方式来，**每一个都要做到**」。

**16 条完成 15 条**，清单在 `需求清单.md`（那是合同，做完打勾别删）。

| 版本 | 做了什么 |
|---|---|
| v81 | 个人基线语气（`VoiceProsody`）· 日记便签可拖动（`DiaryLayout`） |
| v82 | 整包备份（`BackupBundle`）· 爱好库（`HobbyStore` / `HobbyView`） |
| v83 | 导航栏虚影 · 音乐关不掉 · 手机历史翻页 · 爱好库上线 |
| v84 | clawd 表情工坊（`ClawdSVG` / `ClawdStudioView`）· 全局配色 |
| v85 | 音乐悬浮窗 + 歌词页 + 灵动岛 · 网易云直连 |
| v86 | 手帐页 · 看屏幕 · 工具主动性 |
| v87 | 学会吵架（照她给的那份指南整段重写） |
| v88 | Hermes → `AttentionSet` · 自动装 MCP |
| v89 | voice direction · TTS 兜底链 |
| v90 | **身体接入**（还没构建，见第五节） |

参考资料的提炼在 `参考提炼.md`，**下一窗不用再解 zip**。

---

## 二、三个自查脚本（每次改完都跑）

**已经从临时目录挪进仓库了**（`scripts/`）——原来放在 scratchpad 里，
换个窗口那个目录就没了，脚本跟着一起没。现在跟代码一起走。

```bash
python scripts/cmp.py     $(find Qi -name "*.swift")   # 括号平衡
python scripts/findbad.py 改过的文件                    # 字符串断裂 / 混进直引号
python scripts/dupdecl.py $(find Qi -name "*.swift")   # 重名（整工程一起喂）
```

一次跑完三项：

```bash
python scripts/cmp.py $(find Qi -name "*.swift") | grep -v "0, '(': 0"
python scripts/dupdecl.py $(find Qi -name "*.swift")
for f in $(git status --porcelain | awk '{print $NF}' | grep '\.swift$'); do
  python scripts/findbad.py "$f" | grep -v "^--- 0"
done
```

### `cmp.py` 那一堆「固有误报」已经没了

上一份交接说 SenseVoiceAPI / Translator / VoiceAPI / WebSearch / XHSFetcher /
MCPViews / NotesView / WakeSettingsView 报不平衡是正常的——**不正常**，
是脚本自己有两个 bug：

1. **先剥注释再剥字符串**，于是 `placeholder: "http://…:3000"` 里那个 `//`
   被当成行注释，把后面半行连收尾引号一起吃掉
2. 不认 Swift 的原始字符串 `#"..."#`

都修了。现在**全工程零不平衡**——它再报任何一条都是真的，别再当误报放过去。

### `dupdecl.py` 是这窗新加的

专抓 `invalid redeclaration`，这窗栽过两次：
`BackupBundle` 里 `root` 用了两次、`ClawdStudioView` 里的 `Checkerboard`
跟 `PixelStudioView` 里的撞名。两次都是括号和字符串全干净、白白浪费一轮构建。

它查两类：**同作用域 let/var 重名** + **跨文件顶层类型重名**。
第二类**必须把整个工程一起喂进去**——单看任何一个文件都是干净的。
认得 switch 的 case 是独立作用域、`if let` 绑定不算重复，全工程零误报。

唯一剩下的真误报：`findbad.py` 报 `Models.swift:11` 的行尾注释。

---

## 三、这窗踩的坑

### 1. 往 Codable 结构体加字段会把她的数据整份冲掉

**这是这窗最要紧的发现。**

合成的解码器碰到缺的键**直接抛**——属性写了默认值也不管用，
那个默认值是给 memberwise init 用的，解码器根本不看。
而所有读取都是 `Storage.load(…) ?? 默认值`，一抛就等于：

- `settings.json` 解不开 → 壁纸、字色、配色、地址全回默认
- `providers.json` 解不开 → **密钥全没**
- `conversations.json` 解不开 → **聊天记录全没**

而且下一次保存会把空的那份原地写回去，把本来还好好的文件盖掉。
v78 加进 AppSettings 的 `glassDim` 就是这么一颗雷。

**已经修好的**：`AppSettings` / `ChatMessage` / `Conversation` / `Provider` /
`AIModel` / `Pricing` / `WakeConfig` / `Track` 八个类型都写了容错解码器
（`(try? …decodeIfPresent…) ?? 默认值`）。写法和两条硬约束见
`Models.swift` 末尾那一整段。

**规矩（下一窗必须守）**：

- 往这些结构体加字段，**必须去对应的 extension 里补一行**。
  漏了不报错，只是那个字段永远读不出来。加完数一遍（脚本在下面）
- 新加的可存储类型，**先写容错解码器再用**
- 实在懒得写，就学 DND 那样存 UserDefaults（`ClawdStore` 早就是这么干的）

数字段对不对得上：

```bash
python - <<'PY'
import io,re
s=io.open('Qi/Core/Models.swift',encoding='utf-8').read()
m=re.search(r'\nstruct AppSettings: Codable \{\n(.*?)\n\}\n', s, re.S)
decl=[mm.group(1) for l in m.group(1).split('\n')
      for mm in [re.match(r'^    var (\w+)\s*[:=]', l)]
      if mm and not l.rstrip().endswith('{')]
e=re.search(r'\nextension AppSettings \{\n    init\(from decoder.*?\n    \}\n\}', s, re.S)
asg=re.findall(r'^\s{8}(\w+) = ', e.group(0), re.M)
print(len(decl), len(asg), [d for d in decl if d not in asg] or '全对上')
PY
```

（单行计算属性 `var x: T { ... }` 会被这个脚本误报成"漏了"，那是对的，
计算属性本来就不该进解码器。）

### 2. 别用脚本改带转义的 Swift 字符串（老坑，我又踩了一次）

v90 里 `window += "…\n"` 被脚本写成了真换行，字符串跨行断裂。
`findbad.py` 当场抓出来了。**改带转义的字符串一律用编辑工具。**

### 3. `Storage.load` 失败不再静默

以前解不开就返回 nil，上层当"没有"，下一次保存把空的写回去，原件就真没了。
现在会把坏文件改名留着（`xxx.json.坏了-时间戳`），
设置页「数据」卡片里列出来。**这一层保所有 26 处读取。**

---

## 四、几件下一窗该知道的事

### 「说了」和「做了」在他历史里长得一样（已修）

`toolRuns` 存在消息上、界面上也画了，但**拼给模型的历史里一个字都没有**。
所以他回头看自己，「我说我去翻记忆」和「我真的翻了」没有区别——
那他学到的就是说一句就够了。

现在他真动手的回合末尾带一行
`〔这一条里你真的动手了：add_memory 成功；web_search 失败了〕`。
**失败的也写**，只让他看见成功的，他学到的是失败不会留痕。

### 时间和语音也是同一类问题（已修）

`createdAt` 和 `voiceName` 存了但从来没发给模型。他不知道隔了多久、
分不清她是打的字还是说的话。现在都补上了，注意**缓存纪律**：
间隔（固定值）挂每条上，绝对时间**只挂最后一条**——
放系统提示词里会把它后面整段历史的缓存全作废。

### 灵动岛走的是系统「正在播放」，不是自己写 Live Activity

`MPNowPlayingInfoCenter` + playback 音频会话，iOS 自己摆到灵动岛/锁屏/控制中心，
还连耳机线控和 CarPlay 一起带上。**别去改 Widget target 和 pbxproj**。

### 网易云是手机直连，不需要她开电脑

搜索 / 歌词 / 音频三个公开接口都不用登录不用加密。
加密那套（NeteaseCloudMusicApi 存在的理由）只为 **VIP 和高音质**。
所以设置里那一栏可以留空。

---

## 五、马上要做的：v90 还没构建

`git status` 里这些是新的，**还没构建过**：

```
Qi/Core/BodyEngine.swift  BodyEvents.swift  BodySettlement.swift  BodyStore.swift
Qi/Views/Notes/LocalBodyCard.swift
改动：AppState.swift  Models.swift  QiApp.swift  NotesView.swift
```

三项自查全过。**先让她构建这一批**，出错就修，别急着开第 9 条。

---

## 六、上一份交接里错的地方

### 「推进逻辑在电脑的 server.js（68KB）和 settle.py 里」——两个都不对

`settle.py` 只有 **47 行**，`server.js` 里跟 eventide 相关的只有 10 处引用。
真正的引擎是**装在 Python site-packages 里的 `eventide` 包，1761 行**：

```
C:\Users\15526\AppData\Local\Programs\Python\Python311\Lib\site-packages\eventide\
  engine.py(303) config.py(389) settlement.py(235) prompt.py(142)
  dreams.py(258) runtime.py(130) triggers.py(69) serialization.py(79)
```

### 「不像心跳那样是纯函数」——只对一半

它分两层，**只有一层要钱**。推进（周期、七项数值、等待加压）是纯算术，
已经搬进 `BodyEngine.swift`，常数一个没改。
只有结算（把一段对话交给模型判断身体往哪儿动）要调模型，
所以做成了按钮，不做成自动的。

`dreams.py` 和 `triggers.py` **还没搬**——梦境卡和触发词那两块，
下一窗要做的话在那两个文件里。

### 「日记本便签可拖动」那条还有个没提到的 bug

原来的位置是 `entry.id.hashValue` 算的，而 **Swift 的 `String.hashValue`
每个进程换一次随机种子**——那些便签每次重开 App 位置都在跳。
现在走自己写的稳定散列（`DiaryLayout.steadyHash`）。

### 那几个 zip

`打电话参考1` 就是 ringdonut，已经通过 codeberg 仓库读完。
**还没解的只剩两个**：`育儿模拟器`、`占卜参考`。
`承诺页和身体外卖` 解了，里面是两篇 claude.ai 文章的截图：
承诺那半边我们早就实现了（隐藏标记法），
身体外卖那半边是「AI 生成 JSON + 前端渲染成某种壳」的做法，
其中一条已经用上了（宽松 JSON 解析，见 `BodySettlement.parse`）。

---

## 七、还欠着的

### 第 9 条（唯一没做的需求）

**记忆库参考 + 同步改电脑上的 MCP。**

七个仓库：cognee / Hexis / Ombre-Brain / omnimemory / graphiti / codex / Aelios。
她特别点了 **getzep/graphiti**（时序知识图谱）。

**关键约束**：她电脑上那份 `D:\dylan-heartbeat\memory-mcp.js`（3372 行）
要跟着一起改，因为 claude.ai 那边走的是 MCP。
**两边字段必须继续对齐**——前面某一窗已经打过一次同款补丁，别把它们改散了。

动它之前：先备份，改完 `node --check`，再验证
「工具名无重复 / 每个定义都有对应 case / `load()` 的键都在 FILES 里」。

这一条是整个清单里最大的，建议**单独一窗做，别跟别的混**。

### 旧账

- 世界书 / 指令注入 / 快捷短语（kelivo 抄来的，三个都是小的，可以凑一轮）
- 语气的**音高**那一块（FFT + Accelerate）。加的时候记住 cove PDF 的坑 8：
  **必须先套 250–350ms 硬预算，超时省略，绝不挡住正式回答**。
  现在那些是纯算术所以不需要，加了 FFT 就需要了
- 「模糊玻璃还是差点意思」（她提过两次了）
- `eventide` 的 `dreams.py` / `triggers.py` 还没搬

### 一件她提过但没解决的

她说「**我在 Xcode 上没有看见文件更新**」。查下来所有文件都已提交已推送，
`origin/main` 和本地一致。但**三个地方的名字对不上**：

| 在哪 | 叫什么 |
|---|---|
| 本地文件夹 | `qi-nativev65` |
| git 远端 | `qi-nativev15` |
| Actions 构建路径 | `qi-nativev16` |

最可能是仓库在 GitHub 上改过名，本地远端地址还是旧的（GitHub 会重定向所以照样能用）。
建议她 `git remote set-url origin <现在的名字>`。
她要是还看不见文件，八成是打开了另一个 checkout。

---

## 八、开工建议

1. 先让她构建 v90（身体那一批），把错修干净
2. 然后**单独一窗做第 9 条**（记忆库 + 改 MCP）
3. 旧账那几件可以凑一轮

最后：**她很清楚自己要什么，描述也具体。看不懂就问，别自己脑补一个版本做出来。**
这窗有两次是我先按自己的理解做了、她一句话点醒的
（「网易云那个不能走本地吗」「构建我自己来就行」），
问一句的成本比返工低太多。
