# 栖 · 交接给下一个窗（v91）

接 `交接-栖-v90.md`（同一个文件夹）。**那份里的东西这窗基本都还成立**，
只有第二节里「cmp.py 现在零误报」那句要改（见下面第四节）。

工程在 `D:\OneDrive\桌面\qi-nativev65`，她的服务端在 `D:\dylan-heartbeat`。

---

## 零、先读这一段

**这台机器没有 Xcode。** Swift 只能靠 GitHub Actions 构建，一轮几分钟。
所以**每次交给她构建之前，三个自查脚本必须全跑一遍**。

三条铁律（一直有效）：

1. **她按次计费，不要为了省 token 做任何截断。**
2. **除了「她跟他说话」和「他调工具」，任何会额外调模型的地方都必须是她主动点的。**
3. **她提的需求先照做，别自作主张改小。** 拿不准就问。
4. **不确定的地方要说出来，别装懂。**

这窗新学到的第五条：

5. **她看不懂日文。** 这窗一开始我用日文问了三个问题，她回「抱歉我看不懂日文，
   只是我 ip 在日本」。**中文。**

---

## 一、这窗做了什么（v90 → v91）

她这窗给了四条（编号接着 `需求清单.md` 往下排，17–20）：

| 需求 | 落在哪 |
|---|---|
| 17 欲望系统（desire_for_ai）| `DesireEngine.swift` · `DesireBarsView.swift` · **`memory-mcp.js` 同步改了** |
| 18 可互动娃娃（gist 的 Claude 版）| `DollStore.swift` · `DollWidget.swift` · `DollRoomView.swift` |
| 19 大富翁（spicy-monopoly）| `MonopolyEngine.swift` · `MonopolyGame.swift` · `MonopolyView.swift` · `Qi/Resources/Monopoly/` |
| 20 游戏页重做 | `GamesView.swift`（整个重写） |

**都还没构建过。** 三项自查全过，但一行都没编译过。

### 17 欲望系统

那份 PDF 讲的是三层。**第②层（念头池）我们早就有了**——`ThoughtPool.swift`，
常数一个没差（0.82 衰减 / 1.10 加强 / 0.8 升执念 / 反哺三次出池）。
这窗补的是①和③：

- 八维驱动条（想她/好奇/想沉淀/记挂/想看人/累/渴/压着），随时间自己涨
- 召唤力 = 条子值 + **0.35** × 关联执念强度（照抄）
- 疲惫闸 **0.72**（照抄）、反哺 **+0.18**（照抄）、整张 `ACTION_SATISFY` 回落表（照抄）
- `pick_intent` → 八件想做的事，第一人称

**有一张表是我自己定的，不是照抄的**：每小时自然涨多少。
原文档那条 `_ease_drive` 曲线在 twin 那边，不在这份 PDF 里，我没有那段代码。
定的依据写在 `DesireEngine.swift` 文件头：渴最快（约 10 小时到顶）、
责任最慢（约 40 小时）。以后拿到原始曲线，**换掉那张 `risePerHour` 就行，别的不用动**。

接线：
- 念头池反哺现在是**真的推一下**（以前只记了个 feedNote 给界面看，"反哺欲望"是句空话）
- 她一发消息，「想她」×0.82、「压着」×0.90（`DesireEngine.touched()`）
- `AttentionSet` 第 ⑧ 条：**只在她把「驱动行为」打开之后才摆**（默认关，跟原文档语义一致）
- 两个新工具：`read_desire` / `satisfied`

**电脑那边（她要求的「电脑里的也一起修」）**：`memory-mcp.js` 里加了同一套 JS 版——
`DRIVE_KEYS` / `DRIVE_RISE` / `ACTION_SATISFY` 常数一模一样，
`desire.json` 和 `thoughts.json` 字段跟手机端一模一样（时间都是 ISO8601），
**两边可以直接对拷**。四个工具：`stir_thought` / `read_thoughts` / `read_desire` / `satisfied`。
`wake_up` 末尾会带上（驱动行为开着才带欲望那句，念头池那句一直带）。
改完跑过四项验证：`node --check` ✓ / 工具名无重复 ✓ / 每个定义都有 case ✓ /
load·save 的键都在 FILES 里 ✓。备份在 `memory-mcp.js.bak-20260817-2303`。

### 18 娃娃

原版（Dan & Joy 那份 gist）是 **ChatGPT 版**：Node MCP 服务器管状态 +
HTTPS 托管的 widget + `window.openai.callTool`。
**那套的前提是有一台一直开着的服务器。** 她说得很清楚：不喜欢一直开着电脑。

所以这版压成两层：状态在 `DollStore.swift`（存 `doll.json`），
HTML 只管画和传操作，桥是 `window.qi.act(action, args)`（WKWebView 的 reply 通道）。
原文档那些设计原则照搬了：**每次回一份完整快照**（不是增量）、
`version` 每次加一、**只要一个统一的 action**、热区是透明按钮点一下闪一下、
内容按状态拼（本地模板那层）+ 他能自己写一句递进来（原文档里 MCPContentProvider 那层）。

她说「1 和 2 都可以，甚至都做」，所以**两个都做了**：
游戏里的娃娃房（整页）+ 聊天里内嵌的卡片（`ChatMessage.widget == "doll"`），
**同一份状态**。卡片是活的——往回翻聊天记录看到的是它现在的样子，不是当时那一帧。

工具：`doll_action`。

### 19 大富翁

她的原话：「**这个需要模型参与吗 如果需要的话可以做成mcp 不需要的话就做成本地swift好了**」。
拆开来看：掷骰/走格/抽卡/红线过滤/算账**全是纯算术** → 本地 Swift；
需要模型的只有「轮到阿晏的那道任务他自己演」→ 那本来就发生在聊天里。
所以引擎搬成本地，他通过 `monopoly` 工具掷骰，演出在对话里。**一分钱不花，不联网。**

三个 JSON 放在 `Qi/Resources/Monopoly/`（933 张任务 / 真心话 / 35 张身份卡）
+ 授权全文（CC BY-NC 4.0，Ren & Puppy，非商业，署名保留）。

安全那几条一条没删：安全词 404（页面上是最显眼那个按钮，不藏二级菜单）、
14 个红线开关（kink 挡一道 + 内容正则再兜一道）、后庭默认关每人独立、
纯 top 开关、强度三档 + 前 2 回合热身 + 前半场不开顶、随时免费 skip。

**搬到的**：escalate 窗口曲线、发牌式反转累加器、防堵（连 3 张同强度抬窗口）、
跨局 LRU 去重、玩法类型/kink 轮换、高潮尾段优先插入卡、
`_anal_ok` 的骑乘活判、`_pen_ok` 的骨架判被插方、地盘/过路费/差遣、
监狱、未知格好坏事、功能卡 8 张全接了效果、同格对决、终局算账。

**没搬的（说清楚，别让下一窗以为有）**：
身份卡**发了、人设照念**，但只接了简单钩子（强度±/给币±/摸卡价/免疫监狱/
过圈奖励/过路费+1/未知格运气）。
🌀淫纹猜部位、😴睡美人、💱换任务配额、⛪告解抽成、对决的身份加成——**这一版没有**。
位置都在 `MonopolyGame.swift` 里留好了。

### 20 游戏页

顶上「网页 / 对话」两栏（跟聊天记录搜索一个样式，她自己点的这个方案），
栏里按类型分组（桌游/养成/益智/文字/其他）。长按本地 HTML 游戏可以改分类。

分类是按**怎么玩**分的，不按技术实现分——她说「我也不太确定是 mcp 还是什么」，
那本来就不该她操心。

`LocalGame` 加了 `genre` 字段，**所以补了容错解码器**——不补的话她之前放进去的
游戏会整份读不出来（就是 v90 交接第三节那颗雷）。

---

## 二、马上要做的：v91 还没构建

新文件：

```
Qi/Core/DesireEngine.swift  DollStore.swift  MonopolyEngine.swift  MonopolyGame.swift
Qi/Views/More/DesireBarsView.swift  DollRoomView.swift  DollWidget.swift  MonopolyView.swift
Qi/Resources/Monopoly/（三个 json + 授权）
改动：AppState.swift  AttentionSet.swift  Models.swift  NativeTools.swift
      ThoughtPool.swift  MessageBubbleView.swift  GamesView.swift  ThoughtPoolView.swift
      scripts/cmp.py
电脑那边：D:\dylan-heartbeat\memory-mcp.js
```

三项自查全过（`cmp.py` 全工程零不平衡、`dupdecl.py` 零重名、
`findbad.py` 只剩 `Models.swift:11` 那条老误报）。
`ChatMessage` 35 字段 / 35 行解码器对上，`AppSettings` 51 / 51 对上
（脚本这窗从交接文档里挪进了 `scripts/decoder_check.py`，别再往文档里贴那段 heredoc 了）。

**构建之前她可能要确认一件事**：`Qi/Resources/` 是这窗新建的目录。
Xcode 16 的同步组理论上会把里面的 json 当资源自动打包，
但**我没有 Xcode 验证不了**。要是构建出来 `MonopolyLib.ready` 是 false、
游戏页报「任务库没读出来」，那就是资源没进包——去 target 的
Copy Bundle Resources 里确认一下。

---

## 三、这窗踩的坑

### `cmp.py` 又有一个 bug（第三个）

它报 `DollRoomView.swift` 差两个 `{`，**代码一个字没错**。
原因：四种字符串是**一种一种依次剥**的，而 RAW（`#"..."#`）排在 STR 前面，
于是这行

```swift
DollWebView(accentHex: "#" + app.settings.accentColor.hexString)
```

里的 `"#"`，那个 `#"` 被当成原始字符串的开头，一路吃到后面某个 `"#`，
中间整片代码连括号一起被吞掉。

改成**四种一次扫过去**（一个交替正则）就对了：扫到那个 `"` 的时候
RAW 这一支匹配不上，STR 直接把 `"#"` 整个吃掉。
测过：真不平衡的文件照样报，`"#" +` 和 `#"a{b"#` 都不再误报。

⚠️ RAW 里那个 `\1` 是反向引用，合并之后还对，是因为排它前面的两支
一个捕获组都没有。**以后往前面加带捕获组的形态，记得改成命名组。**

所以 v90 交接里「它再报任何一条都是真的」这句，现在要加一句：
**报了先自己数一遍**（`scripts/` 里那个 braces 思路：剥字符串+注释后逐行累加深度）。

### 她看不懂日文

见第零节第 5 条。

---

## 四、还欠着的

### 第 9 条（唯一没做的老需求）

**记忆库参考（cognee / Hexis / Ombre-Brain / omnimemory / graphiti / codex / Aelios，
她特别点了 getzep/graphiti）+ 同步改电脑上的 MCP。**

`memory-mcp.js` 这窗已经动过一次（加了欲望那一摊，3372 → 约 3600 行）。
**动它之前先备份**，改完 `node --check`，再跑那四项验证
（工具名无重复 / 每个定义都有 case / load 的键都在 FILES 里 / save 同理）。
这一条建议**单独一窗做**。

### 旧账

- 世界书 / 指令注入 / 快捷短语（kelivo 抄来的，三个小的可以凑一轮）
- 语气的音高（FFT + Accelerate），记得先立 250–350ms 硬预算
- 「模糊玻璃还是差点意思」（她提过两次了）
- `eventide` 的 `dreams.py` / `triggers.py` 还没搬
- 大富翁那几个没搬的身份钩子（见上面 19）
- 娃娃现在是 SVG 简笔画。手上没有立绘素材——她要是想要更像的，
  得先有图，不然只能继续画线条

### 那几个 zip

`育儿模拟器`、`占卜参考` 还没解。

---

## 五、开工建议

1. 先让她构建 v91（这四条一批），出错就修
2. 然后**单独一窗做第 9 条**（记忆库 + 改 MCP）
3. 旧账那几件凑一轮

最后还是那句：**她很清楚自己要什么。看不懂就问，别自己脑补一个版本做出来。**
这窗问的三个问题（大富翁怎么接 / 娃娃放哪 / 游戏页怎么分）她全都给了明确答案，
其中「大富翁要不要模型参与」那个反问，直接决定了整条路怎么走——
问一句真的比返工便宜。
