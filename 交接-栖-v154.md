# 栖 · 交接给下一个窗（v154）

接 `交接-栖-v120.md`（那份里第二节那批坑**依然有效，照读**）。
v144–v154 的事全在 `需求清单.md` 里按日期记着，**那份是合同，先读它**。
这份只写「现在站在哪儿」「还欠什么」「别再踩的坑」。

工程在 `D:\OneDrive\桌面\qi-nativev65`，她的服务端在 `D:\dylan-heartbeat`。

---

## 零、开工前必跑

**这台机器没有 Xcode**，只能靠 GitHub Actions 构建。
交给她构建之前必须全跑一遍：

```bash
python scripts/cmp.py
python scripts/dupdecl.py
python scripts/findbad.py
python scripts/dupcase.py
python scripts/strspan.py
python scripts/decoder_check.py Qi/Core/Models.swift AppSettings
```

⚠️ **`scripts/strspan.py` 是 v154 新加的，别漏。**
它查「普通字符串字面量跨行」——v154 的构建就是挂在这个上：
我拿脚本往一段说明里插字，**转义换行写成了真换行**，
`Text("前半` 断在半截。而 `balance` 那类只数引号配不配对的检查
**逮不住它**（下一行末尾恰好又有个引号，看着就是闭合了）。

另外还有两个检查没进仓库、每次自己写一下就行：
· **括号／引号平衡**（按注释、字符串、多行字符串分状态扫）
· **顶层重名类型**（`struct/class/enum/actor/protocol` 不缩进的那些）

铁律（一直有效，一条都别打折）：

1. **她按次计费，不要为了省 token 做任何截断。** 带多少条历史都是同样的钱。
2. **除了「她跟他说话」和「他调工具」，任何会额外调模型的地方都必须是她主动点的。**
3. **她提的需求先照做，别自作主张改小。** 拿不准就问。
4. **不确定的地方要说出来，别装懂。**
5. **她看不懂日文。中文。人在国内，日本 IP 只是梯子。**

---

## 一、⚠️ 两条硬约束

### 仓库还是**公开**的

所以：**不许把任何密钥、任何第三方素材写进仓库。**

已有的做法照抄：`VoiceService.defaults` 里写着 `let key = ""`，
注释是「密钥不写进代码——仓库是公开的，写进去等于贴在墙上」。

**高德那把 key 只活在 `AppSettings.amapKey`（设置里的 SecureField）。**
她在聊天里贴过一次，已经建议她重新生成——**别再让它出现在任何文件里**。

素材：只有 **CC0 / 公有领域**（Kenney、OpenGameArt 过滤 CC0）才能进仓库。
itch.io / craftpix 的免费包她自己用没问题，**但不能提交**（那是再分发）。
她自己 AI 生成的图不受限。

### 转私人这件事在她手上

她说过构建没错误之后要把仓库转私人。**但先别催**：
公开仓库 Actions 分钟数无限，私有免费额度一个月 2000 分钟、
macOS 还要 ×10（≈200 分钟 ≈ 10–20 次构建）。
**边迭代边公开是对的**，等定下来再翻。

---

## 二、v150–v154 这几窗做了什么

一句话：**从「功能齐了」转到「用起来不硌手」。**

- **v150 太卡了** —— 两个根：`PixelSprite.color()` 每查一个像素就把整行
  重新拆成数组；`ImageStore.load` 每帧从磁盘重解 PNG。
  前者改成造的时候摊平成 `[Color?]`，后者加了 `ImageStore.cached`（NSCache）。
  **凡是画在会动的画面里的图，一律走 `cached`。**
  顺带：备份改成流式（导出边算边写、导入内存映射 + 手写 JSON 扫描），
  两头都搬到后台，**导完当场报「N 个图片语音」**。
- **v151 不规则切割** —— 裁整版图的时候按**连通域标号**把邻居的像素抹成透明。
  图必须是矩形，里面的内容不必。
- **v152 授权 · VAD · 自唤醒** —— 见下面第三节的行动项。
- **v153 clawd 进遮挡排序** —— 他不再永远压在家具前面。
  另外墙纸／地板可换（房间名旁边那支画笔）。
- **v154 厨房和浴室** —— 加了八件家具，那两间不再是空的。

---

## 三、⚠️ 交给她之前要说的两件事

### ① HealthKit 要在 Xcode 里勾一下

v152 加了「健康和待办」（设置 → 健康和待办，三个开关默认全关）。

**提醒事项和日历装上就能用**（只要 Info.plist 里那几句用途说明，已经写了）。

**健康要给 target 勾上 HealthKit capability。** 免费开发者账号能不能勾不确定，
所以 `HealthTools.swift` **故意不往 entitlements 里写任何东西**：
没勾的话只是授权那步失败、工具回一句「读不到」，
**App 照样装得上，提醒事项那半完全不受影响。**

如果她要健康那半真的能用，得让她在 Xcode 里加一次；
加不上就当没有，别为了它去动签名配置。

### ② 备份里的图片数

她报过「图片并没有备份，我之前写的关键词全都白瞎了」。
v150 把导出改成流式并且**当场报数**。

**下一窗第一件事：问她那个数是多少。**
· 不是 0 → 「导入备份进来的歌曲不能播放」多半是同一个根（二进制文件没进包），
  顺带就好了
· 是 0 → 判断错了，得重查

---

## 四、还欠着的

**卡在她那儿（我做不了，等她给东西）：**
- **仓库转私人** —— 她的决定，见第一节
- **OpenNomi 的音频实现** —— 我提议去扒他们的 Opus/VAD 那几个文件，她没回。
  我们自己那套 VAD 已经能用（`VoiceRecorder` 里的能量法 + 本底噪声自适应），
  他们那套的价值是**流式**，不是断句
- **amap key** —— 要她自己去高德开放平台实名认证拿一把 Web 服务 key。
  接的时候别忘了精度那条线还是她的（默认只到城市）

**她刚放弃的（别再提，除非她自己说起）：**
- **安卓机当常驻唤醒** —— 我给了完整方案（Termux + Tailscale + 一个
  返回 `{"say":true,"text":"…"}` 的小服务，栖这边接口早就留好了：
  设置 → 自动唤醒 → 「先问电脑上那份服务」）。**她说「算了，有点麻烦」。**

**我自己知道还没做的：**
- **墙和地板的默认长相** —— 现在她不换图的话还是纯色墙 + 棋盘格。
  换图那条路通了（v153），但没有内置的好看默认
- **`film-matinee`** —— 抽帧要电脑，一直搁着

**没有一份从头到尾对过的账。**
她的 bug 是分好几轮报的，我核对过的那批（塔罗往后选牌、音乐、
日记批注、表情多选删除、引用间隔、markdown、输入框工具）都在。
**但我不敢说清单是全的**——建议下一窗一开始就请她按页翻一遍。

---

## 五、这几窗新栽的坑（v120 那份第二节之外的）

### 1. 反斜杠，第八次

`\n` 在 heredoc 和 Python 字符串里转手两次就变成了真换行。
**往文件里写带反斜杠的内容，一律用写文件的工具**，别经 heredoc、
别经 Python 字符串。这一窗修检查脚本的时候**又栽了一次，同一个坑，隔了三分钟**。

### 2. 拿脚本改代码之前，先想「这一改会造出什么形状的错」

再想现有的检查抓不抓得住那个形状。抓不住就**先补检查**。
v154 是先撞了墙才想起来的。

### 3. 弹窗要挂在「按钮活着的那个分支」上

`.sheet` 挂在 `roomPage`（房间那一档），按钮在「柜子」那一档——
她点了没反应，切回房间才补弹出来。
**别挂在一个 switch 只走其中一路的公共尾巴上。**

### 4. 「X 没反应」先查有没有人调用过那个入口

她问「拖给 clawd 他不搬，是动画还没画吗」。不是：
动画一直都在，**`store.pickUp` 在整个小屋里一次都没被调用过**。
一件功能「看起来做了」可能是**上下游各做了一半、中间没接上**。

### 5. 同一件事只能有一个算法

`IsoRoom.fit` 的注释里写着「只此一份」，但那句话只管住了画屋子和摆家具，
**没管住走路——因为走路那边根本没引用它**。
「只此一份」要靠「所有人都来引用」成立，光在注释里声明是不够的。

### 6. `Text("**这样**")` 只有写死的字面量才渲染

一旦是拼出来的（`"前半" + "后半"`）或者带插值，Swift 选的是
`init<S: StringProtocol>` 那个重载，两对星号原样显示。
**拼出来的一律 `Text(MD.inline(…))`。**

### 7. 加新家具要动五处，漏哪处都不报错

目录 / `shape(of:)` / `home(for:)` / `IsoArt` + `isoSprite(of:)` /
**`RoomActs.act(_:)`**。最后一条最阴：漏了会编译过、能跑，
但他跑过去站着说一句「……」。
加完写脚本对一遍「`actions` 里用到的名字 vs `act` 表里有的」。

### 8. 推迟一件事的时候，把「为什么现在不做」写下来

clawd 不进深度排序推了好几窗，理由是「两套坐标」。
那个理由**在修别的东西的时候自己消失了**——
写下来了才能在某天发现它没了。

---

## 六、最后

她很清楚自己要什么，而且**她的判断经常比我的对**。这几窗她纠正过我三次：

· 「不规则切割」—— 她指出按矩形裁必然带上邻居。她对，
  而且指的是**我算完又扔掉的那条信息**（连通域标号）
· 「拖给他他不搬，是动画没画吗」—— 她的怀疑方向不对，但**问题是真的**，
  而且根子比她猜的更浅：手势根本没接
· 「配家具也是你的口语化」—— 她对。物件和功能的名称要正式，那不是聊天

**看不懂就问，别自己脑补一个版本做出来。**
