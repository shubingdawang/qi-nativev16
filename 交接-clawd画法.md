# 交接：clawd 的画法（未完成）

写给下一个窗口。**当前仓库是坏的，先读完再动手。**

---

## 一、现在是什么状态

`Qi/Core/PixelSprite.swift` 里的图纸已经整套换成了
[`clawd-emotes-skill`](https://github.com/xixicc186/clawd-emotes-skill)
那只（扁身子、四条腿、身侧两个小手），但是：

### ⚠️ 代码和图纸对不上，app 现在是错的

图纸换了身材，`Qi/Core/ClawdRig.swift` 里那些**写死的行号列号一个都没跟着改**：

| 常量 | 现在写的 | 按新图纸应该是 |
|---|---|---|
| `bodyLeft` / `bodyRight` | 8 / 31 | 5 / 34 |
| `bodyTop` | 4 | 16 |
| `legTop` | 21 | 34 |
| `armRow` | 8 | 24 |
| 脚底行 | 26 | 39 |

所以**戴帽子、举家具、招手、弯腿这些全都会错位**。
要么把这张表填进去，要么先回退到 `6837b1b`（换身材之前的最后一个好版本）。

### 已知画面缺陷（她当场指出来的，都还没修）

1. **腿缺几块、眼睛有虚影**
2. **道具颜色被吃掉**（玫瑰、蛋糕这些被压进了一个很小的调色板）
3. 飘着的爱心/音符没了（这条是**故意**的，见第三节）

---

## 二、腿缺块和眼睛虚影是同一个根因 —— 缩放倍数不是整数

**这是整件事里最要紧的一条。**

他们的图纸单位（SVG unit）里：

- 腿 = **1 单位宽**
- 眼睛 = **1 单位宽** × 2 单位高
- 手 = 2×2
- 躯干 = 11×7

我选了 **2.5 格/单位**（为了凑我们原来 40 格宽的画布）。
于是 1 单位 = 2.5 格 —— **落不到整格上**：
腿一条占 2 格、下一条占 3 格；眼睛边缘的抗锯齿被随机吸成一格深一格浅，
看着就是虚影。

| 格/单位 | 1 单位宽的腿 | 画布 |
|---|---|---|
| 2.0 | 2 格，干净 | 32 × 34 |
| **2.5** | **2.5 格，必碎** ← 现在用的 | 40 × 43 |
| 3.0 | 3 格，干净 | **48 × 52** |
| 4.0 | 4 格，干净 | 64 × 69 |

**下一步就是改成 3.0，画布 48×52。** 别再用 2.5。

---

## 三、图纸里装什么、不装什么

逐个动画量过（连通填充，只统计**跟身体连着**的部分）：

```
打电脑 0.1   看书 0.4   拍照 0.8   喝咖啡 1.0   打游戏 2.1
画画 2.9    洗澡 3.8   浇花 3.8   情人节(玫瑰) 4.4   吉他 4.6
听音乐 4.9   睡觉(被子) 6.6          ← 单位：躯干顶以上几个 SVG 单位
```

而**飘着的粒子**要到 25~26 单位（爱心、音符）。

所以：

- **图纸只装「握在手里的东西」**，上方留 6.6 单位就够
- **飘着的粒子单独画在图纸外面**（我们本来就有这套：
  `ClawdView` 里的 `dataBits`（干活的数据粒子）和 `.loving` 那圈爱心）

⚠️ 她说「你扣下来的爱心都是不整齐的」——就是因为我把飘着的爱心
也塞进了图纸，被裁剪框切掉了大半。**别塞。**

---

## 四、她反复强调、我反复做错的几条

按她指出的顺序列，每条都是我栽过的：

1. **「所有的都跟着一起改」**
   我总是只改一样（缩了电脑不改手、换了图纸不改行号常量），
   结果永远差一截。**改一个尺寸，跟它有关系的全部要重算。**

2. **「就算他是 svg+css，他动画的每一帧也是像素画」**
   人家仓库里带着渲染好的 GIF，直接扒像素，别照着源码里的数字自己画。

3. **取色要去渲染图上量，别照 SVG 源码写的。**
   `clawd-on-desk` 的 logo 源码写 `#FFFFFF`，但上面压着半透明发光圆，
   实际渲染是 `(182,222,240)`。按源码匹配会漏成透明。

4. **会动的东西一律发 GIF，不发帧图。**
   她的原话：「我是人不能只看帧。」

5. **改脚本用 `replace` 一定要 `assert`。**
   静默跳过一次，就是「说做了其实没做」，她一眼看穿。

6. **不要 `git checkout HEAD~N -- <file>` 去重跑生成器。**
   会把同一个提交里别的改动一起带走（粒子代码就这么被删过两次）。
   生成器都是幂等的，直接在当前文件上跑。

7. **动手改画法之前，先看这东西现在是怎么画的。**
   我在「举手」上翻了四次车，才发现手臂根本不在图纸里，
   而是 `ClawdRig.arm` 绕肩膀转的独立一块。

---

## 五、素材在哪

两个仓库（她指定用**第二个**，第一个她说「他也是弱智」）：

- ~~`rullerzhou-afk/clawd-on-desk`~~ / ~~`Angel2518975237/Clawd-on-Desk`~~ 弃用
- **`xixicc186/clawd-emotes-skill`** ← 用这个

里面有用的：

- `clawd-emotes/references/anatomy.md` —— **基础身体的坐标全写死在这儿**
  （躯干 x2 y6 w11 h7、脚 x∈{3,5,9,11} y13 w1 h2、
  手 x0/x13 y9 w2 h2、眼 x4/x10 y8 w1 h2、嘴 x6.4 y10.8 w2.2 h1）
- `clawd-emotes/references/prop-recipes.md` —— 道具怎么挂在手上的五种做法
- `clawd-emotes/gallery/*.gif` —— **24 个渲染好的动画**，直接扒像素

坐标换算（`viewBox="-15 -25 45 45"` 渲染成 240×240）：

```
每单位 = 240 / 45 = 5.3333 px
px_x = (svg_x + 15) × 5.3333
px_y = (svg_y + 25) × 5.3333
```

验过：躯干 x2..13 → px 90.7..149.3，跟实测的皮肤外框对得上。

---

## 六、还没做的

- **扫地丢了扫把**。他们的 `prop-recipes.md` 里有筷子、画笔、洒水壶、
  吉他、话筒、哑铃、海绵——**没有扫帚**。
  等她定：改成他们现成的「浇花」，还是照 Technique C 补一把。
- **道具的颜色**。她说「允许 clawd 拿的东西表情有其他颜色」——
  现在 `照抄表情.py` 里的 `TARGETS` 是一张写死的小表，
  玫瑰、蛋糕这些颜色被硬吸走了。应该改成
  **按每张图自己的颜色生成调色板**，只把**皮肤**吸到她的珊瑚红上。
- gallery 里还有一堆没接：喝咖啡、吃东西、弹吉他、唱歌、听音乐、
  画画、拍照、洗澡、打游戏、举哑铃，以及生日/圣诞/春节/七夕/中秋/端午/万圣节。

---

## 七、唯一要保留我们自己的东西

**肤色。** 他们是 `#D38E71`（偏褐），我们用 `#F2715F` ——
那是她拼豆图纸上的 `F23`，是 clawd 的身份。
形状和道具全用他们的，颜色用她的。

（她还没说要不要连肤色一起换成他们的，可以问一句。）

---

## 八、相关脚本

- `scripts/照抄表情.py` —— 现在这套转换的全部逻辑都在这儿，**改这个**
- `scripts/查一遍.py` —— 提交前必跑，22 项检查
- `scripts/抬肩膀.py`、`scripts/加高图纸.py`、`scripts/加宽图纸.py`
  —— 换身材之前那套图纸的历史迁移脚本，**现在已经没用了**，
  留着只是为了看当时为什么那么改
