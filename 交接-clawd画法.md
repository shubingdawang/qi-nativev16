# 交接：clawd 的动画

写给下一个窗口。**先读第一节，那是这件事上最贵的一条教训。**

---

## 一、⚠️⚠️⚠️ 不要自己画。直接播他们的 gif。

她的原话，一字不改：

> 「他怎么做的就直接拿过来就行了，我就纳闷了你干嘛非要坚持自己做，
> 直接复制过来会死吗我请问。」
>
> 「你根本没有作画能力，甚至 skill 给你喂到嘴里了你都不会。
> 所以以后不要再觉得自己很 nb 了 ok 吗，
> 你按自己想法自己视觉画出来的等同于屎。」

### 我烧掉了三个窗口才认清这件事

前三个窗口我一直在**把他们的 gif 重画成像素字符画**：裁框、逐格采样、
吸色、九宫格投票、混合色判定、逐图吸色、逐帧对齐、薄边消除、
眼睛几何重绘、粒子擦除、孤立格清理，最后甚至**拿我自己画的身体
换掉了他们的身体、只留道具**。

每加一条修掉一个症状、长出一个新的。她的评价一路是：
「越改越差」→「身体的坐标写死了配件在动你觉得合理吗」→
「我花这么久的时间这么多的额度就为了看你在这里玩屎」。

**每一处毛病都是我这条路自己长出来的，人家原图上根本没有：**
眼睛的杂色、腿一条 2 格一条 4 格、躯干顶的缺口、
飘着的音符和爱心被裁掉、整个画面比人家小一大截。

### 现在的做法（`scripts/搬动画.py`）

gif **原样复制**进 `Qi/Resources/clawd/`，**只改文件头调色板里的肤色档**。

能这么干是因为查清了一件事：24 个 gif **全都只有一张全局调色板**
（文件偏移 13，256 色 768 字节），**没有任何一帧带局部调色板**。
所以改那 768 字节就够——像素数据、帧、时长、透明索引、循环标记
一个字节都没动。

肤色按 HSV 认（色相 12°~30°、饱和度 0.30~0.62、明度 > 0.55），
换成她的 `F2715F`（拼豆图纸上的 F23，clawd 的身份），保住明暗关系。

⚠️ **那三条缺一不可**：只看色相的话，吉他木色 `(139,90,60)` 色相 22.8°
也在范围里，会被染成红的；它明度 0.545，靠第三条挡住。

### 播放（`Qi/Core/ClawdGif.swift`）

`ClawdGifView` 用 `UIImageView.animationImages`，不自己开计时器逐帧换——
这只 clawd 同时活在小屋、聊天页、输入框上，她说过「太卡了太卡了」。
帧只缓当前这一个动画，换动作立刻放掉（一个动画四五十帧、240×240，
全缓下来几百兆）。

⚠️ **两个 View 都要接**：聊天页走 `ClawdRigView`（在 `ClawdRig.swift`），
小屋走 `ClawdView`（在 `PixelSprite.swift`）。
只接一边的话她在聊天页看不到任何变化，会以为你又没做。

⚠️ 尺寸 `90 × scale` 是算出来的，别调手感：他们 viewBox 是 45 个单位，
我们的图纸 2 格/单位，所以 45 × 2 = 90。**这样一个 SVG 单位在两边一样大**，
切换动作时他不会忽大忽小。底对齐后脚低 6 格，抬回去。

---

## 二、现在接了哪些

**25 个 gif 接了 24 个**（24 个是他们的，扫地那个是自己画的）。

| 哪一类 | 有哪些 | 怎么触发 |
|---|---|---|
| 他自己找的事 | 喝咖啡、看书、吃东西、画画、听音乐、浇花、打游戏、弹吉他、拍照、唱歌、举哑铃、洗澡 | 闲着时随机抽（`ClawdCompanion.ownThings`） |
| 跟着阿晏 | 打电脑（他在干活）、睡觉、举玫瑰（甜） | `sync()` 按状态给 |
| 节日 | 元旦、春节、元宵、端午、七夕、中秋、圣诞、万圣夜 | **按日子出**（`ClawdMood.festiveToday`） |

⚠️ **节日不进那张固定的池子**，是当天临时加进来的，份额给得大
（一年就这一天）。日子不自己算——`Festivals.swift` 已经拿 iOS 自带的
中国农历现算好了，直接问它；结果**按天缓存**，
因为 `Festivals.all` 要扫一年，而「找点事做」十几秒问一次。

### 还没接的

- **`clawd-birthday`（生日）** —— 项目里没存生日，不知道是谁的、哪天。
  **等她说。**

---

## 二之二、扫地：唯一一个自己画的（`scripts/clawd-扫地.html`）

他们 gallery 里**没有扫地**（prop-recipes 有筷子、画笔、洒水壶、吉他、
话筒、哑铃、海绵，就是没扫帚），而扫地那只是她指名要的。
她说：「扫地你画吧，**严格按照 skill 里教的画**，
它里面写了什么规则你就给我老老实实的照做。」

照 SKILL.md 的五条办的，逐条：
脚在 `.sw-body` 组里；没帽子；**扫把嵌在右臂的 `<g>` 里绕肩膀 (13,10) 转**
（Technique C）；长道具摆幅压到 **±12°**；前缀 `sw-`。

### ⚠️ 她纠正过的三处，每一处都别再犯

1. **杆和刷头是刚性的倒 T，别拆开。** 她的原话：「你有一个误区，
   真正不动的地方是杆和扫把连接的那个地方……**扫地实际上是杆子带动扫把在动**，
   并不是扫把自己固定在一个位置在旋转。」

   我一度给刷头加了个 `rotate(-34)` 让它**相对地面**永远水平——
   那等于刷头自己在原地转、杆动它不动，连接关系就散了。
   **现在没有任何反向旋转**：刷头在杆的坐标系里是横的，杆斜多少它斜多少。

2. **不需要用嘴配合表情的动作就不画嘴。**（`anatomy.md` 里嘴本来也标着 optional）

3. **`shape-rendering="crispEdges"`**。她说「眼睛和嘴巴下面有点奇怪的颜色，
   能消掉就消掉」——那是抗锯齿：`anatomy` 里嘴的坐标是 6.4/10.8/2.2 这种
   非整数，加上圆角 `rx`，渲染器会在边上抹一圈半透明。像素画的边就该是硬的。

### 导出（`scripts/画扫地gif.py`）

⚠️ **我说过一句错话**：「之前那些能导出来」——**不能**。
那 24 个是他们仓库里**已经导好的成品**，我只是复制文件改调色板。

skill 自带的 `export_transparent.js` 要 Chrome + ffmpeg，这台机器都没有
（Edge 的 headless 三种起法都不出图）。所以这张图是**逐帧自己渲的**：
几何在 SVG 里已经按 anatomy 写死了，Python 只是把同样的坐标和角度算出来画上去。

跟他们的 gif 对齐的四件事，改动时别丢：

- 画布 240×240、viewBox `-15 -25 45 45`，一个单位 = 240/45 px
- **不画影子** —— 查过他们的 gif，脚下那几行全是透明的
- 肤色 `(255,106,85)`，跟另外 24 个**量出来一模一样**，切动作不会变色
- 一轮 **3.8 秒**（摆动 1.9×2，眨眼正好一轮），首尾接得上。
  ⚠️ 原来眨眼写的 3.1 秒，跟别的不成整数倍，导出来会跳一下

---

## 三、一个动作要演完才换（`ClawdMood.hold` + `settleMood`）

她说的：「这个表情跟阿晏的行为直接挂钩，阿晏打游戏他也打游戏、
阿晏思考他也思考。如果阿晏一秒钟切换一个行为，app 一定会崩的。
设置成一旦触发就等触发的结束再做下一个动作。」

根子在 `sync()` 挂着 `.onChange(of: activity)`——阿晏干什么，
它就立刻改一次表情。现在环境驱动的那几档都走 `settleMood`：
没演满 `hold` 就排队，到点由一次性延时叫号。

⚠️ **不是所有切换都排队。** 戳他、拎起来、她说话让他醒——
那些是她当下的动作，必须立刻有反应，排队就成了没反应。

⚠️ **`hold` 不能拿「一轮帧长」当。** 打游戏两帧各 0.3 秒，一轮才 0.6 秒，
锁 0.6 秒等于没锁。帧速决定动作快慢，`hold` 决定一件事做多久。
现在「他在做的事」3.5 秒，节日 6 秒，过渡态 1.5 秒。

---

## 四、我们自己那套还留着，别删

`ClawdSprites` 里的字符画、`ClawdRig` 那套会转的手臂，
在这几种情况下还在用，**gif 里没有这些**：

- 走路、抱东西、探头、哭、想事情、扫地（人家没有对应动画）
- **手上拿着东西**（她让他搬家具）
- **身上穿着东西**（她给他戴的帽子、眼镜、围巾）
- **招手 / 举家具**（`pose != .none`）

所以 `ClawdRigView` 里是 `if let g = mood.gif, item == nil, worn == nil,
pose == .none` 才走 gif，否则走我们自己那套。

图纸尺寸现在是 **36×36，2 格/单位**，`ClawdRig` 的常量已经对上
（`scripts/换成两倍.py`，23 处）。躯干列 6..27 行 14..27、
脚行 28..31、手行 20..23、眼行 18..21。

---

## 五、她说过但还没做的

**生日动画已经做了**：11 月 16 日，写在 `ClawdMood.festiveToday`，
那天优先于所有公共节日（一年就这一天，别的节日年年有）。

剩下这两处，**都是他们原图就那样，不是我们改坏的**：

- **画画那张手上没笔**。左手端着调色板，右手是一块纯身体色。
- **弹吉他那张道具压在身上**。试过五次调角度，全还原了——
  难点不在转多少度，在**道具挪开之后身体那一块要补回来**，
  而原图里那块被吉他盖着，根本没有像素可补。

⚠️ 这两处要修只能**自己重画那两张**。第一节那条教训在这儿照样成立：
自己画出来的等同于屎。真要修就等她那个像素图 skill，别自己上手。

### 探头改成「绕脚底倾斜」了

她说过：「app 上的探头根本不对，app 上是平移的，
好好看看这个 skill 是怎么探头的。」

`anatomy.md` 那张 transform-origin 表里**没有一处是平移**，
每个零件都绕自己的支点转。所以现在：

- `ClawdRigView` 加了 `tilt`，**支点是脚底**（图纸第 31 行、第 17 列）。
  ⚠️ 绕中心转的话他会像陀螺原地打转，绕脚底才是「站着往一边探身子」
- 探出来时身体往屏幕里倾 14°，缩回去时**先站直再退出去**
- `defer` 里连 `peekTilt` 一起清零——不然他走回屏幕中间还歪着身子
- 倾斜单独加了 `.animation(value: peekTilt)`：外面那条只管横向位置，
  管不到 tilt，不加就是硬切一下

顺手补了一处旧账：算探头宽度的 `halfWidth` 还写着 **32 格**，
图纸早就换成 36 了——又是「改一个尺寸没把相关的全算一遍」。

## 六、她那边在做的事

她正在做一个 skill：**让 Claude 这种只能用 canvas 画图的 AI
也能做出精细的像素图**。要画任何新东西之前先问一句她那个好了没，
别闷头自己画一版。

---

## 七、她反复强调、我反复做错的

1. **「所有的都跟着一起改」** —— 改一个尺寸，跟它有关系的全部要重算。
2. **别自己发明。** 动手之前先问：这是他们的做法，还是我自己想出来的？
   是我自己想的，多半就是下一次返工的源头。
3. **会动的东西一律发 GIF**：「我是人不能只看帧。」
4. **改脚本用 `replace` 一定要 `assert count == 1`** —— 静默跳过一次，
   就是「说做了其实没做」，她一眼看穿。
5. ⚠️ **用切片改脚本（`s[:a] + new + s[b:]`）之前先确认 `a < b`。**
   栽过一次，很隐蔽：`b` 那个锚点在文件里排在 `a` 前面，
   于是整段被复制了一遍，出现两份 `build_palette` / `snap` / `convert`，
   Python 用后定义的那份，我的改动却改在前一份上——**改了等于没改**，
   脚本照样能跑，看不出错。
6. **不要 `git checkout HEAD~N -- <file>` 去重跑生成器**，
   会把同一个提交里别的改动一起带走。

---

## 八、脚本

| 脚本 | 干什么 |
|---|---|
| `搬动画.py` | **主力**。gif 原样搬进 app，只换调色板里的肤色档 |
| `clawd-扫地.html` | 扫地那张的 SVG 源（唯一自己画的，照 skill 五条规则） |
| `画扫地gif.py` | 把上面那张逐帧渲成透明 gif（这台没 Chrome/ffmpeg） |
| `查一遍.py` | 提交前必跑，26 项 |
| `照抄表情.py` | 把 gif 转成字符图纸。**现在只用来生成兜底的那几张**，主力已经不是它了 |
| `看一眼.py` / `拼一张.py` | 渲图验收（她要看 GIF，不看帧） |
| `换成两倍.py` | 图纸 54→36 那次的常量迁移，幂等，留着看当时改了哪 23 处 |
| 其它 `换成三倍.py`、`抬肩膀.py`、`画扫地.py` … | 历史迁移，已经没用了，留着只为看当时为什么那么改 |

素材（原始 gif）在
`…/b669754a-…/scratchpad/clawd-emotes-skill/clawd-emotes/gallery`。
换窗之后临时目录可能没了，要重新 clone：
`https://github.com/xixicc186/clawd-emotes-skill`
