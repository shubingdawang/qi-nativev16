# 栖 · 交接给下一个窗（v99）

接 `交接-栖-v91.md`。v92–v99 的事全在 `需求清单.md` 里按日期记着，
**那份是合同，先读它**。这份只写 v91 之后交接文档漏掉的那几窗，
以及这一窗（v98/v99）踩到的坑。

工程在 `D:\OneDrive\桌面\qi-nativev65`，她的服务端在 `D:\dylan-heartbeat`。

---

## 零、先读这一段

**这台机器没有 Xcode。** Swift 只能靠 GitHub Actions 构建，一轮几分钟。
交给她构建之前，`scripts/` 里那几个自查脚本必须全跑：

```bash
python scripts/cmp.py && python scripts/dupdecl.py && python scripts/findbad.py
python scripts/decoder_check.py Qi/Core/Models.swift ChatMessage
python scripts/decoder_check.py Qi/Core/Models.swift AppSettings
python scripts/decoder_check.py Qi/Core/Models.swift Conversation
```

（`Conversation` 会报一条 `activeMembers` 缺失——那是计算属性，脚本认不出来，
是老误报。`findbad.py` 那条 `Models.swift:11` 同理。）

铁律（一直有效）：

1. **她按次计费，不要为了省 token 做任何截断。**
   ——注意这也意味着**带多少条历史都是同样的钱**，压缩不是为了省钱。
2. **除了「她跟他说话」和「他调工具」，任何会额外调模型的地方都必须是她主动点的。**
3. **她提的需求先照做，别自作主张改小。** 拿不准就问。
4. **不确定的地方要说出来，别装懂。**
5. **她看不懂日文。中文。**

---

## 一、这一窗（v98 → v99）做了什么

### 1. 第 9 条的后半：另外六个记忆库仓库

graphiti v96 做过了。这轮读剩下六个，挑的标准只有一条：**不能多调一次模型**。

- **Ombre-Brain**：未了结优先浮出、零参数浮出（它叫 `breath`）、核心信念上限 20
- **omnimemory**：复合分「相关度 × (1 + 重要度)」、太像就别重复记（75/100）
- **Aelios**：候选队列（他拿不准的先放着，她点头才进库）、术语表 glossary
- **Hexis**：只取一句思路（确定的和推断的分开存）
- **cognee / codex**：**没搬**，理由写在 `需求清单.md` 那张表里

三个仓库的向量检索全没搬（要么联网要么本机跑模型），换成 **BM25 + 字的二元组 +
整串命中加成**，纯算术。实测搜「日本的房子」能命中「她在日本租的那间房子很小」，
以前 `contains` 一条都搜不到。

算法都在 `Qi/Core/MemoryRecall.swift`，电脑那边 `memory-mcp.js` 里有一份**一模一样**的
（常数、公式、字段全对齐，改一边记得改另一边）。

新工具四个（两边都有）：`surface_memories` / `mark_memory` / `propose_memory` / `glossary`。

⚠️ **遗忘曲线（λ=0.05）做出来又删了。** 她看完说：
「记忆库本来就是存住记忆的，又遗忘感觉有点鸡肋……时间长了模型自己也会把注意力
分散掉，也是要去专门查，所以作用貌似不大。」她说得对——筛选手段已经够了。
现在时间只剩「同分时新的排前面」。**别再把它加回来。**

### 2. 滚雪球压缩（`Qi/Core/ContextCompactor.swift`）

她的原话：「因为我不想换窗 所以上下文压缩是肯定的……
**我希望能保持一个窗口一直不忘记他自己的人格和说话方式**」。

- 每 60 条压一次（设置 → 通用，可调 40/60/100/关）
- 滚雪球：第二次压的是「上一次的浓缩件 + 这一段新的」
- **最后 6 条不压**（她刚说完的话得留原文）
- 发给他的 = 系统提示 + 最新浓缩件 + 没压过的原文

**关键的一半是原话样本**：除了模型写的浓缩件，还机械地挑一份他真说过的原句、
一字不改（不调模型、不花钱），按「最早几句 + 最近几句」留 12 句。
摘要是转述，转述过的话不再是他说的话——这一份才是「不忘记怎么说话」的落点。

**这套还没被真正触发过**（要聊满 60 条）。下一窗留意她会不会说「他突然变了」，
真出问题先看那一窗「对话设定 → 滚雪球压缩 → 看看压成了什么」。

### 3. 她装上 v98 后提的三条 bug（v99 修完）

- **长按菜单全没了** —— 根子是气泡上那句 `.textSelection(.enabled)`：
  iOS 文字选择自带长按手势，优先级压过我们那条，弹出来的是系统「拷贝 | 分享…」。
  **删掉它就回来了。**（以后再改气泡要记着这条。）
- **报错之后聊不下去** —— 红方块以前是死的。现在挂「重试 / 删掉这条」，
  新增 `AppState.retry(_:in:)`。
- **编辑历史 ‹›** —— `ChatMessage.edits` 新字段。
- **旧备份新构建不认** —— 文件选择器加 `.data`（微信存下来的那份系统认不出类型，
  是灰的选不中）、剥 BOM、**不检查 version**、解不开时报出字节数和开头。
  她那份 `栖备份-20260818-1840.json` 我拆过：5.6 MB、v2、格式完全正确。
- **足迹填不了小数** —— `TextField(value:format:)` 每敲一键解析一次，
  「0.」被解析成 0 立刻回写。换成 `PriceField`（收键盘时才解析）。
- **他不知道该存哪张图** —— 两个根子：`recentUserImage` 只看 `imageNames`，
  **表情消息（`stickerID`）一张都数不到**；以及图根本没有号。
  现在两种都认，且消息里会标「从左到右是 #3 #2 #1」，`#1` 永远最新，
  跟工具的 `index` 对齐。新工具 `list_recent_images`。

---

## 二、这一窗踩的坑（下一窗别再踩）

### 1. 用 Bash 的 heredoc 改文件，反斜杠会被吞

`python - <<'PY'` 里写两个反斜杠加 n，落到文件里会变成**真的换行**，
Swift 字符串当场断掉；Swift 多行字符串里行尾那个续行反斜杠会被 Python 直接吃掉。

**做法**：脚本里写 `B = chr(92)`，正文用占位符 `@BN@` / `@B@`，最后 `.replace()` 回去。
这一窗因为这个白改了三次。

### 2. 一条命令太长会被截断

超过大约 8–10 KB 的 heredoc，末尾终止符会丢，bash 报
`unexpected EOF while looking for matching`。**写大文件要分几段追加**（`'a'` 模式）。
另外 python 三引号字符串里不能出现三个连续的双引号，写 Swift 多行字符串要用占位符。

### 3. `NativeTools.add()` 没有 `required` 默认值

`MemoryTools` 里那个有默认值，`NativeTools` 里那个**没有**。
加工具时必须显式写 `required: []`，不然编译报 missing argument。
这一窗因为这个废了她一次构建。

### 4. SwiftUI 结构体里，`@State` 别夹在存储属性中间

`@State private var editPage` 插在 `onRetry` 和 `onCloseMenu` 之间，
会把外面按顺序传参的调用搞错位（编译器报「argument X must precede argument Y」）。
**@State 一律放在所有存储属性后面。**

---

## 三、还欠着的（按她说的顺序）

详见 `需求清单.md` 最后几节，这里只提最要紧的：

- ⚠️ **下一窗第一件事：拿 PWA 的源码逐条对一遍**（`D:\my-app\www\app.js`，6878 行）。
  她的原话：「我才发现 pwa 里很多的功能在 app 里都没有…前面的窗偷懒了。」
  已经对出来少了相册那四个工具（`send_photo_from_folder` / `list_saved_photos` /
  `delete_photo_from_folder` / `delete_folder`）。
- 提高工具自主性 p6 那份排查的①②（能力块要真的进 System / 末尾锚点压制主动性）
- 保存删除图片那张小卡 UI（p11）· PhotoStack · 他放音乐那张卡（p5）
- clawd 房间几个 bug · 表情工坊 · 手帐素材库 · 塔罗牌面
- 设置里「这是什么」贴到对应项 · 主题「底」那张卡删掉
- markdown 渲染（她看到加粗标记原样显示）——
  **注意 `SettingsNote` 和 `MarkdownText` 都已经会解析了**，
  下次要先问清楚是在哪一页看到的，别瞎改一通。

---

## 四、最后

她很清楚自己要什么。**看不懂就问，别自己脑补一个版本做出来。**
这一窗问得最值的一次是遗忘曲线——做完了她说不要，删掉反而是对的；
要是当初先问一句，那半天就省下来了。

她这周额度已经用满（100%），下周才重置。**下一窗开工前先确认她的额度，
能一批做完的别拆成三次构建。**
