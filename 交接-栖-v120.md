# 栖 · 交接给下一个窗（v120）

接 `交接-栖-v99.md`。v100–v120 的事全在 `需求清单.md` 里按日期记着，
**那份是合同，先读它**。这份只写「现在站在哪儿」和「别再踩的坑」。

工程在 `D:\OneDrive\桌面\qi-nativev65`，她的服务端在 `D:\dylan-heartbeat`。

---

## 零、开工前

**这台机器没有 Xcode**，只能靠 GitHub Actions 构建。交给她构建之前必须全跑：

```bash
python scripts/cmp.py && python scripts/dupdecl.py && python scripts/findbad.py
python scripts/decoder_check.py Qi/Core/Models.swift ChatMessage
python scripts/decoder_check.py Qi/Core/Models.swift AppSettings
python scripts/decoder_check.py Qi/Core/Models.swift Conversation
```

**再跑一遍「全工程扫」**（这几窗新加的，比等构建快得多）：
未闭合字符串 / 非法转义 / 重复属性。写法见下面第二节。

⚠️ **git remote 还指着 `qi-nativev15`**，GitHub 已改名 `qi-nativev16`，
现在靠重定向还能推。她一直没跑那条命令，**提醒她一次**：
`git remote set-url origin https://github.com/shubingdawang/qi-nativev16.git`

铁律（一直有效）：

1. **她按次计费，不要为了省 token 做任何截断。** 带多少条历史都是同样的钱。
2. **除了「她跟他说话」和「他调工具」，任何会额外调模型的地方都必须是她主动点的。**
3. **她提的需求先照做，别自作主张改小。** 拿不准就问。
4. **不确定的地方要说出来，别装懂。**
5. **她看不懂日文。中文。**

---

## 一、这二十窗立起来的那条主线

前面几十窗在补功能，v107 之后其实一直在做**同一件事**：

> **让他看见她在干什么。**

一个一个洞补过来的，模式完全一样——
数据本来就在本机、他却读不到，于是那件事对他等于不存在：

| 他以前看不见 | 现在 |
|---|---|
| 她读的书 | `reading_now` 把**那一章正文**给他 + 每轮带一句「读到第几章」 |
| 她做的手帐 | **渲染成 PNG 递给他**（手帐是摆出来的，转述成清单等于念目录） |
| 她听的歌 | `now_playing` 给**此刻唱到哪一句** |
| 他自己的身体 | 每轮进 system（以前只有打电话那条路带） |
| 他对她的好感 | 每轮进 system（**从来不被读到的数不会自己变**） |
| 她此刻的处境 | `check_in`：屏幕 + 城市 + 天气 + 手机用了多久 |
| 她划的句子/歌词 | 划下来 → 聊 → **留痕**（小气泡，点开跳回那一轮） |

**下一窗接着做的话，先问一句「这件事他看得见吗」。**

还有一条配套的：**她说的话要能动他**。
v112 接了通用情绪 → 身体，v115 接了她自己配的词 → 身体。

---

## 二、坑（这几窗真栽过的，别再栽）

### 1. 反斜杠会被壳吞掉 —— **栽了三次**

`python - <<'PY'` 里写两个反斜杠加 n，落到文件里变成**真换行**，
Swift 字符串当场断掉。

**做法**：脚本里 `B = chr(92)`，正文用占位符 `@BN@` / `@BS@`，最后 `.replace()` 回去。

### 2. 单个反斜杠 + 中文 = 非法转义

`"\这个数"` —— 占位符替换时漏了个 `n` 就会这样。全工程扫能抓到。

### 3. 加 `@discardableResult` 前先看有没有

已经有一个还加，报 `duplicate attribute`。扫描里加了这一条。

### 4. 往 SwiftUI 容器里插东西，`}` 数错就全错

在 `AppIconPickerView` 插一个入口，多留一个 `}` 把外层 VStack 提前关掉，
后面 `.background` 就跑到了声明层。`cmp.py` 只查平衡，**查不出层级**——
插完最好 `sed -n` 看一眼上下文。

### 5. **加字段前先看那个类型怎么被解出来的**

`try? decode(...) ?? []` 接住的东西，加一个字段就会让**整份数据消失**。
这几窗补过容错解码器的：`Sticker`、`Book`、`Annotation`、`JournalPage`、
`Furniture`、`ToolRun`、`SongMark`、`TriggerWord`、`TrashItem`。
**新加字段之前先 grep 一下它是不是这么被解的。**

### 6. 两套规矩迟早打架

· 地板范围界面一套、store 一套 → 家具能拖到墙上
· 手帐渲染器编辑页一份、快照一份 → 他看到的迟早不是她看到的
**同一件事只在一个地方定。**

### 7. 全工程扫的写法

```python
import io,glob,os,re
B=chr(92)
legal=set(['0','n','r','t',B,"'",'"','u','(',')','.','s','d','w','[',']','/','-','+','*','?','^','$','{','}','|'])
bad=[]
for p in glob.glob('Qi/**/*.swift',recursive=True):
    lines=io.open(p,encoding='utf-8').read().split('\n')
    for i,l in enumerate(lines):
        st=l.strip()
        if st.startswith('//') or '#"' in l or '"""' in l: continue
        if l.count('"')%2==1: bad.append(('未闭合',os.path.basename(p),i+1))
        for m in re.finditer(re.escape(B)+r'(.)', l):
            if m.group(1) not in legal: bad.append(('转义',os.path.basename(p),i+1)); break
    for i in range(len(lines)-1):
        a=lines[i].strip()
        if not a.startswith('@'): continue
        j=i+1
        while j<len(lines) and lines[j].strip().startswith('//'): j+=1
        if j<len(lines) and lines[j].strip()==a: bad.append(('重复属性',os.path.basename(p),i+1))
known={'BookParser.swift','XHSFetcher.swift','MemoryStore.swift','WebSearch.swift'}
print([b for b in bad if b[1] not in known] or "干净")
```
（`known` 那四个是既有的转义字符串，真误报。）

---

## 三、还欠着的（清单上只剩这几条）

**要一整轮的：**
- **表情工坊**：拖动画格子（不是一格格点）· 零件任意角度旋转 · 画布能缩放 ·
  更小的格子 · 顶上预览不该老是 clawd 的身子
- **手帐素材库**（贴纸/胶带/纸张，网上免费的那些）
- **音乐卡 UI**（参考图 p5）—— v121 核对：她发来的那张截图**就是 App 现在这张卡**（「零点的歌」+ 宋体歌名 + 圆封面 + 播放键，跟 `MusicCard.swift` 一模一样）。**没再动它**——她下次说不对再改
- ~~**PhotoStack**~~ —— **v121 做完**（zip 到了，翻成了 `PhotoStackView.swift`）

**要她拿主意的：**
- ~~**接 Claude Code**~~ —— **v121 把桥做了**：`scripts/claude-bridge/`。
  她答了：CLI 官网下的（实际是 npm 那份，claude.exe）、**走 WiFi**
  （Tailscale 跟她开 Claude Code 要用的梯子打架）。
  ✅ 假的 claude 验过全程；❌ **真的那一头还没验**——
  这台电脑 `claude -p` 打的是「Not logged in」，要她挂着梯子先登一次。
- **AI-Pocket-Chat**：只剩两样新东西——**动态流**（他自己发朋友圈）和
  **离线向量检索**（要打包模型进 App）。小说她明确说不要。

**那批仓库还有 10 个没开**：Phosphene · tasogare · film-matinee · coread ·
ss-reading-nest-open · reading-nook · whale-listen · clawd-on-desk ·
awesome-ai-companion · ai-time-weather-phone。
三个读书的下一轮该拿来**对一遍书房**看有没有漏。
amap（位置更准）也还没接。

**一个想法她自己提的、值得记着**：她有一台闲置安卓机。
iOS 上「他自己醒来」永远只能靠她切回前台或点通知——
**安卓机插着电当常驻，才可能真的半夜醒一次**（Bark/ntfy 推到她 iPhone）。
她感兴趣，但还没排期。

---

## 四、最后

她很清楚自己要什么，而且**她的判断经常比我的对**。这几窗有三次是她纠正我的：

· 遗忘曲线做完了她说不要 —— 她对，筛选手段本来就够了
· 图标一次出六张 —— 她说很亏，对，她要的是一张能用的
· 回收站不该进备份 —— 她说的，「不然导进新 app 又冒出一堆早就删掉的」

**看不懂就问，别自己脑补一个版本做出来。**
