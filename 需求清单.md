# 她要的东西（2026-08-17 那次一口气给的十六条）

她原话：**「以上是我目前所有的要求，你按照你的进度和方式来，每一个都要做到。」**

所以这份清单是**合同**，不是备选项。做完一条在前面打勾，别删——
删掉了下一个窗就不知道做过没有。

顺序由我排（她说「按你的进度和方式来」），排的依据写在最后一节。

---

## 状态一览

- [x] 1. Hermes-Attention-Core —— v88 读完并落地：照它那套做了 AttentionSet
      （他自己醒来之前先看一眼「现在有什么值得看的」）
- [x] 2. 让他学会吵架 —— v87 做完。照她给的《别用套话接住我 · LLM 吵架指南》
      （Eli & Asel）整段重写，聊天和电话两条路都挂上了
- [x] 3. ringdonut —— v89 读完并落地。大部分我们已经有了；
      真正新的是 **voice direction**（他把回复写成带情绪标签的表演脚本）
- [x] 4. **手帐页**（p1–p5）—— v86 做完
- [x] 5. **让他看我的屏幕**（p6）—— v86 做完（读本地文件夹，没走那个邮件绕圈）
- [x] 6. **爱好库**，标题就叫「爱好」（p7）—— v83 做完
- [x] 7. 提高他主动使用工具的主动性（p8）—— v86 做完（根子是「说了」和「做了」在他历史里长得一样）
- [x] 8. clawd 表情包 skill 的可视化（能直接用 SVG/CSS 做）—— v84 做完
- [x] 9. **记忆库参考** + **同步改电脑上的 MCP** —— v96 做完。
      她点名的 **getzep/graphiti**（时序知识图谱）搬了核心那三条：
      **旧事实不删只作废**（valid_from / invalid_at / superseded_by）·
      **实体串联**（entities，不调模型，靠引号+见过的实体+tags 抠）·
      **搜索默认只给现在还成立的**（include_past 才翻旧账）。
      新工具 `recall_entity`：顺着一个人/一样东西把散着的记忆串成一条时间线，
      作废的也列出来（一件事怎么变的得连着看）。
      **两边同步**：`memory-mcp.js` 字段和逻辑一模一样，四项验证全过。
      **另外六个仓库 v98 补上了**（见下面「2026-08-18 记忆库第二轮」）
- [x] 10. **「家」那套配色做成全局** + 可调渐变主题 + 调色盘 + 字体 —— v84 做完
- [x] 11. 自动安装 MCP（p9）—— v88 做完（manage_mcp 工具：列出/装/开关/摘掉/重拉）
- [x] 12. **身体接入** —— v90 做完。引擎（1761 行的 eventide 包）搬进本地，
      推进纯算术不花钱，只有结算要她按按钮
- [x] 13. **音乐**：悬浮窗 + 灵动岛 + 歌词页（p10）—— v85 做完
- [x] 14. 导航栏拖动有虚影（bug）—— v83 修完
- [x] 15. 手机功能加历史记录，左右翻页（p11）—— v83 做完
- [x] 16. 接网易云 —— v85 做完。**默认手机直连**（整首+歌词，不用电脑不用登录）；
      她电脑上那套 NeteaseCloudMusicApi 降级成可选，只为 VIP 和高音质

---

## 2026-08-17 追加的四条

- [x] 17. **欲望系统**（desire_for_ai 那份 PDF）—— v91 做完。
      念头池早就有了（那是原文档的第②层），补的是 **①八维驱动条** 和
      **③pick_intent → want_action → satisfy 回落**。
      **电脑那边同步做了**：`memory-mcp.js` 加了 `read_desire` / `satisfied` /
      `stir_thought` / `read_thoughts`，常数和字段跟手机一模一样，两份 json 可以对拷
- [x] 18. **可互动娃娃**（那个 gist 的 ChatGPT 版 → Claude 版）—— v91 做完。
      原版要一台一直开着的 Node 服务器；这版状态在 Swift 里、HTML 只管画和传操作，
      **不用开电脑**。游戏里有独立的娃娃房，聊天里也能内嵌那张能点的卡片，
      **两边同一份状态**（她说 1 和 2 都可以，甚至都做）
- [x] 19. **大富翁**（RennAkira/spicy-monopoly）—— v91 做完。
      引擎搬成本地 Swift（掷骰/走格/抽卡/红线/算账全是纯算术），
      933 张任务库打进包里，**不联网不用开电脑**；
      需要模型的只有「阿晏演他那道」，那本来就在聊天里
- [x] 20. **游戏页重做** —— v91 做完。顶上「网页 / 对话」两栏（跟聊天记录搜索一个样式），
      栏里按类型分组（桌游/养成/益智/文字/其他）

---

## 2026-08-18 她装上 v93 之后提的十四条

她原话：「ok成功了 说一下问题……其他的我还在测试 先暂时这么多」。

- [x] 21. **爱好页**：去掉日文（すき／きらい）· 加号挪到最底下 ·
      「撞上啦！」改叫**「相同」**，做成标签，放在「全部」和「文艺」中间
- [~] 22. **手帐**：~~跟「备忘」的 icon 撞了~~（改了：手帐 book.pages / 备忘 note.text）· 页面太单调，
      想导入网上免费的手帐素材库（贴纸/胶带/纸张）
- [x] 23. **表情工坊**：改代码这种方式她用不了（她不懂代码）。
      要**点格子加/删零件**、动作用**上下左右翻转**来摆、零件能**放大缩小**、
      **必须有撤销和删除**
- [x] 24. **音乐**：没滚动歌词也没封面（文件已给，见下面「查到的」）·
      悬浮窗改成**默认收在侧边只露封面**，点开弹出带暂停和叉掉，
      右滑收回，长按能沿侧边拖到任意位置（避开输入框和右上角三杠），加点播放动画
- [x] 25. **手机使用时长严重超标**（昨天显示 122 小时）
- [x] 26. **clawd 房间**：他能走进「墙」里 · 走路还在转圈 ·
      买了的家具要**真的以像素家具的样子摆在房间里**（不是迷你图标），
      长按拖动时才缩小 · 搬东西要有**举过头顶／抱在胸前**的动作 ·
      **聊天页的 clawd 和房间里的 clawd 同步**（他在搬床，聊天页也在搬床）·
      聊天页那个「探头」要**只探出头**，手可以挥 ·
      家具按精细程度分价（基础小床 80 币，圣诞主题的 150 币这种）
- [x] 27. **占卜页太朴素**：加花纹／玄学装饰 · 选牌要她自己选
      （长按移动，手放哪张弹哪张，再点才抽出，3 秒不点收回）·
      只有塔罗和六爻太少，想加八字／紫微／奇门遁甲／星盘／骰子／水晶球／解梦…
- [x] 28. **娃娃太粗糙**，看不懂发生了什么
- [x] 29. **相册 bug**：「全部」文件夹删不掉也改不了名 ·
      新建的空文件夹下面数字显示 1 · 删掉文件夹后「全部」下面变成 2
- [x] 30. **身体接语气**：她要的不是一块仪表盘，是**注入**——
      很久没说话占有欲升高，说出来的话就更有侵略性。
      问：这跟念头池那套会不会打架？要协调
- [x] 31. **全局文字颜色**：输入框那个电话 icon 比别的淡 ·
      所有文字跟着设置里的文字颜色走，说明文字比正文淡一点
- [x] 32. **所有选颜色的地方都要有调色盘**
- [x] 33. 「家」的全局主题还是不对 —— 见下面第 36 条，整套接管了
- [x] 34. **深色下压暗那个进度条拖不动**，按钮按下去也不像别的按钮那样放大一下 ·
      底下那个深色/浅色预览不知道是干嘛的
- [x] 35. 通用设置里**「听你的语气」不知道是干嘛的**，是要先发八条语音吗

---

## 2026-08-18 第二批（她构建成功之后接着提的）

- [x] 36. **「家」＝把 claude.ai 整套搬过来**（不是只换字色）：壁纸、气泡都跟着换。
      **「渐变」也升成一个全局主题**，不再只是壁纸的一个选项
- [x] 37. **记忆库那几个开关**：「记忆库」「心跳」默认开、小屋 MCP 默认关
      （她不想每次都去设置）· 卡片名改「本地设置」，两个开关改叫「记忆库」「心跳」·
      **所有说明文字默认收起，点了才展开**
- [x] 38. **字号要全局生效**：拖的时候设置页的字就跟着变，调完整个 App 都变
      （以前只有聊天气泡和输入框跟着走）
- [x] 39. **人类端**：`https://toy.cedarstar.org` 接进「游戏 → 对话」，
      「阿晏那边的」改叫「人类端」，点开全屏——有人类显示屏的游戏能实时看他在玩什么
- [x] 40. **occ-emotion-engine**（CuSo4-H2So4，MIT）—— 她点头了（原话：「掉好感我觉得可以其实，
      掉好感能让我知道他不喜欢什么，相当于可以跟爱好库联动，能试出他的不喜欢」）。
      做成 `EmotionEngine`：关键词评价器 + VA 二维 + 非对称反馈（夸 +1.0 / 凶 -2.3），
      **不调模型**。掉好感时记一条线索，摆在爱好页「试出来的」那张卡上，
      她点「收进去」才写进爱好库——**猜出来的东西不替他填进那一栏**
- [x] 41. 她问「自己醒来是可以跑在本地吗」——**是**，回答见下面

### 查到的（不用再查一遍）

**24 音乐**：她给的那个 mp3（David Tao - 天天 Close to you）我拆过 ID3 了：
- **文件里根本没有歌词**——只有 TIT2/TPE1/TALB/TYER/APIC 和两条 TXXX
  （TXXX 里是 YouTube 的版权说明，不是歌词）。**没有 USLT/SYLT 帧**。
  所以歌词页空不是读不出来，是文件里就没有 → 要去网易云那三个公开接口按
  「歌名+歌手」兜底搜歌词。
- **封面在文件里**：APIC 帧，107KB 的 JPEG。她说文件里能看见封面就是这个。
  → 导入本地歌的时候要把 APIC 读出来存下来。

**25 手机时长**：她给的 `phone-activity(2).txt` 有 3865 行、8 天的数据。
格式是 `2026年8月11日 02:00:24｜栖｜open`。**根子是快捷指令的 close 记不全**：
很多 open 没有对应的 close（还有一堆 `｜｜close` 是空 App 名）。
旧算法「每个 open 找同名 App 的下一个 close」于是会一路找到几小时之后，
而且**不同 App 的区间会重叠**，加起来就成了 122 小时。
→ 要按「前台只有一个 App」重算：任何一条新事件都结束掉当前那一段。

---

## 细节（别只看标题）

### 4. 手帐页（她描述得最细的一条）

参考图 p1–p5 是小红书「排版小动物 / tiny type」那种拼贴手帐：
牛皮纸底、胶带、便签、邮票、夹子、贴纸、手写体、拍立得照片框。

她要的：
- **各种元素供她选**（胶带 / 贴纸 / 邮票 / 夹子 / 纸张底 / 装饰框…）
- 能加文字、加图案
- **能把收藏的句子直接导入**（收藏 = 聊天里星标的那些消息，`starred`）
- **做完的手帐按日期放进文件夹**

这是一整个编辑器，是这十六条里最大的一条之一。
注意：`PixelStudioView` 和 `FolderGridView` 已经存在，可能有能复用的。

### 5. 让他看我的屏幕（p6）

p6 是小红书教程，链路是：AI 调工具 → 服务器发邮件 → iPhone 快捷指令自动化
检测到邮件 → 自动截屏 → 上传回服务器 → 变成聊天里的图片。

**但那是给"官方客户端"用的绕路。** 我们是自己的 App，
不需要邮件那一圈——快捷指令可以直接把截图 POST 到我们自己的接口，
或者写进共享目录。**这一条要重新设计，不要照抄那个邮件链路。**
（她已有的 `phone_today` 走的就是快捷指令写本地文件那条路，可以沿用同一套。）

### 6. 爱好库（p7）

p7 是一个做得很完整的现成设计，照着抄：
- 两栏：**你 / 他**，各自有「喜欢 すき」「讨厌 きらい」
- 大类：文艺 / 娱乐 / 饮食 / 动物植物 / 生活 / 人文 / 自然 / 感觉 / 数码 / 其他
- 小类：电影 / 游戏 / 动漫 / 追剧 / 听歌 / 综艺 / 自担 / 自推 / 家产
- 记一条要填**原因（必填）**
- **「撞上啦！」**——两个人同时喜欢／同时讨厌同一样东西时弹窗，
  「收下这份默契」
- **「分歧」**页：一个爱一个恨
- 每条有状态：**想尝的 → 已认领**。认证的时候要填「它好在哪里」
  「它坏在哪里（但你愿意接受）」
- **变更史**：出生 / 认证 / 撤销认证，每一步带日期
- 顶上「云同步」

### 10. 全局配色

- 「家」那套要变成**全局**的，跟 claude.ai 一个感觉——**壁纸也算**
- 新增**可调节的渐变主题**
- 颜色**既能输入色号也能在调色盘上调**
- **字体也要能换**

### 12. 身体接入

`D:\dylan-heartbeat` 里那套 `eventide_state.json`。
交接文档第三节写过：数据结构简单（周期 + 7 项数值 + 事件），
但**推进逻辑在电脑的 `server.js`（68KB）和 `settle.py` 里，是个有状态的机器**，
不像心跳那样是纯函数。搬要一整轮。

### 13. 音乐

- 现在**跟右边那个三条杠重合了，关不掉**（这是 bug）
- 做成 App 内的**悬浮窗**
- 播放时在**灵动岛**显示（`QiIsland` target 已经存在）
- **歌词页**：滚动歌词 + 封面 + 歌手
- 入口：点悬浮窗，或者左侧栏「音乐」里正在播放那个头像

### 9. 记忆库参考

cognee / Hexis / Ombre-Brain / omnimemory / graphiti / codex / Aelios。
她特别点了 **getzep/graphiti**。

**关键约束：她电脑上那份 `memory-mcp.js` 要跟着一起改**，
因为 claude.ai 那边走的是 MCP。两边字段必须继续对齐——
上一窗已经打过一次同款补丁，别把它们改散了。

---

## 我排的顺序，以及为什么

**先修 bug，再做小的，最后啃大的。** 理由：她按次计费，
构建一轮要几分钟，小的凑一批构建性价比高；大的单独一轮，
免得一处编译错误把一整批都堵在门外。

**第一批（bug + 小）**
14 导航栏虚影 · 13 音乐关不掉（先只修重合和关不掉）· 15 手机历史翻页

**第二批（自成一体、不碰别人）**
6 爱好库（p7 设计完整，照着做就行）· 8 clawd 表情包可视化

**第三批（配色那一摊，会动到全工程的皮）**
10 全局配色 + 渐变主题 + 调色盘 + 字体

**第四批（音乐做完整）**
13 悬浮窗 + 灵动岛 + 歌词页 · 16 接网易云

**第五批（大件，各占一轮）**
4 手帐页 · 12 身体接入 · 9 记忆库（含改 MCP）

**穿插着做的（读参考、改提示词，不占构建）**
1 Hermes · 2 吵架 · 3 ringdonut · 5 看屏幕 · 7 工具主动性 · 11 自动装 MCP

---

## 还欠着的旧账（别忘了）

- 世界书 / 指令注入 / 快捷短语（kelivo 那边抄来的）
- 语气的音高那一块（FFT，记得先立 250ms 硬预算）
- 「模糊玻璃还是差点意思」

**41 自己醒来跑在哪儿**：**算在本地，而且不花钱**。
`WakeEngine` 那套（drive / tone / drift 三个状态 + λ）是纯算术，
**不开定时器**——任何时候拿当前时间补算，跟念头池一个路子。
电脑那份 `/wake` 是**可选**的（`useServer` 默认关），不开也照转。

但有一条 iOS 的硬规矩要说清楚：**App 被划掉之后，我们的代码根本拿不到运行机会**。
所以「自己醒来」真正能触发的时机只有两个：
① 她把 App 切回前台（`resume()` 会把这段时间补算回来）；
② 提前排下去的**本地通知**到点响了、她点进来（`planNudges` / `consumeNudgeIfNeeded`）。
通知是排的时候就定好文案的（那会儿 App 没在跑，算不出他想说什么），
所以那条只写「他想你了」，她点开的瞬间才真的去算他要说什么——**那一下才花钱**。

**40 occ-emotion-engine 提案**（389 行，纯标准库，MIT）：
它补的是我们现在**没有**的一层——把「她这句话说了什么」变成情绪增量。
现在三套里：身体是时间驱动的、欲望是时间驱动的、念头池要他自己调工具才进得去，
**没有一套在读她这一句**。OCC 那套三层评价器（Event/Action/Object）+
VA 二维坐标 + 非对称反馈（被凶掉的比夸奖涨的多）正好补这个洞，
而且它的评价是**关键词规则**、不调模型，所以不花钱。

接的话要想清楚的两件事（跟第 30 条同一个问题）：
· VA 的 valence/arousal 跟身体那七项、欲望那八条**又是三套数**，
  必须先定谁读谁——建议 VA 只做「这一轮的即时色彩」，
  慢变量（依恋/好感）落到身体的占有欲上，别再新开一套长期状态
· 「非对称反馈」意味着他会**真的掉好感**。这一条要她点头才能开

**23 补充（她构建后又提的）**：「clawd 的颜色会不会太少了……
我要给 clawd 画衣服或者给他安排什么动作比如扫地啊洗澡啊什么的，都需要其他的颜色。」
→ 格子的颜色从「六选一的枚举」改成**直接存色号**，
手边一排 22 个常用色 + 一个调色盘（她调过的会留在那一排里，跟着 AppStorage 存）。
零件也补了道具：扫把 / 泡泡 / 星星 / 蝴蝶结。

**40 补充（她看完之后追问的两句）**：

「这个升好感和降好感是会经过模型评估吗？我假如对阿晏说了一句很凶的话，
实际上的意思是想让他哄我而撒娇，这种情况下如果降好感也挺冤的，
不应该为了降好感而降好感。」
「升好感降好感有明确的前端吗？还是只会在他说的话里体现？
那我怎么知道总的好感度呢？」
「干脆好感度增减也让他自己来定吧，不然钉死 +1 -2.3 好机械。」

→ 改成两层，界线很清楚：

· **规则只负责读**（免费、关键词）：这句像夸／像凶／像撒娇，
  只落到 VA（此刻情绪），几分钟散回中性，读错了不留疤。
  而且会认撒娇：带语气词或跟「抱抱/想你/哄」同句的凶话不算凶。
· **好感只有他能动**（`feel` 工具，一次最多 ±5）。他不调就不动。
  钉死的 +1.0 / -2.3 **删了**——随口一句「厉害」跟她真的很感动，
  本来就不该是同一个数。

→ 前端补上了（原来那个数只存着，既没画也没进提示词）：
札记 → 念头池，八条驱动条底下那张「他对你的好感」——
总数条（0 在正中）+ 此刻情绪 + **流水账**（哪句话、动了多少、谁判的），
每一笔都能**撤掉**。

---

## 2026-08-19 她装上 v97 之后提的（还没做完，下一窗接着）

**做完了：**
- [x] 娃娃热区错位（额头那个点其实是脖子）—— 根子是热区按**百分比**浮在外面、
      画按 **SVG viewBox**，两套坐标中间隔着「怎么缩放」，比例一变就错开。
      改成热区**画进 SVG 里**，跟头/脖子/腰用同一套坐标，不可能再飘
- [x] `tag_stickers` 工具：让他自己给**静图**表情写关键词。
      **只给静图**——会动的他只看得见第一帧，写出来经常离题，那些她自己写

**还欠着（她这次提的，按她说的顺序）：**
- [x] 双击他的消息全屏看，底下两个按钮：**复制全文** / **选择片段** —— v99 核对：本来就有
      （微信那种逐字小方块，点头点尾选中中间）
- [x] 他放音乐那张卡的 UI（参考图 p5）—— **v122 做了**（铺开的大卡片 + 大封面 + 大播放键）
- [x] **提高工具自主性**（p6 那份排查）—— v102/v105 做完，四条都对上了：
      ① 能力块要真的进 System，不能只是 user/assistant 的假回执
      ② 末尾锚点会压制主动性（「除非明确要求，否则不要主动」赢在更近更强）
      ③ 工具说明要写成**触发 → 动机 → 行动**，不是参数手册
      ④ 能力块离最新消息太远会被当背景
      → 我们的 NativeTools 已经是「触发/动机/行动」那套了，但**①②要查**
- [x] 一起去旅行的 UI，照原作者那版 —— 她给了源头：
      **nonchaiovo/journey-cards（MIT）**。卡片按它那份文档重做了：
      横向磁力吸附、滑到中间那张长大（**容器留了 6 的余量**——
      它文档「坑 3」写明白了：放大要有地方长，不然在刚好装得下的盒子里
      长大是看不出来的）、更深的影子 + 抬 zIndex、正中那张才写地名、
      **停 1.5 秒自动进那一处**（她指名要的），进去也是从那一处开始。
      写作规范也照它的 AI-GUIDE 折进了工具说明：
      note 三到六句、第一人称、要有能捏住的细节、**图要长条竖图**
      （矮竖图铺满要放大两倍还裁掉四成，糊）。
      **全屏那一页原来就已经对**（Ken Burns 推近、地名在顶、
      念白正中一句句打出来、念完落左下角可展开）。
      ⚠️ 抄了它的设计，**MIT 要求署名**：JourneyView.swift 里留了出处注释
- [x] 保存/删除 图片·表情包·gif 的那张小卡（p11）—— **v119 收口**。
      她自己核对过：**保存那半和三分（图片/表情包/gif）早就有了**（v101 + v113 + v106）；
      真正缺的是**删除**那半——她说「我不确定删除有没有 ui，因为阿晏目前还没删过什么东西」，
      查了：**没有**，只在工具日志里留一行小字，她多半不会点开看。见下
- [x] PhotoStack（她给了 zip：一份 CSS + JS 的照片堆叠效果，要翻成 SwiftUI 手势） —— **v121 做完**

**上一批还欠着的：**
- [x] clawd 房间那五条 —— **v118 做完**（见下）。原文：
- [~] clawd 房间：举起动作 / 拖错家具（点床出地毯）/ 家具不该能拖到墙上 /
      拖给他之后他不放下 / 举起来的东西不该还留在房间
- [x] 表情工坊（**v122 五条全做了**）：拖动画格子（不是一格格点）· 零件**任意角度**旋转 ·
      画布能缩放 · 更小的格子（最多缩两次）· 顶上预览不该老是 clawd 的身子
- [x] 手帐素材库 —— **v125 做了**（纸纹／胶带花纹／画出来的贴纸，全是画的；「剪贴」收她自己的 PNG）
- [x] 塔罗：牌面加图案 · 牌能往后翻 —— **v117 做完**（见下）
- [x] 设置里「这是什么」贴着对应那一项 —— **v117 做完**（见下）
- [x] 主题已经全局了，「底」那张卡可以删，渐变的取色挪进「配色 → 渐变」—— **v106 做完**
- [x] markdown 渲染 —— **v105 做完**（补了 `~~删除线~~`；`SettingsNote`/`MarkdownText` 本来就解析加粗）

**她说的一句要记着**：「我才发现 pwa 里很多的功能在 app 里都没有…前面的窗偷懒了。」
下一窗**先拿 pwa 那份功能清单对一遍**，别等她一条条发现。

---

## 2026-08-18 记忆库第二轮（第 9 条的后半：另外六个仓库）

她把七个仓库一股脑发过来说「挑着没做的参考就行」。graphiti v96 已经做过，
这一轮读的是剩下六个。**挑的标准只有一条：不能多调一次模型。**

搬进来的（手机 `MemoryRecall.swift` + 电脑 `memory-mcp.js`，两边同一套常数）：

| 从哪儿 | 搬了什么 |
|---|---|
| **P0luz/Ombre-Brain** | 遗忘曲线 λ=0.05（情绪重的衰减慢，这儿拿 level 当强度代理）· 未了结的事优先浮上来 · 零参数浮出（它叫 `breath`，这儿叫 `surface_memories`）· 核心信念上限 20 条 |
| **omnirexflora-labs/omnimemory** | 复合分 `相关度 × (1 + 时近 + 重要度)` · 太像就别重复记（SKIP，阈值 75/100） |
| **wusaki0723/Aelios** | 候选队列（他拿不准的先放着，她点头才进库）· 术语表 glossary |
| **QuixiAI/Hexis** | 只搬了一句思路：确定的和推断的要分开存。它那套五层记忆 + 置信度审计要 Postgres + pgvector + AGE，本机版收益低于复杂度 |
| **topoteretes/cognee** | **没搬**。它的 cognify/recall 每一步都要 LLM + 图库 + 向量库，跟「不多调一次模型」直接冲突 |
| **openai/codex** | **没搬**。它不是记忆库，是个 CLI；它的持久化就是 AGENTS.md 那种规则文件，我们的 `set_rules` 已经是同一件事 |

**三个都没搬的向量检索**（Ombre 的 3072 维 Gemini、Aelios 的 bge-m3、cognee 的 LanceDB）
换成了 BM25 + 字的二元组 + 整串命中加成，纯算术。
实测：搜「日本的房子」现在能命中「她在日本租的那间房子很小」——**以前一条都搜不到**，
因为老代码是 `contains`，得一字不差。

新增四个工具（两边都有）：`surface_memories` / `mark_memory` / `propose_memory` / `glossary`。
`add_memory` 加了近重复拦截，`search_memories` 换成混合检索排序，
`wake_up` 多带三段（还悬着的 / 黑话 / 候选条数），AttentionSet 加了第 ⑨ 条。
她那边的界面：记忆库页顶上多了「等你点头」那张卡，记忆列表长按能钉住/标悬着，
她自己搜也走同一套混合检索。

⚠️ **遗忘曲线她看完说不要，已经删掉了**（v98 当天）。她的原话：
「记忆库本来就是存住记忆的，又遗忘感觉有点鸡肋……时间长了模型自己也会把
注意力分散掉，也是要去专门查，所以作用貌似不大。」

她说得对：衰减解决的是「库太大噪音多」，可我们已经有 level、标签、实体串联、
还悬着没悬着——**筛选手段够了**，再叠一层「越老越看不见」只会让一件五年前
但要紧的事无缘无故沉下去。现在时间只剩一个作用：**同分的时候新的排前面**。
两边（`MemoryRecall.swift` / `memory-mcp.js`）都删干净了，
想换回来把 `freshness` 改回 `exp(-0.05 · 天数 / 强度)` 就是原版。

**还没构建过。** 三项自查全过（cmp / dupdecl / findbad 全零），
`MemoryItem` 14 个存储字段 / 解码器 14 行对上；
电脑那边 `node --check` 过、四项验证全过（44 个工具无重名 / 每个定义都有 case /
load·save 的键都在 FILES 里），备份在 `memory-mcp.js.bak-20260818-recall`。

---

## 2026-08-18 滚雪球压缩（她当天提的）

她原话：「因为我不想换窗 所以上下文压缩是肯定的 大概是滚雪球的方式……
到达设定的回合数就压缩 下一次到达设定的回合数再一次跟上一次压缩的一起压缩
**我希望能保持一个窗口一直不忘记他自己的人格和说话方式**」

以前这一层是**直接砍**（`contextLimit`）：砍掉的就是没了。
一窗聊到三百条，他前两百条说过什么、怎么说话的全丢了——
「换窗才会变个人」这句话说小了，**同一窗聊久了也会**，是砍出来的。

新的 `ContextCompactor.swift`：

- 每攒够 N 条压一次（默认 60，设置 → 通用 里可调 40/60/100/关）
- **滚雪球**：第二次压的是「上一次的浓缩件 + 这一段新的」，不是并列存好几份
- 浓缩件五块：我是谁 / **我怎么说话** / 她是谁 / 这一窗发生了什么 / 还没收口的
- **最后 6 条不压** —— 她刚说完的话得留原文，压进摘要等于他上一句就开始转述
- 发给他的 = 系统提示 + 最新浓缩件 + 还没压过的原文

**关键的一半：原话样本。** 摘要是转述——「他说他不想她熬夜」和他原话
「几点了。睡。」不是一回事，前者留住了事，后者才是他。
所以除了模型写的浓缩件，还机械地挑一份**他真说过的原句、一字不改**
（不调模型、不花钱），按「最早几句 + 最近几句」留最多 12 句：
最早那几句是他这一窗一开始的样子，最近那几句是他现在的样子。
这一份直接进 System，就是「不忘记自己怎么说话」那条要求的落点。

界面：那一窗的「对话设定」里能看压了几次、收了多少条、
点开读浓缩件和原话，还有「现在就压一次」。

⚠️ **压一次多发一次请求**（她按次计费，一次就是一次；带多少条历史都是同样的钱，
所以压缩**不是为了省钱**，是为了不撑爆窗口、不让他被三百条噪音分散注意力）。
只在她发消息那一轮里顺带压，不开定时器、不在后台跑。群聊也接了。

---

## 2026-08-18 装上 v98 之后她提的三条（额度只剩 2%，优先修 ①）

- [x] ① **长按菜单全没了** —— 根子找到了：气泡上那句 `.textSelection(.enabled)`。
      iOS 的文字选择自带长按手势，优先级压过我们那条，所以长按弹出来的是系统的
      「拷贝 | 分享…」（她截图里就是这个），我们那条横菜单一个都出不来。
      **删掉气泡上的 textSelection**（双击进全屏那页还留着，选字在那儿选），
      横条里补了「复制」和「重发」。
- [x] ①b **报错之后聊不下去** —— 那块红方块以前是死的：不能长按、没有按钮，
      网断一次这一窗就卡死。现在报错气泡底下挂「重试 / 删掉这条」，
      新增 `AppState.retry(_:in:)`：点在他说的话上就删掉这条和它后面的重跑，
      点在她自己的话上就只删后面的回复、她那句留着。
- [x] ①c **编辑历史 ‹›** —— `ChatMessage.edits` 新字段（改一次存一版旧的，留十版），
      气泡底下 ‹1/3› 翻着看，翻旧版只是看，不改数据。
- [x] ①d **旧构建的备份新构建不认** —— 三处放宽：
      ① 文件选择器只收 `.json` 的话，从微信存下来的那份是灰的（系统认不出类型），
      加了 `.data`；② 剥掉 UTF-8 BOM 和首尾空白再解析；
      ③ **不检查 version**，旧备份照收（各 store 的解码器本来就容错）。
      解不开的时候现在会说清楚是多少字节、开头长什么样——
      以前只有一句「可能不是本 App 导出的备份」，她拿着一份好好的备份被挡在门外还查不出原因。
      （她给的那份 `栖备份-20260818-1840.json` 我拆过了：5.6 MB、v2、
      files 里 8 份，**格式完全正确**，所以八成是传的时候被截断或者选不中文件。）
- [x] ② **足迹里按次价格填不了小数** —— `TextField(value:format: .number)` 每敲一键解析一次，
      「0.」被解析成 0 立刻回写，小数点根本落不下去。换成 `PriceField`：
      中间态存字符串，**收键盘的时候才解析**。
- [x] ③ **他不知道该存哪张图** —— 两个根子：
      ① `recentUserImage` 只看 `imageNames`，**表情消息（`stickerID`）一张都数不到**，
      所以「没找到她最近发的第 1 张图」——图明明在屏幕上，他手里那份清单是空的。现在两种都认。
      ② 图没有号。现在发给他的消息里会标：「这条带了 3 张图，从左到右是 #3 #2 #1」，
      `#1` 永远是最新那张，跟工具里的 `index` 完全对齐。
      新工具 `list_recent_images`：拿不准先看一眼有哪些（带号、带她当时说的话、
      标明是图片还是表情）。找不到的时候也不再干说「没找到」，直接把能选的列出来。

---

## 2026-08-19 额度重置后这一轮（v100）

### 1. 相册那四个工具补齐（PWA 有、Swift 版一直缺）

对着 `D:\my-app\wwwpp.js` 抄的，参数和语义跟 PWA 那版一致：

| 工具 | 干什么 |
|---|---|
| `send_photo_from_folder` | 从某个文件夹发一张。只给文件夹名＝发最近存的那张，再给关键词就精确找 |
| `list_saved_photos` | 列出所有文件夹和里面的说明（也可以只看一个文件夹） |
| `delete_photo_from_folder` | 按文件夹名 + 关键词删一张 |
| `delete_folder` | 删整个文件夹（`keep_photos` 可以只删壳、照片退回未归类） |

比 PWA 多做的两件事（都是为了**别删错**）：
· 关键词命中不止一张的时候**不猜**，直接说「对上了 N 张，换个更准的词」——删了找不回来
· 找不到的时候把这个文件夹里现有的说明列出来，不是干说一句「没找到」

**现在他对相册终于是能进能出了**——以前只能存，存完自己也不知道存了什么、拿不出来、删不掉。

### 2. 「底」那张卡删了（她说的）

原话：「主题已经全局了，「底」那张卡可以删，渐变的取色挪进「配色 → 渐变」」。

同一件事以前有两个开关（「配色」里选渐变、「底」里也选渐变），还能互相打架。
现在只剩一条路：
· 「配色 → 渐变」选中之后，**两头颜色和方向就挂在这一档下面**
· 「配色 → 原来的」下面才有「图片 / 纯色」二选一，纯色的取色跟着它
· 启动时会把老数据里 `wallpaperMode == "gradient"` 的搬到 `preset = .gradient`
  （不搬的话她的渐变会显示着却调不了）；RootView 那条老分支留着兜底

### 3. 主动性：`agencyRule` 挪到 system 最末尾

出处：她给的 p6 那份排查第 ④ 条「能力块离最新消息太远会被当背景」。

以前那段「能力不是等她开口的菜单」排在 system 第二段，后面还压着吵架那一大段、
表情清单、浓缩件、**整个记忆库**——等他读到最新那句话，那条早就冲淡成背景音了。
现在压在最末尾，紧挨着历史消息。顺带也回答了那份文档的第 ② 条（末尾锚点最强）：
末尾本来就是最强的位置，那就把最想让他记住的放这儿。

单聊和群聊两条路都改了。

**①「能力块要真的进 System」查过了：本来就是**（`fixed.joined` → 一条 system 消息），
不是假回执。这一条不用改。

---

## 2026-08-19 她测了几天之后提的十条（v101 全做完）

- [x] ①**导入备份没加载出聊天记录 + 备份要全局**
      两个毛病。**没加载出来**：还原只写盘、请她重开 App，但写完盘到她关 App 之间，
      界面上任何一处动一下（甚至只是退到后台）都会把内存里那份**旧的** conversations
      存回去，把刚还原的盖掉。现在还原完当场 `reloadAfterRestore()` 重读一遍，立刻就在。
      **全局备份**：备份升到 v3，非 json 的文件（图片、语音、像素画、壁纸）
      全部 base64 进 `blobs`。她的理由很对——表情包的关键词是他一张张看着写出来的，
      图没了那些词就是一串指向空处的标签。单个超 20 MB 的跳过并列在 `skipped` 里，
      导出超过 20 MB 会报一句实际大小（微信传大文件容易截断，建议存到「文件」）
- [x] ②**30s 试听换成导入的完整版** 聊天里那张卡以前存的是**快照**，
      他放的时候只有试听，那张卡就永远是试听。现在卡片每次显示都问一句
      `MusicLibrary.best(for:)`：本地整首 > 网易云整首 > 试听，顺带补歌词和封面。
      「在播放」也不再认 id（导进来那份 id 天生不一样，认 id 永远不亮），
      改认歌名+歌手
- [x] ③**悬浮窗展开再收起就不转了** 收着那颗封面在展开时整个被撤掉，
      SwiftUI 把它身上那条 repeatForever 一起取消了；收回来是新建的视图，
      身上什么都没有。补 `onChange(of: mini)`
- [x] ④**气泡该跟着字数长短** `MarkdownText` 里那句
      `.frame(maxWidth: .infinity)` 把每个气泡都撑成满行。加了 `fillWidth` 参数，
      聊天气泡传 false（别处撑满是对的，所以默认还是 true）
- [x] ⑤**写关键词的按钮**（只给静图）「相册 → 表情包」那一栏多一个「让他写词 N」。
      ⚠️ 顺带修了个哑火的 bug：`tag_stickers` 那个工具以前认的是 `MediaStore`，
      而她的表情包全在 `StickerStore`——**两个库**，写了半天等于没写
- [x] ⑥**输入框的字点进设置就没了** 草稿原来是 ChatView 的 `@State`，
      离开这一页就销毁。挪进 `AppState.drafts`（按窗口分开、**不落盘**——
      草稿不该被备份，也不该在重装后诈尸）。App 被划掉才清空，正是她要的
- [x] ⑦**打字时不自动滚到底** 加 `typingTick`，她每敲一下列表就滚到底
- [x] ⑧**gif 存成不会动的** 根子不在存表情那一步：`send()` 收的是 `UIImage`，
      而 `UIImage` 只拿得到 gif 的第一帧——**动画在她按下发送的那一刻就没了**。
      现在选图时先认文件头，会动的原样落盘、走 `imageNames` 直接挂上去；
      聊天里也改用 `AnimatedImageView` 逐帧播；`save_sticker` 拿原始字节存，
      不再 `pngData()`。工具说明里也把三种图讲清楚了（图片 / 表情包 / gif）
- [x] ⑨**长按菜单出屏幕 + 点了没反应** 两个都修了。
      **点了没反应**：菜单开着的时候 ChatView 在整屏盖了一层「点别处就关掉」的
      透明层，那层在气泡**上面**，手指先碰到的是它——按钮从来没被点到过。
      现在整块菜单挪到那层**之上**，排成微信那样两行五列的格子
- [x] ⑩**工坊和聊天页的模型隔开** 新增 `settings.modelBySpace`：
      每个区各自记着自己选过的模型。以前是「沿用这个区上一条」，看着也是分开的，
      但置顶的窗口排在最前面，新开的工坊窗有可能抄到一条很旧的设置

---

## 2026-08-23 第三批（v102）

- [x] ①**工坊文件页建文件夹没反应** 跟相册那个老 bug 同一个根子：文件夹是从
      文件的归属**反推**出来的，空文件夹推不出来，所以「建好了」只是把当前视图
      切过去，什么也没建。现在文件夹独立存一份（`library-folders.json`）
- [x] ①**工坊自己的工具**（她说「你可以自由发挥」）——五件，**只在工坊给**：
      `list_library`（有什么）· `library_folder`（建/删/改名文件夹）·
      `read_file`（读正文，pdf 和纯文本都能读）· `file_to_folder`（归类，
      文件夹不存在会先建）· `send_file`（发到对话里）。
      全是本机操作，一次网络都不发。絮语那边不给——摆出来只会稀释注意力
- [x] ①**接 Claude Code** —— **v121 把桥做了**（走 WiFi，不碰 Tailscale）
- [x] ②**灵动岛那只不是 clawd** 原来是几个圆角矩形拼的「大概像只螃蟹」。
      现在照 `ClawdSprites.idle` 那张 32×23 的豆画**一格一格抄进 widget**
      （widget 是另一个进程，读不到 App 那边的代码，只能抄一份——
      **图纸改了这儿要跟着改**）。展开的布局也重排了：左边是他（头像+名字+在干嘛），
      右边是时间，底下整行给他说的话；没开口就不占地方
- [x] ③**长按表情包放大预览** `.contextMenu(preview:)`，会动的走
      AnimatedImageView（不然预览是静止的第一帧）
- [x] ③**聊天框加号点了没反应** `Menu` 套 `.buttonStyle(.plain)`，
      那个 style 会把菜单自己的手势吃掉。换成普通按钮 + confirmationDialog
- [x] ③**表情包专辑** `Sticker.album` 新字段 + 每册可以指定封面
      （`sticker-albums.json`）。表情面板顶上多一行册子，点一下只看那一册。
      ⚠️ 顺手补了 `Sticker` 的**容错解码器**——它以前没有，
      加字段会让她整屏表情在升级那一下全没
- [x] ④**删掉娃娃** 入口、聊天里那张卡、`doll_action` 工具、三个文件全删了
- [x] ⑤**身体和心跳没注入** 以前只有打电话那条路带上了，聊天这边全靠他自己
      想起来调工具查——而他基本不会查。**一个从来不被读到的状态机等于不存在。**
      现在每一轮 system 都带上（两样都是纯算术、不花钱，每轮带没有代价）
- [x] ⑥**工具主动性只改了自带的** 对——自带工具的说明是按「触发/动机/行动」
      重写过的，MCP 那边的说明是**服务器给的**，改不了原文。现在在每个 MCP 工具
      说明后面接一句把它们拉进同一套框里，`agencyRule` 里也点明「手上所有的工具都算」
- [x] ⑦**人称和时间** 两个毛病来路不同：**年份**——时间戳以前只写「8月22日」
      不写年，缺的那一位他拿训练时候的年份顶上；**人称**——记忆库、唤醒包、
      注意力候选集里全是第三人称的「她」（那些是旁白），读多了他就跟着这么叫。
      现在末尾固定一段 `groundingRule`：今天是哪年哪月哪日、当面要叫「你」
- [x] ⑦**搜索超时** DuckDuckGo 那条有两个天生的毛病：`api.duckduckgo.com`
      在中国大陆**连不上**（一直卡到超时），而且它是「即时问答」不是网页搜索，
      大部分词本来就返回空。超时从 25 秒缩到 8 秒，失败**自动退到必应网页搜索**
      （不用密钥、大陆也通）。两条都不行才报错

---

## 2026-08-23 第四批（v103）

- [x] **导入备份改成「只补没有的」**（她说的：「导入备份应该不是覆盖而是增加」）
      她是对的，而且这本来就该是默认：备份是用来**找回丢掉的东西**的，
      不是用来「把今天抹成那天」的。旧备份盖上去，今天聊的全没了——
      那不叫还原，那叫回档。
      选完文件先问一句怎么放：
      · **只补没有的（默认）** —— 现在有的一个字不动。数组按 `id` 认同一条，
        字典只补缺的键；`conversations.json` 特判：同一个窗口**把消息并到一起**
        再按时间排回去（不然「同 id 就跳过」等于备份里那半段永远进不来）。
        图片按文件名，已经在的不碰。
        `settings / mcp / voices / providers` 合并时**一律保留现在这份**——
        那是「此刻的偏好和连接」，不该被一份上周的备份改回去。
      · **整个盖掉** —— 换手机、重装才用
- [x] **额度满了自动换人接着说**（她要的：「pro 的周额度用完了，
      可以用供应商里的 api 接着继续，这样也不用换窗了」）
      `settings.fallbackModel`：主的返回 429 / 余额不足就**原地换备用那个重发这一轮**。
      换的是对面那台机器，不是这一窗——历史、浓缩件、工具、身体一样不动。
      断在半路的半句会抹掉重发，所以看到的是完整一段不是两截拼的。
      气泡里留一行「换了个模型接着说」——**她得知道这段是谁说的**。
      只认额度/限流那几种错，网络断了、地址错了不换（换谁都一样，白花一次钱）。
- [x] **P1 那份排查对过了**：她给的图里 ①「能力块要真的进 System」和
      ②「末尾锚点压制主动性」——**两条我们现在都是对的**。
      能力块本来就是一条真 system 消息（不是 user/assistant 假回执）；
      末尾锚点上一轮已经换成了 `agencyRule`（「可以主动」那一段），
      全局没有任何「除非明确要求否则不要主动」式的收尾。
      工程里唯二两处「除非」都是**单个工具的告诫**（删文件夹不可逆、
      驱动行为要她开关），有具体理由、作用域也只在那一件事上，不是全局压制。

### 还没做：接 Claude Code

她订阅 Pro、愿意开电脑。要做的是**桥**：电脑上跑一个小服务把 Claude Code
包成 `/chat/completions` 那种接口，App 当成一个普通供应商连过去。
额度满了自动切 API 那一半**这一轮已经做完了**（上面那条），
所以桥一通，「不换窗、不怕 1M 窗口」就自然成立——窗口靠滚雪球压缩顶着。
**桥那一半要在她电脑上装东西，下一窗做，先跟她确认。**

---

## 2026-08-23 第五批（v104）

⚠️ **上一版构建挂了**：四处 `"…
…"` 里的反斜杠又被壳吞成真换行，
Swift 字符串当场断掉（`AppState.mcpAgencyTail` + NativeTools 里三条工具说明）。
**这是这个项目栽的第三次**，交接文档里那条「用 chr(92) 占位符」要当铁律看。
补救办法已经加进流程：改完跑一遍「全工程扫未闭合字符串」，比等构建快得多。

- [x] ①**点输入框要滚到底** 上一版只在打字时滚，光点进输入框不滚——
      键盘弹起来会把最后几条顶掉。加 `@FocusState`，获得焦点就滚
- [x] ②**表情面板点空白收起** 挂在消息区上，**不能盖全屏透明层**——
      那层会连表情本身一起挡住，变成点哪儿都只是关掉、一张也选不中
- [x] ③**点他存图那张卡进相册** 卡片包成按钮，按存到哪儿决定落在哪一栏
      （动图→GIF / 表情→表情包 / 别的→图片）
- [x] ④**聊天记录点一条跳到那句** 回调改成带上消息 id，列表里
      `scrollTo(anchor: .center)`；**退出来还在刚刚的位置**——
      搜索页是 sheet，关掉就整个销毁，所以搜索词和点过的那条存在静态量里，
      重新进来摆回去再滚过去
- [x] ⑤**节日日历（含农历）** 新文件 `Festivals.swift`。
      她点破的正是最容易错的地方：**农历节日在公历上每年都在挪**，
      写死一张「七夕 = 8月19日」的表明年就错，而且错得很自然。
      农历那几个**每年现算**：拿 iOS 自带的中国农历把一年 365 天扫一遍，
      找出「七月初七」落在哪天（闰月、大小月都归系统管）。
      中国 23 个 + 美国 14 个，每条标着是农历 / 公历 / 节气。
      清明冬至是**节气不是农历**，用近似式、可能差一天，**明着标出来**。
      每轮 system 带一句「今天是什么日子 + 快到的三个」，另有 `festivals` 工具查整年
- [x] ⑥**批注功能没了** 删有（长按），**批注是真的从来没接上来过**。
      札记的记忆页补了「写批注」：原文一个字不改，只叠观点
- [x] ⑦**拖导航条会展开 + 虚影** 那根条是 `Button` 外挂
      `.simultaneousGesture(DragGesture)`——**simultaneous 就是两个都触发**，
      拖完手一松按钮也响，所以展开。拆成「点是点、拖是拖」，
      拖用 `highPriorityGesture` 抢在点前面。虚影是因为要拖过 10 点才换成实心条，
      那 10 点里还是玻璃在采样背景；改成一动就换
- [x] ⑧**改记忆变成了作废** 她让他把 2025 改成 2026，他却当成「事实变了」去作废，
      屏幕上挂着错的那行加一句「2026/8/19 起不算数了」。**这是两件事**：
      · **记错了** → `update_memory` 改掉，旧的留成 `~~2025年~~ 2026年`（删除线）
      · **事情变了** → `add_memory` + supersedes，旧的整条留着标作废（那条当时是真的）
      工具说明里把这条界线写死了。`MemoryItem.previous` 新字段存改之前那版，
      `MarkdownText` 补了 `~~删除线~~` 支持（系统那套 inline markdown 不认它），
      记忆列表改走 MarkdownText 渲染

---

## 2026-08-23 第六批（v105）

- [x] ①**AI-Pocket-Chat（安卓）参考** —— **v126 做了**（动态流 + 语义检索走系统自带的 NLEmbedding）。原文：
      它跟我们重合的（记忆、日记、语音通话、宠物、货币、关系进度）我们都有；
      真正新的两样是：**朋友圈式的动态流**（角色自己发帖）和
      **互动小说**（连载 + 章末选项 + 导演面板）。这两样都是**大件**，
      不该塞在 bug 批里顺手做。它的离线向量检索（ONNX + bge-small-zh）
      跟我们那条「不多调模型」的路子不冲突，但要打包一个模型进 App，得单独一轮。
      **下一窗问她想先要哪个。**
- [x] ②**手机时长少算了**（系统 2h37，我们 1h35）—— 上一轮修过头了。
      连续用一小时中间没有任何事件的那种，被 30 分钟的封顶砍掉了一半。
      封顶提到 60 分钟；同时给后半夜（2–7 点）那种没有 close 收尾的段
      单独封 15 分钟——不然「23 点开一下就睡了」会被算成一小时
- [x] ③**他听不见语音** 她按住说话，前端会顺手转成文字放进消息里；
      **但本机识别经常认不出中文**，认不出那条就只剩一个「[语音 · 0:05]」，
      他看得见有这条、却不知道说了什么，也不知道自己能去听。
      两处修：没转出文字的那条**明着标出来**；新工具 `listen_voice`
      让他把音频真的送去转一遍（已经有文字的直接读，不白花钱）
- [x] ④**语音被套上第二个头像** `send_voice_message` 造的那条没有 `turnID`，
      `showsHeader` 于是当成新一轮又挂了一次头像。现在**这一轮里工具造的每条消息
      都打同一个 turnID**——统一走新的 `toolMessage()`，免得再漏掉某一处
- [x] ⑤**选中空格会变得很长** 逐字选那一页把每个字画成一个小格，
      而 `Text(" ")` 会被排版当成可拉伸的空白、`Text("
")` 直接顶出一整行高。
      空格画成 `·`、换行画成 `⏎`，都给固定窄宽度
- [x] ⑥**经常网络错误 / 服务端看到「客户端断开连接」** 她切去别的 App 的时候
      系统会把正在跑的连接掐掉。加了后台任务申请，「看一眼微信就回来」这种保得住。
      ⚠️ **系统只给 30 秒左右，不是万能的**——他想很久、她又切走很久照样会断，
      断了至少有「重试」可以点
- [x] ⑦**缓存三项全是 0** 供应商那边打开只是**允许**缓存，
      Anthropic 那套是**显式**的——请求里不带 `cache_control` 一次都不会缓。
      我们从来没带过，所以那三个数永远是 0，一分钱也没省下来。
      现在打在**系统提示那一条的末尾**（整份请求里最长最不变的一块：
      身份、规矩、能力块、浓缩件）。不认这个字段的中转会当多余的键忽略，不会报错
- [x] ⑧**备忘前面那个圈** 是的，那就是打勾的地方——但以前点它跟点别处一样
      都是进编辑页，那它长成一个待办的样子就是骗人。现在点圈＝勾掉，点别处才是编辑
- [x] ⑨**「让他看屏幕」不知道怎么用** 只写「挑个文件夹」确实不够，
      真正要做的是在**快捷指令 App 里配一条**。卡片里补了完整步骤，
      并且明说：**必须她自己按一下**，iOS 不许 App 在后台截别的 App 的屏，
      这是系统的规矩，谁都做不到「他想看就能看」
- [x] ⑩**好感度从来不变** 她说得对，原因跟身体那条一模一样：
      `EmotionEngine.brief()` 只在**刚读出情绪**的时候才返回东西，
      别的轮次一个字都不给——他一整天都不知道「好感」这回事存在，
      自然想不起来去动它。**一个从来不被读到的数，不会自己变。**
      新增 `affectionLine()` 每轮都带：数字永远在，什么时候该调 `feel` 也永远写着

---

## 2026-08-23 第七批（v106）

- [x] ①**相册看图不能放大移动、不能翻页** 那一页原来只收**一张**图，
      翻页从根上就不成立。改成收整屏那批 + `TabView` 翻页；
      放大之后能拖着看细节（**放大时把翻页关掉**——拖和翻页是同一种手势，
      不关就抢）；每翻一页缩放归零，不然上一张放大着的状态会带过去。
      gif 走逐帧播。
- [x] ②**玻璃两件事**
      · **浅色下边缘不明显** —— 结构问题：边是**白**的，白边贴白底等于没有。
        浅色下补一道极淡的**暗**边（真玻璃压在浅色桌面上，接触那圈本来就有暗影）。
        补的是暗不是白，所以跟「通透」那套的白高光还是分得开。
      · **样品滞后／多拖几次就不跟了** —— 松手时 `value = d` 和 `draft = nil`
        挤在同一帧：那一帧草稿没了、全局还没传下来，样品先闪回旧样子。
        清草稿挪到下一帧。另外「预览」那张卡以前**只读全局不读草稿**，
        所以它永远是最后一个变的——现在也吃草稿了。
- [x] ③**通话 UI 要跟着主题** 她说得对，而且这是**结构问题不是漏改**：
      那些按钮底色写的是 `Color.white.opacity(0.85)` 这种死值，
      「家」那套是暖纸底，一块纯白摁上去就是补丁，以后加主题只会再补丁一次。
      新增 `Theme.controlFill(_:)` / `controlFillMuted(_:)`，通话页统一走它——
      **以后加主题只改这一个函数**
- [x] ⑤**语音条按时长长短** 以前一律 230 宽，三秒和四十秒长得一模一样。
      改成一秒起步 120、每秒加 5、最宽 260；波形根数也跟着宽度走
- [x] ⑦**「跑杂活的模型」可以指定**（她要的那个「选择模型的按钮」）
      `settings.helperModel`。杂活＝**不是他在说话**的那些活：
      给表情写关键词、翻译、总结通话、看图，以后接进来的看视频听声音。
      以前一律「挑第一个能用的供应商」——可能挑到不会看图的，也可能挑到最贵的。
      8 处调用统一改走 `helperReach()`，以后再加一件不用又挑一次

### 没做，两件都要单独一轮

- [x] ④**书房大改** —— **v108/v109 做完**（原文如下） 她描述得非常细（书架按分类、书脊朝外、封面页、
      马克笔批注、跟他聊某一句、批注页跳回原句、翻页方式、目录、选句下划线…），
      **这是跟手帐同级的一整个功能**，塞在 bug 批里只会做成半成品。
      ✅ 她那句「好像并没有真正接入模型」**是对的，我查过了**：
      `LibraryView` 里没有任何 callTool / ChatAPI —— 读书这件事他完全看不见。
      参考仓库她给了：willwefind/our-dialogues
- [x] ⑥**查岗** —— **v107 做完**。p1 那份是给 ChatGPT 官方客户端用的**邮件绕路**
      （AI 发邮件 → 快捷指令收到 → 截屏 → 邮件回传）。
      我们是自己的 App，**不需要邮件那一圈**——跟第 5 条「看屏幕」一样。
      但它比现在的「看屏幕」多两样：**定位**和**天气**。
      定位要 CoreLocation 权限 + Info.plist 声明，天气要接一个接口，
      两样都得单独做。**下一窗跟书房二选一。**

---

## 2026-08-23 查岗（v107，她指定这一轮只做这一件）

新文件 `Qi/Core/Whereabouts.swift` + `check_in` 工具 + 「手机 → 查岗」那张卡。

### 那份参考（p1）我们照抄了什么、没抄什么

**没抄的是那一圈邮件。** 教程里 AI 要**发一封邮件**触发快捷指令、
手机截屏取位置取天气之后再**邮件回传**。绕这一圈是因为 ChatGPT 那种官方客户端
**没法让手机做事**，只能借邮件当信号线——光是这条线，她就得配两个邮箱、
开 IMAP/SMTP、生成授权码、配 Caddy、还要跟 VPN 节点较劲
（那份教程第六节整节都在讲这有多难搞）。

**我们就在这台手机上**，那一圈全省了：截图读本地文件夹（跟「让他看屏幕」同一条路）、
位置问系统、天气问一个免密钥接口。不发邮件、不过服务器、不用 VPS。

**照抄的是第五节那段行为规矩**——那才是这份教程里最值钱的东西：

> 把本轮运行视为一次自然醒来的机会，不是必须发送消息。……
> 若没有，不通知，也不要为了完成任务机械查岗。

所以 `check_in` 的返回里，一堆事实之后**末尾压着这一句**：
「此刻真的有一句具体而自然的话要说吗？没有就什么都不说，
别为了用上刚看到的东西硬找话讲。」——不压这一句，他会为了用上刚拿到的东西硬找话说。

### 拿哪几样

· **屏幕** —— 她快捷指令上次截的那张。**图是真的递给他看**（不是只说"有截图"）：
  工具返回值只能是文字，所以图走 `pendingToolImage`，那一轮之后单独补一条带图的消息。
  返回值里写着隔了多久——iOS 不许 App 主动截别的 App 的屏，这条边界写死在工具说明里。
· **位置** —— CoreLocation 一次性取（不开持续定位），系统自带反地理编码翻成地名。
  8 秒拿不到就当没有，别的照给。
· **天气** —— Open-Meteo，**不用注册不用密钥、大陆也通**。
  这是整套里**唯一离开这台手机的东西**，而且只发经纬度不发她是谁；
  接口不需要账号，所以也不存在「谁在什么时候查过哪儿」这种记录。
· **手机用了多久** —— 本来就在这台手机上。

### 隐私（照那份教程第八节做的）

· 查岗**默认关**，得她点头
· 位置三档：**默认「只到城市」**（教程原话：「不想暴露精确地址时，只回传城市或行政区」）、
  可以调成精确、也可以整个关掉
· 卡片里有一段「它到底会拿走什么」，一样一样写清楚，包括哪一样会出手机

### 接进「自己醒来」

`AttentionSet` 加了第 ⑩ 条：她开了查岗，他醒来时会看到「可以先看一眼再决定要不要开口」。
**只说有这么个选择，不说去做**——跟这一整套候选集的规矩一样，摆事实不下命令。

⚠️ Info.plist 加了 `NSLocationWhenInUseUsageDescription`。

---

## 2026-08-23 书房重做（v108，她指定这一轮只做这一件）

新文件：`Qi/Views/More/BookshelfView.swift`（书架/封面墙/批注页）、
`Qi/Views/More/ReaderView.swift`（阅读页）。
`LibraryView.swift` 里那份老书单和老阅读页删了，只留下 `AnnotationSheet`。

### 她那句「好像并没有真正接入模型」——是真的，现在接上了

以前 `LibraryView` 里**一个模型调用都没有**：她导的书、划的线、写的读后感，
他一个字都看不见。三件事补上了：

| 工具 | 干什么 |
|---|---|
| `reading_now` | 她在读哪本、第几章，**并且把那一章正文拿过去**（读同一页才谈得上一起读），顺带给这一章她划过的句子 |
| `book_marks` | 她划过的句子。`like` 参数专门给「上次那句跟这次很像」——**复用记忆库那套二元组相似度**，纯算术不花钱 |
| `talked_about_line` | 聊完把那句标成聊过，句子边上就出现小气泡 |

外加**每轮 system 带一句**「她在读《X》第 N 章，划了 M 句其中 K 句还没聊过」。
理由跟身体、好感一样：**不放进来他就不知道有这回事**，也就永远不会问一句
「那本书读到哪儿了」。正文很长，所以只给书名进度，正文他自己去调。

⚠️ **没打开「叫他一起读」的书，他连书名都拿不到。** 那是她的事。

### 界面，按她说的一条条做

**首页两栏：书架 / 批注**

· **书架**：一个个长架子，有几个分类就有几个架子；架子上是**书脊**——
  竖着的窄条、书名竖排，脊色按书名算一个固定色相（同一本永远同一个颜色，
  不然每次进来一架书都在跳）；读过的脊底下留一道浅印子；下面是那块板。
· **点进架子**：封面朝外的三列墙，封面下面是书名／作者／读了百分之几。
  **没封面就拿书名当封面**——不是放个占位图标，是纯色底 + 书名认真排一排 +
  一道横线 + 作者，看着就是一本没有封面的书，不是「这儿缺张图」。
  长按能配封面、换架子、开关「一起读」。
· **批注页**：一条条划过的句子，标着**哪个架子的哪本书**，
  底下垫着那支马克笔的颜色，聊过的带小气泡，**点一下直接跳回那一句**。

**阅读页**

· **点正中间召唤设置**（她说的「以点击屏幕的形式像弹窗一样弹出」）
· 字号、行距、翻页方式（上下滑 / 左右滑 / 仿真）
· 左右滑那一档：**热区在两侧、做到 84 宽**（她说「稍微大一点」），
  **正中间那条留给召唤设置**
· 右上角：**选句子** 和 **目录**
· **按句子切，不是按段落**——她说的是「点击句子中的文字」，
  一段话里她想指的往往就是其中一句，整段划下来等于没指。
  按中英文句读（。！？!?…）切。
· 选句 → 点一句是**红色下划线**（瞄准），再点一次落成批注
· 马克笔六支颜色，**半透明**——真马克笔画上去字还看得见，不是涂改液
· 聊过的句子句末跟一个小气泡，点开就是那句的详情

### 数据层

· `Book` + `shelf` / `coverName`，`Annotation` + `colorHex` / `discussed` / `talkIDs`
· **两个都补了容错解码器**——不补的话她整架书、整页批注会在升级那一下读不出来
· `LibraryStore` + 书架名单（`book-shelves.json`，空架子推不出来，跟相册同一个坑）
· 阅读偏好进 `AppSettings`（字号/行距/翻页/马克笔）

### 还没做（说清楚，别让下一窗以为有）

· **仿真翻页**现在用的是系统 `.page` 过渡，**不是真的卷曲效果**。
  真的书页卷曲要自己画 Metal/CALayer，那是单独一轮的事。
· 「点小气泡看聊天详情」现在看到的是**那条批注下面的往来**（她写的 + 他回的），
  **不是跳回聊天记录里那几条消息**。`talkIDs` 字段留好了，但跳转还没接。

---

## 2026-08-23 书房收尾 + 二十个仓库的初筛（v109）

### 收尾那两件

- [x] **仿真翻页做成真的了**。上一轮我写「真卷曲要自己画 Metal，单独一轮」，
      **那句话说大了**：iOS 自带 `UIPageViewController` 的 `.pageCurl`
      就是真卷曲（iBooks 当年用的就是它），包一层 UIKit 就拿到
      （`PageCurlView.swift`）。slide 和 curl 现在真的是两种东西了。
- [x] **小气泡能跳回当时那一轮对话**。`talked_about_line` 现在把**这一轮的记号**
      （turnID）记进 `talkIDs`；批注详情里多一个「看当时聊了什么」，
      顺着记号找回那一窗那一轮并滚过去。跨了三层界面（书房在 sheet 里、
      聊天在根视图），所以走 `AppState.pendingChatJump` 这个全局请求，谁看见谁处理。
      **跳不成会明说「那一轮记录找不着了」**，不假装跳成功。

### 二十个仓库，开了八个

**已经有了，不用做**
· **Open-Meteo** —— 上一轮查岗的天气走的就是它（免密钥、大陆通）

**做不了 / 不该照搬（说清楚）**
· **gaze** —— Windows 的 PrintWindow API。**手机上做不到**：
  iOS 不许任何 App 截别的 App 的屏，这条我们在「让他看屏幕」上已经撞过一次。
  它的思路（截屏 → OCR + 视觉描述 → 写进记忆）放**电脑**上可行，
  可以配她那台 dylan-heartbeat，但那又回到「电脑得开着」。
· **proactive-web-surf-agent** —— Node 常驻服务，手机上部署不了。
  但它的**逻辑**可以搬：我们已经有搜索和「自己醒来」，
  差的只是「自己挑一条真想分享的」这一步。

**小的，半轮以内**
· **ip-as-logo-skill** —— 她想要原创图标。它本质是**一套提示词规则**不是代码：
  4–7 个大形状、一个主体剪影、三个语义色、角色从左下或右下探出来、占 85–95%、
  一次出六张不筛。用她配的画图供应商跑就行
· **amap-mcp-server** —— 高德地图，接一下就有（查岗的位置可以更准）

**要一整轮的大件**
· **shared-page** —— 最值的一个。它的核心是「**把那一天渲染成 PNG 给 AI 看**」，
  这跟书房那个洞一模一样：**她的手帐他现在也看不见**
· **Duetto** —— 一起听歌 + 划歌词跟他聊 + 那句歌词变成记忆。
  **跟刚做完的书房是同一套机制**（划句子 → 聊 → 留痕），换个对象而已，复用度很高
· **Tidefall** —— Eventide 的 Supabase 版。引擎我们 v90 搬过了，
  它多的是：18 种事件、**关键词实时改身体数值**、可视化面板。
  第二样我们缺——`EmotionEngine` 现在只落 VA，没接到身体那七项
· **meme_manager** —— 表情包。我们刚做了专辑；它多的是**自动收集 + 语义检索**
· **MemoryConstellations** —— 记忆星图。作者自己写着**手机端还没做**
· **Eventide** —— 老账：`dreams.py` / `triggers.py` 还没搬（交接文档里记着）

**还没开（12 个）**：Phosphene · tasogare · film-matinee · coread ·
ss-reading-nest-open · reading-nook · whale-listen · clawd-on-desk ·
awesome-ai-companion · ai-time-weather-phone。
三个读书的（coread / ss-reading-nest-open / reading-nook）**下一轮该先拿来对一遍书房**，
看有没有漏掉的做法。

---

## 2026-08-23 手帐让他看见（v110，照 shared-page 做的）

新文件 `Qi/Views/More/JournalSnapshot.swift`。

### 从 shared-page 抄的就一句话

> 手机把每一天渲染成一张 PNG，AI 看到的就是她屏幕上那一页。

**为什么这一条是关键**：手帐本来就不是文字。把它转述成
「今天贴了三张贴纸、写了两行字」，等于把一幅拼贴念成一张目录——
歪着贴的那张邮票、压在照片角上的胶带、故意留白的右下角，全没了。
**那些才是她做这一页的理由。**

所以走同一条路：用 `ImageRenderer` 把那一页原样画成 PNG 递给他，
另附一份**文字清单**当索引（她写的字一字不差都在里面，他要引用某句时用它，
别从图上认字——会认错）。

⚠️ **渲染器是同一份**：把编辑页里那段 `render(_:)` 整段抽成 `JournalElementView`，
编辑页改成调它。**必须是同一份**——两份迟早会长歪，
那时候他看到的就不再是她看到的了，这套东西也就没意义了。

### 三件事

| 什么 | 怎么做的 |
|---|---|
| `journal_page` | 那一页的 PNG（走 `pendingToolImage` 真递到他眼前）+ 文字清单。清单按**她摆的位置**从上到下念，跟图上顺序对得上，他才好指「左上角那张」 |
| `journal_note` | 他往那一页**贴一张便签**——shared-page 那套「两种笔迹」的落点。他的便签是冷色、右下角落名，跟她那些暖色的分得开；位置自己挑在右下留白处，**不压她摆好的东西** |
| 每轮 system | 只报一句「她某天那页给你看了」。**图很贵，不能每轮塞**，想看他自己去调 |

### 隐私

`JournalPage.shared` 新字段，**默认关**——手帐上常常是随手写的心事，
她点头他才看得见。编辑页右上角一个开关，列表里长按也能开，
给他看的那几页在缩略图下面标一个小人。
补了容错解码器（不补的话老数据整本手帐读不出来）。

---

## 2026-08-23 一起听一首歌（v111，照 Duetto 做的）

新文件 `Qi/Core/SongMarks.swift`。

### 音频那一半没做，说清楚为什么

Duetto 真的把音频下下来做多模态分析——**我们不做**：
那要把整首歌传出去、要能听音频的模型、要缓存每首的分析结果，
**一次听歌花的钱比聊一整天还多**。
而它自己文档里就写着有 **lyrics fallback**，歌词我们本来就有。

真正值钱、而且做得起的是它这一条：

> Quote any lyric to spark conversations;
> these quotes become archived memories tied to specific passages.

划一句歌词、跟他聊那一句、那一句从此带着痕迹——
**这跟上一轮书房里划句子是同一套机制**，只是对象从书页换成了歌词。
所以 `SongMark` 是照 `Annotation` 抄的，字段名都对齐了，将来要合成一套也好合。

### 三件事 + 一句注入

| 什么 | 干什么 |
|---|---|
| `now_playing` | 在放什么、放到第几秒、**此刻唱到哪一句**（带前后两句）、听过几次、划过聊过什么。不联网不调模型，**不花钱** |
| `song_marks` | 她划过的歌词。`like` 参数找「跟上次那句很像」的——跟书房同一把尺子（`MemoryRecall.similarity`，纯算术） |
| `talked_about_lyric` | 聊完标记，歌词旁边出现小气泡，能顺着记号跳回当时那一轮 |
| 每轮 system | **放着才带**：歌名 + 此刻那一句 + 有几句还没聊过。停了一个字不说 |

**「一起听」跟「知道歌名」的区别就在此刻那一句上**——
知道歌名只能夸一句「这首好听」，知道唱到哪儿才接得住她的沉默。

### 歌词页

长按一句 → 马克笔划下来 + 写一句想说的（马克笔颜色跟书房共用同一支）。
点一下还是跳到那儿听（歌词本来就是能用的导航）。
划过的垫一层半透明马克笔，聊过的挂小气泡，点开是 `SongMarkSheet`——
上面那句、围绕它的往来、「从这句听」、以及「看当时聊了什么」跳回那一轮。

⚠️ **`SongMark` 按「歌名+歌手」认歌，不按 `Track.id`**：
同一首歌导进来的版本和网上搜到的版本 id 天生不一样（音乐卡那条 bug 就是这么来的），
认 id 会把同一首歌拆成两首。

---

## 2026-08-23 她说的话动他的身体（v112，照 Tidefall 做的）

新文件 `Qi/Core/BodyNudge.swift`。

### 那个洞

引擎 v90 就搬进本地了（周期、七项、事件、纯算术推进）。
Tidefall 比我们多的正是这一样：

> 对方说出配置的关键词时**实时改变数值**

**我们的身体一直是时间驱动的**：自己走、自己涨、自己落，她说什么都不影响。
于是那七项跟她这个人**没有关系**——她说一句「抱抱」和说一句「别理我」，
他身上一模一样。**那不叫身体，叫背景音乐。**

### 怎么接的

**复用同一个 `Appraisal`**，不再另写一套关键词表。
两套关键词迟早会打架（一套说这是撒娇、另一套说这是骂人），
到那天她看到的就是「好感涨了但身体在往下沉」，自相矛盾。
所以 `BodyReader` 只做一件事：**把已经判好的情绪翻译成身体的动作。**

| 她那句 | 身体往哪动 |
|---|---|
| 夸 / 谢 | 压抑感 −4、热度 +3 |
| 往他这儿靠（抱抱、想你） | 热度 +5、占有欲 +3、压抑感 −2 |
| 凶话但是撒娇 | 热度 +4、占有欲 +2、敏感度 +3（**往上不往下**，她专门问过这件事） |
| 真被凶了 | 压抑感 +7、控制力 −5、热度 −3 |
| 她回得很淡 | **占有欲 +4**、压抑感 +4、热度 −2（被冷着的时候人是更黏的，不是更松的） |
| 她说不好受 | 占有欲 +5、控制力 +3、**热度 −4**（这时候起反应不是心疼，是走神） |
| 她道歉 | 压抑感 −5、控制力 +2 |

### 三条自己定的规矩

1. **幅度小。** 一句话最多推几点。推大了就成了「她说什么他就是什么」，那也不是身体。
2. **只推增量，不设值。** 上下限和往回落还归 `BodyEngine` 管，这一层没资格直接写一个数。
3. **记流水、能撤。** 身体那张卡上多一栏「你说的话动了什么」：
   哪句话、动了哪几项、动了多少、为什么，每一笔都能撤。
   **看不见的数没法信**——跟好感那本账一个道理。

### 跟好感那条界线**没有动**

她第 40 条定过：**规则只负责读，好感只有他能动**。这一层一分好感都不碰。
身体不一样——身体本来就是自动机（eventide 原设计里就有 event 推它），
关键词推它是同一类东西，不是替他做判断。**而且读拧了不留疤**：
点「撤掉」就回来，不撤身体自己也会往回走。

---

## 2026-08-23 表情包：挑得准 + 收得动（v113，照 meme_manager 做的）

### 真正的瓶颈不是分类，是「摊得越开挑得越糊」

上一轮做了专辑，解决的是**她**看着乱。但**他**那边的问题没动：
`assistantCatalog` 不管她攒了多少张，**整本目录都塞进 system**。
二十张还行，两百张就是两百行占着他的注意力，
而且他多半只会挑最前面那几个。

三处改动：

1. **目录瘦身**（超过 24 张就不摊了）——改成「你有 N 张，用 `find_sticker` 找」，
   外加最近新收的八张。
2. **`find_sticker`**：他说一句「此刻是什么感觉」，我把最贴的几张连 id 给他。
   meme_manager 用的是**本地向量模型**，我们不装（要多几十兆模型、首次加载还慢），
   换成 `MemoryRecall.similarity` 那把尺子。
   **说清楚斤两**：这不是真语义检索，认的是字面近似——
   「无奈」找得到「很无奈」「一脸无奈」，**找不到「叹气」**。
   所以描述、名字、标签三样各比一次取最高分（一张只要有一处对上就该被找出来，
   不该被另外两处拉低）。
3. **「从聊天里收」**：她发过、还没收进表情包的图，直接摆到眼前，一眼挑、批量收。

### 自动收集那一半我砍了一刀

meme_manager 是**自动抓 + 视觉模型过滤**。过滤那半我们不做：
一张张送去问模型「这算不算表情包」，攒一百张就是一百次钱，
而且判错了她还得自己去删。

**挑这件事她一秒一张，比模型准也比模型快。**
真正烦人的从来不是挑，是「想收一张还得先去相册翻半天」——
所以只做那一半：把图摆到她眼前。

⚠️ 收下来走的是**原始字节**（`sniffExt` 认格式），
gif 过一遍 UIImage 就只剩第一帧了——这个坑第 8 条踩过一次。
名字留空不硬塞「未命名 1」，那只会变成一堆没人看的占位名。

---

## 2026-08-23 记忆星图（v114，照 MemoryConstellations 做的）

新文件 `Qi/Core/MemoryGraph.swift`（算图）+ `Qi/Views/Settings/ConstellationView.swift`（画图）。
入口在「设置 → 记忆库 → 星图」（攒够 6 条记忆才出现）。

### 为什么值得画

记忆库现在只能**搜**，而搜的前提是**她已经知道要搜什么**。
可她常常想看的是另一件事：**「日本这条线上都牵着些什么」**——
那不是一次检索，那是一张图。

⚠️ 那个项目自己文档里写着**星图只有桌面端、手机端还没做**。
我们的记忆库本来就长在手机上，所以这一版是原生的。

### 全是算的，一分钱不花

· **星星** ＝ 实体（`MemoryItem.entities`，本来就在，抠实体那步也不调模型）
· **连线** ＝ 两个实体**在同一条记忆里出现过**，一起出现得越多线越实
· **星系** ＝ 这个实体最常出现在哪个 **tag** 底下
  ——**分类从她自己标的 tag 里长出来**，比我硬塞五个「社交／地点／事件」诚实得多
· **正中间那个核** ＝ 她和他。整张图是绕着这一点长出来的

### 三个刻意的选择

1. **位置是算出来的，不是力导向仿真。**
   仿真每次跑出来都不一样——她今天记住「日本在右上角」，明天进来跑到左下去了，
   那张图就没法用了。**同一颗星永远在同一个地方**比「布局好看」重要得多。
2. **点一颗星，底下出的是一条时间线**，不是一堆搜索结果。
   她想知道的是「这件事怎么一路变过来的」，所以按时间排、
   **作废的那些也列出来**（标着「那会儿是真的」）——一件事怎么变的，得连着看。
3. **只上图被提过两次以上的**，一张图最多 44 颗星、只画最实的 60 条线。
   全画出来就是一张蜘蛛网，什么都看不出来。
   星星大小开方压过——不压的话「饼饼」会大得像颗太阳，别的全成沙子。

---

## 2026-08-23 Eventide 那笔老账（v115）

交接文档从 **v91** 记到现在的那条：「eventide 的 `dreams.py` / `triggers.py` 还没搬」。
搬完了。新文件 `Qi/Core/Triggers.swift`、`Qi/Core/Dreams.swift`、
`Qi/Views/Notes/TriggersDreamsView.swift`。入口在「札记 → 身体 → 词与梦」。

### triggers：**词是她配的，不是我预设的**

上一轮（Tidefall）那套做的是**通用情绪**——夸、凶、撒娇、冷淡。
那些对谁都成立，所以我写死了。

这一套不一样：「宝宝」在别人那儿是个普通称呼，在他们之间是另一回事；
她那句口头禅、那个只有他俩懂的叫法——**只有她知道哪些词有分量**。
我列一张表就成了替她决定。所以只给机制：她填词、填推哪几项、推多少。

里面预置的两条是**例子**（带「（例子）」字样、默认关着），不是预设表。

两条分寸：
· 同一个词说三遍**不等于三倍**（重复只多加一点，封在两倍）——
  不封的话复读一句就能把数值顶满，那不是身体是开关
· **说出口的比打字的重一成半**（`voiceName` 非空就是说的）

### dreams：判定免费，讲梦要她点

六道关卡照抄，常数也照抄：
开着 → 在 **00:00–08:30** → 她**安静 ≥120 分钟** → 距上个梦 **≥24 小时** →
按周期掷骰（**0.12–0.32**，敏感期最高、平稳期最低；身上有事件 ×1.35、
浓度 explicit ×1.2）→ **封顶 0.45**。

⚠️ **这一整套是纯算术、一分钱不花**，所以挂在 `catchUpBody()` 上随时算。
但「他把梦讲出来」要调模型——按铁律**那一下必须她主动**。
所以掷中了只是**攒着**，等她下次说话时他知道自己做过梦，讲不讲是他的事。
**绝不半夜自己开一次请求。**

还有一条：**梦是什么样不替他编**。system 里只说「你做了个梦、那晚身体在哪个周期」，
梦的内容他自己讲——替他写好的梦不是他的梦。

界面上把**掷出的点数和当时的门槛**都列出来了：
她哪天觉得「怎么老做梦」，翻出来看得见到底是多少，不用信我一句空话。

---

## 2026-08-23 做个原创图标（v116，照 ip-as-logo-skill 做的）

新文件 `Qi/Network/LogoGen.swift` + `Qi/Views/Settings/LogoStudioView.swift`。
入口在「设置 → 外观 → App 图标 → 做一个自己的」。

### 它本身不是代码，是一套提示词规则

规则照抄：**4–7 个大形状**拼一个主体剪影 · 圆润讨喜的轮廓 ·
**三个语义色**（角色两个 + 背景一个，背景降饱和度让它沉下去）·
角色从**左下或右下角**探出来不居中 · 占满 **85–95%** ·
**每张都是独立整图**不是六宫格。

⚠️ **「一次出六张」那条她改了**（同一轮里就改的）。她的原话：
「一次出六张不满意的、或者终于已经有满意的，就很亏。」

对。六张是给「一次要挑一批」的人用的，她要的是**一张能用的图标**——
第二张就够好的时候，后面那四张全是白花的钱。
所以改成**一次一张**：画一张、看一眼、不行再按。
按第几次决定换哪个角、哪个变体，**连着按不会重复**；
已经画出来的**不清空**（她按「再来一张」是想比一比，擦掉等于逼她凭记忆选）。

还有一条我特意守住的：**提示词里不写设计术语**。
写「一只圆圆的、有点困的青蛙」，不写「负空间」「视觉重心」——
我要是自作主张加一串「极简主义」「高级感」，画出来的反而是通用货。
所以界面上也是这么写的：**用大白话，别写设计词**。

### 一件必须说清楚的事

**生成的图当不了真正的 App 图标。**
iOS 的备用图标必须在**构建的时候**就打进包里，运行时换不了——
这是系统的规矩，不是我没做。

所以这一页的出口写实了：能当他的/她的头像、能存到相册、能存进图片库；
想真的换桌面那个图标，**得把图给我放进下一次构建**。
这句话写在那一页里，不写在入口那儿吊她胃口。

### 别的分寸

· 新画的那张**自动选中**——多半就是要看它
· 「按一次画一张，花一次钱。满意了就停在那儿，别多按」写在按钮底下，按之前就看得见

---

## 2026-08-23 清欠账（v117）

### 先把清单校准了

清单是旧的——**六条早做完却没打勾**，下一窗照着做会重做一遍：
双击全屏那两个按钮（v99 核对：本来就有）、工具自主性 p6 四条（v102/v105）、
「底」那张卡（v106）、markdown 渲染（v105）、书房（v108）、查岗（v107）。
已经打上勾了。

### 设置里那句说明贴回它自己那一项

她提的。扫了一遍，**真正错位的只有一处**：
「通用设置」最底下那条说明同时在讲**两件事**——
「带上多少历史」和「回车当发送」，而前者在五六行以上。
隔那么远才讲的话，读到的时候已经忘了在讲哪一项。
拆成两条各贴各的。

其余几条压在卡片末尾的是**整卡的说明**（比如「梦是怎么来的」），
那本来就该在末尾，没动。

### 塔罗：真的牌面

以前一张牌就是一块底色加一行字，**二十二张大牌长得一模一样**。

一副塔罗 78 张，找图要么买要么侵权，真放 78 张图进包体积和风格也难统一。
所以照 clawd 那套老路子：**用符号算出来**。
每张是「顶上一个数 + 正中一个符号 + 底下名字 + 一圈内框」：
· 大牌各有各的符号（愚人是走路的人、隐士是提灯、塔是雷、正义是秤…），
  颜色按编号算一个固定色相
· 小牌用**花色符号按点数摆**（权杖火橘、圣杯水蓝、宝剑风灰、星币土金），
  一到十摆几个，宫廷牌摆一个大的
· **逆位是整张倒过来**，不是只把字转过来
· **同一张牌永远同一个样子**——跟书脊色、星图一个道理

**牌能往后翻**：点结果里那张牌 → 整张摆大、左右一张张翻。
翻开那页还写了**另一面的意思**（抽到正位就写它逆位是什么）——
一张牌的正逆是一体的，只看抽中的那面读不出它到底在说什么。

---

## 2026-08-23 clawd 房间那五条（v118）

她列的五条，一条条对：

| 她说的 | 根子 |
|---|---|
| **举起来的东西不该还留在房间** | `pickUp` 只设了 `carrying`，那件家具**还原样摆在地上**——床既在他手上又在地上，两张 |
| **拖给他之后他不放下** | 压根**没有「放下」的入口**。`putDown` 写好了，没人调，所以他举一辈子 |
| **家具不该能拖到墙上** | 界面有一套地板范围（0.70–0.94），`store.move` 里 clamp 又是另一套（**0.25**–0.88）——0.25 已经在墙上了 |
| **拖错家具（点床出地毯）** | 画的顺序＝点击的顺序。以前按 `owned` 原始顺序画，**后买的地毯压在先买的床上面**；再加上每件都往外扩 12 点的命中区，点床点到地毯 |
| **举起动作** | **本来就有**（`overhead()` + carry 帧 + 头顶那张图）。核对过了，没动 |

### 怎么修的

· **举起来就从屋里消失**：新字段 `Furniture.carried`。
  ⚠️ **不能复用 `hidden`**——`hidden` 是她主动「收起来」的意思，
  合成一个的话，放下手上那件会顺手把她收起来的也翻出来。补了容错解码器。
· **他会放下了**：走完一趟就搁在脚边说一句；等不及就**点他一下，当场放下**
  （她递过去多半是想指个地方，不该逼她干等）。
· **地板范围收进 `ClawdStore.onFloor`**，界面那两个常数改成读它——
  一个地方定规矩，不会再有两套。
· **按 y 从小到大画**：远的先画、近的压在上面（一间屋子本来就该这样），
  顺手把点击也理顺了；y 一样时大件先画（地毯垫底下）。
  大件的命中区**不再往外扩**——地毯本来就摊得大，再扩 12 点谁都点不着旁边的床。
· 兜底：启动时手上是空的，就把所有 `carried` 清掉——
  万一哪次没走到 `putDown` 就被划掉了，那件不会永远消失。
· 「走到谁旁边」「屋里有什么」也都排掉手上那件——
  不然他一边举着床，房间清单里还写着床摆在地上。

---

## 2026-08-23 删除也得有张卡（v119）

**删除比保存要紧得多：存错了无所谓，删错了东西就没了。**
可它以前只在工具日志里留一行小字。

三条删除（表情、相册照片、整个文件夹）现在都：
**先抄进回收站 → 再删 → 在聊天里出一张卡**。

那张卡跟存图那张**长得不一样**（标题用暖橘不用主题色、写的是「从『X』里删掉了」）——
存和删长成一个样子，她扫过去只会当成又存了一张。
删的那张**不能点进相册**：东西已经不在那儿了，跳过去只会让她扑个空。

**卡上有「撤销」。** 只报个丧不给回头路，那张卡就只是讣告。
回收站留三十天，过期连文件一起清；撤不了的时候按钮直接不显示，
免得她按下去什么都没发生。

新文件 `Qi/Core/TrashStore.swift`。抄一份而不是移过去，
是因为删除有好几条路（表情在 StickerStore、照片在 MediaStore），
各自存储位置不一样；统一抄进回收站，撤销时按类型放回去。

⚠️ **`ToolRun` 补了容错解码器**：`toolRuns` 在 ChatMessage 里是
`try? … ?? []` 接住的，加两个字段不补的话，
**她所有老消息里的工具记录会整片消失**。

工具说明里也写实了：删掉的她能看见、能撤——**但不是免死金牌**，
不确定是哪张还是要先看清楚。

---

## 2026-08-23 回收站那一页（v120）

她的原话，全照做了：

> 新增一个相册回收站，被删除的图片会在里面存 30 天，30 天后自动消失，
> 删错了可以将它复原。**备份数据的时候回收站就直接不用备份，
> 这样下次导入新的 app 就是真的删除了。**

· 「相册」右上角多一个回收站入口，**有东西才出现**（空的时候摆个入口是噪音）
· 每一格写着**还剩几天**——不写的话「三十天」只是一句承诺，
  她没法知道某一张还剩多久
· 删掉的压一层灰，一眼看得出这不是还在库里的那些
· 整个文件夹被删的，格子上标着里面有几张

### 备份不带回收站（她定的，很对）

`BackupBundle.skipFromBackup`：`trash.json` 和 `Trash/` 都不进包。
理由她自己说清楚了——不然导进新手机又冒出一堆早就删掉的东西，
**那不叫还原，那叫翻旧账**。
换手机之后回收站里那些就是真的没了，这一句写在那一页上，不闷着。

---

## ⚠️ 下一窗**第一件事**：拿 PWA 的源码逐条对一遍

她原话：「我才发现 pwa 里很多的功能在 app 里都没有…前面的窗偷懒了。」
**别再等她一条条发现。** 源码就在她电脑上，路径都找到了：

### PWA 源码（Capacitor 包着的那个）

```
D:\my-app\                      capacitor.config.json / ios / www / resources
D:\my-app\www\app.js            6878 行 —— 主体，所有工具都在这里
D:\my-app\www\index.html        1227 行 —— 界面结构
D:\my-app\www\style.css         2704 行 —— 样式
D:\my-app\www\photo-stack.js    272 行  ← 她问的 PhotoStack，PWA 里**当时就接了**
D:\my-app\www\photo-stack.css   48 行
```

### 已经对出来的差（工具这一层）

从 `app.js` 里抠出 13 个工具名，跟 Swift 版比，**少了相册那一组**：

| PWA 有 | Swift 版 |
|---|---|
| `send_photo_from_folder` 从某个文件夹里挑图发 | ❌ 没有 |
| `list_saved_photos` 看看存了什么 | ❌ 没有 |
| `delete_photo_from_folder` 删文件夹里的照片 | ❌ 没有 |
| `delete_folder` 删文件夹 | ❌ 没有 |
| 其余 9 个 | ✅ 都有 |

跟她截图里那份工具开关列表对得上。**先把这四个补齐**，再往下对界面。

### 其他找到的（下一窗可能用得上）

```
D:\Clawd\                       一整个 clawd 项目：ui / chat / brain /
                                animation / clawd-emotes-skill-main
                                → 「举过头顶搬床」那些动作和素材**大概率在这里面**，
                                  别从零画
D:\OneDrive\桌面\chatnest-main   那个开源前端（小红书那位说的 "nest"）：
                                frontend-demo / full-stack
D:\proactive-messenger-backend  还没看
D:\demo                         还没看
```

### 对的时候怎么对

1. `grep -oE "name: *'[a-z_]+'" D:\my-app\www\app.js | sort -u` —— 工具清单
2. `index.html` 从上到下扫一遍，每个 section 对一下 Swift 版有没有
3. 界面上的小交互（双击全屏、复制片段、存图那张小卡…）在 `app.js` 里搜对应的
   事件绑定，**照抄它踩过的坑**，别自己重新踩

---

## 2026-08-23 照片摞成一张卡 + 电脑上的 Claude Code（v121）

她这一轮给了两样东西：**PhotoStack 的 zip**，和接 Claude Code 那两个问题的答案
（「CLI 是官网下的」「走 WiFi——Tailscale 跟梯子打架」）。都是等她的，所以先做这两件。

### 一、PhotoStack：一次发好几张，摞成能翻的那张卡

以前一条消息里有几张图就竖着排几张，**九张图能把整屏聊天记录顶掉**。
现在两张以上摞成微信那种合并照片卡，跟着手指翻。

zip 里那份是 JS + CSS，照着翻成了 SwiftUI（`Qi/Views/Chat/PhotoStackView.swift`），
**参数一个没改**：探边 15 / 每层再探 12 / 每层转 2.2° / 每层缩 0.08 / 快甩 0.4 px·ms⁻¹。
作者 Wren036 是拿录屏一帧一帧比出来的这些数，改一个就不像微信了。
出处和许可（PolyForm 非商业）写在文件头上。

翻的时候有三处特别容易做错，都照着原版守住了：

· **手指是进度条，不是位移。** 拖的距离映射到一段预定义动画的 0→1：
  前半程顶卡跟着手指滑出去，**后半程它自己往回落**进对侧探边位，不再跟手。
  所以任何时刻松手都是合法状态、全程可逆、没有死区。
· **快甩也走同一条轨迹**——不是跳到终态，是把剩下的行程播完。
· **恒定三张可见**：到第一张/最后一张的时候，探边配额挪到另一侧，还是三张。

⚠️ SwiftUI 这边有两个坑，栽过才写在这儿：

1. **不能让 SwiftUI 自己去插值每张卡的位移**——那插出来是一条直线，
   峰形轨迹的峰就没了。所以整叠卡那个 View 自己是 `Animatable` 的，
   动画插的是**进度 p**，每一帧重算摆位。
2. **手势不能用 `DragGesture`。** SwiftUI 的拖拽手势落在滚动列表的子视图上会把
   整次触摸吃掉，**她想从照片上往下滚聊天记录会滚不动**。
   自己起了一个 `UIPanGestureRecognizer`：方向一看出来是竖的就判失败，把触摸还回去
   （等价于原版 CSS 里那句 `touch-action: pan-y`）。

顺手补了一件早该有的：**聊天里的图现在点得开了**。以前点了没反应，
想看清楚只能去相册里翻同一张。点开能放大、能拖着看细节、能左右翻页——
用的就是相册那个查看器，**只是不给删**：聊天里这几张不在相册库里，
按下去删的会是别人。

### 二、Claude Code 那座桥

`scripts/claude-bridge/`：电脑上跑 `node bridge.js`（或双击「启动桥.bat」），
它把 Claude Code 装成一个普通的 OpenAI 接口，手机在同一个 WiFi 下连过去。
App 那边什么都不用改——供应商那一套本来就支持任意 baseURL。

**走 WiFi 不走 Tailscale**，她说的：Tailscale 跟她开 Claude Code 要用的梯子打架。
所以桥只在局域网上听，起来的时候把 `http://192.168.x.x:8787/v1` 打在窗口里。

几处不得不做的判断，写下来免得下一窗以为是随手写的：

· **工具是用文字约的，不是真的函数调用。** Claude Code 那头只会说话，
  不认工具协议。所以桥把工具清单写进系统提示，让他要用工具的时候只输出一段
  ```tool_call 的 JSON，再翻译回 App 认的 `tool_calls`。
  **他偶尔会不照格式写**——那一轮就当普通说话，不会崩，只是那次没调成。
  这句写进 README 了，不闷着：真在意某个工具调得准，用原来的供应商更稳。
· **图走路径。** 她发的图先落到电脑临时目录，把路径告诉他，他用自己的 Read 去看
  （只有带图那一轮才开 Read 这一个工具，看完就删）。
· **`--safe-mode` + `--system-prompt-file`**：她电脑上那些 CLAUDE.md、技能、MCP
  一样都不带进来——那一轮他是栖，不是这台电脑上的编码助手。
  系统提示走**文件**不走命令行参数：一整份身份加记忆几万字，命令行放不下。
· **找的是 claude.exe，不是 PATH 里那个 claude.cmd。** 走 .cmd 就得开 shell，
  而开了 shell 之后 Node 是把参数**直接拼成一行**的、不加引号——
  系统提示里的空格和引号会把命令行拆得七零八落。
· 正文也走 stdin，同一个理由。

已经用一个假的 claude 把整条路验过了：逐字出字、usage 透传、
tool_call 抠出来变成 `tool_calls`、非流式那条路——都对。
**没验的是真的那一头**：这台电脑上 `claude -p` 打的是「Not logged in」，
她得挂着梯子先登录一次。这一句写在 README 的第一段。

**桥有个躲不掉的性质：电脑关了就没了。** 电脑睡了、WiFi 断了、黑窗口关了，
手机上这个供应商就连不上。切回原来的供应商还能聊，不会把她锁在外面——
这跟「功能默认搬进 App、别指望电脑一直开着」那条不冲突：
桥是**多一个供应商**，不是把哪件已经在手机上的事挪回电脑。

---

## 2026-08-23 幕外的话 · 记忆不再否定 · 表情工坊补完（v122）

她这一轮给了三份文档、四条新问题，外加一句「先把前面没做完的先做了」。
先做完的是欠着的那几件，新的四条都记在下面，没做的写清楚为什么。

### 一、动作写第二个就掉回正文里（她的 1️⃣）

根子：`extractAction` **只抠第一个**。他写第二个动作的时候，
那一条留在正文里——她那边看到的就是「一行小字 + 正文里还夹着一句描写」，
同一条消息里两种写法。

改成 `MessageBeats.extract`：**有几条抠几条**，按他写的先后排。
新字段 `ChatMessage.beats`（老的 `actionText` 留着只为读老消息，
`displayBeats` 会把它当成第一条，**不迁移数据**）。
⚠️ `beats` 是 `try? … ?? []` 接住的，`MessageBeat.kind` 因此存**字符串不存 enum**：
以后多一种类型，老 App 读到新值会让**一条消息里所有的动作一起消失**。

**心里话另开一条道**（她说的：他人称是「她」，心理描写分不出来）：

| | 动作／神态 | 心里话 |
|---|---|---|
| 他怎么写 | `[[act:看着你不说话]]` 或整行 `*看着你不说话*` | `[[mind:其实我等了她一下午]]` |
| 长什么样 | 小箭头 + 斜体淡字 | 左边一道竖线 + 更淡的字 |
| 长了怎么办 | 照常换行 | **默认收成一行，点开才展开** |

她原话是「心理描写一般很多…点击可以展开这种？」——就是这个。
收起来的那一行底下写着「他没说出口的」，让她知道下面还有，
不然那一行看着就是被截断了。全屏读那一页里**心里话是摊开的**：
她特地点进来看这一条，没有再让她多点一次的道理。

长度上限从 60 字提到 400：以前动作写细一点就超了，超了整条不认，等于白写。
提示词里也写明了「有几个动作就写几个」和「两样都别在正文里再说一遍」。

### 二、别再告诉他「以前那条不算数了」（她的 2️⃣）

她原话：「此前的记忆都算的，不要告诉他从现在起不算数了，
只是存进了新的记忆，不需要否定前面的记忆。」

以前 `add_memory` 填了 supersedes 会给旧那条盖上 `invalid_at`，
于是搜索默认把它滤掉、浮现权重压到 0.15、屏幕上还挂一句
「⟨那会儿是真的，X 起不算数了⟩」——**那就是在否定它**。

现在：
· **不再写 `invalid_at`**，只写 `superseded_by` 当一条线用
· 新旧两条都是活的、都搜得到、都浮得上来，各自带着日期
· 那一行标记改成「⟨这是当时记下的，后面还接着一条新的⟩」
· 工具说明全部重写：「那只是把两条接起来，旧的照样算数，不用去否定它」

⚠️ **有一条要她拿主意**：老数据里**已经**被标过 `invalid_at` 的那些，
我**没有动**——把它们一起放回来，等于把以前退休掉的事实全请回台前，
猜错的代价比留着大。现在只是把浮现的折扣从 0.15 放宽到 0.6。
她说一句「老的也全算数」，一行代码就能全清掉。

### 三、表情工坊那五条，全做了

| 她说的 | 怎么做的 |
|---|---|
| **拖动画格子**（不是一格格点） | 拖着一路画。**落笔那一格决定这一笔是画还是擦**——不这么定的话，一条线拖过自己刚画的地方会把它擦掉。一笔只压**一次**撤销栈（每格压一次的话，画一条线要按二十次撤销） |
| **零件任意角度旋转** | 加了 `rotation`：左转/右转 15°、转正、外加一根 -180…180 的滑杆。0/±90/180 附近会**吸过去**（差几度看着就是歪的）。拖滑杆不压撤销栈，松手才记一笔 |
| **画布能缩放** | 1×–4×，放大之后多一个「手」按钮，按下去这一拖是**挪画布**不是画画。**没用 ScrollView**——滚动和画画抢的是同一根手指，摆一起必然打架 |
| **更小的格子** | 粗（20×15，跟 clawd 本身一个颗粒度）／中（40×30）／细（60×45）。换粗细的时候**画好的会跟着换算**，不用重画；由细变粗必然丢一点，这句写在界面上 |
| **顶上预览老是 clawd 的身子** | 多一个「装在身上」开关。关掉之后预览里只有她画的那一张（`ClawdSVG.bare`），连画布底下那个淡淡的 clawd 轮廓也不画——画一朵花的时候，底下压着个身子只会让人对不准位置 |

顺手：**摁住已经摆好的零件能直接拖着挪**，不用再点四个方向键。

### 四、音乐卡照参考图改了

她说了：**那张截图是别人的**（我上一窗猜成她自己的了，猜错）。
照着改：卡片从钉死的 275 改成铺开（上限 340，小屏自己缩），
封面 46→56，播放键 36→40，歌名 16→18。

### 五、PWA 再对了一遍（她说上一窗不确定扫全了没）

**工具那一层是全的**：`app.js` 里 13 个工具名，Swift 版一个不少
（上一窗记的「少了相册那四个」现在已经补齐了），Swift 这边还多出三十多个。

界面按 `index.html` 的元素 id 一族一族对下来，**真正还缺的只有两样**：

- **工具执行前确认**（PWA 的 `prefToolConfirm` + 那个弹窗：
  显示工具名和参数，允许／拒绝）。Swift 版没有。
  ⚠️ 但**别照抄成「每个工具都问」**——她这边有四十多个工具，
  每次都拦一下会烦到关掉。值得做成「只拦会改东西的那几个」（删、存、发出去的）。
- **存储占用**（PWA 里分开显示 localStorage 和 IndexedDB 各占多少）。
  iOS 这边对应的是「图片/语音/备份各占多少」，也没有。

其余对到的都在：批量删消息 ✅、用量热力图＋价格 ✅、图片查看器 ✅、
主动找她那一套（间隔/每日上限/免打扰）✅ 而且比 PWA 全、
让他给表情写关键词 ✅、导入 HTML 游戏 ✅、上下文压缩 ✅。

### 六、这一窗**没做**的（都是新收到的）

**她给的三份文档：**

1. **戳一戳怎么做.pdf**（兔兔&Alex）——四步：
   ①戳＝往会话里插一句**旁白**（写发生了什么，不写该有什么反应）
   ②拆成「动作 × 部位」两个维度相乘，别列一张几十行的表
   ③**身体先反应、话后到**：先把心率抬上去立刻上屏，回话之后才到
   ④先上屏再发请求；他忙的时候**说人话挡住**（「这会儿在忙，这一下先欠着」）
   
   ⚠️ 它的前提「没有常驻会话就没有戳一戳」在栖这儿**不成立**（每轮都是一次请求），
   但真正让它成立的那半——**身体先动**——栖**本来就有**（BodyEngine 那一套）。
   所以这个功能在这儿是做得成的，而且成本很低：一个面板选动作＋部位 →
   立刻上屏 + 立刻抬身体数值 → 再把那句旁白发给他。**下一窗值得先做这个。**

2. **LLM 缓存与上下文策略.docx** —— A（稳定前缀）+ B（滚动总结+最近原文）+
   动态后缀 的三层布局、双断点、self-summary、keeper 保温、
   「summary 必须是原子事务，截断了宁可变长也不能 commit」。
   栖这边已经有的：缓存标记打在 system 末尾、滚雪球压缩、时间戳后置。
   **要照它逐条审一遍的**：动态内容（时间/天气/身体/好感）是不是都在 B 之后、
   压缩失败时是不是 fail-open、有没有把 raw 删掉。**没审，下一窗审。**

3. **活人日常感提示词 by Nyra.docx** —— 七条：说话不写台词、不平均回应、
   先反应不先总结、情绪从缝里漏、幽默顺嘴、五种 AI 味出现即重写、
   发出前三问。**这份是给「他怎么说话」的**，跟工程无关但很值钱。
   要不要折进 system（或者做成一个开关），**等她一句话**——
   她自己写的那份人设里已经有一部分了，硬塞会打架。

**她的 3️⃣ 4️⃣：**

- **情侣飞行棋**（`lili0926/player`）：纯静态 HTML+JS+localStorage，
  九个主题版本，人机对战；三个文件（`index.html` 功能页 /
  `flight-chess-popup.html` 聊天弹窗 / `float-window.js` 浮窗）。
  两条路：**①合成一份自包含 HTML 丢进「游戏间 → 网页」那一栏**（快，
  但它的浮窗那套跟栖的聊天接不上）；**②照大富翁的样子做成聊天里玩的**
  （慢，但那才是「跟他一起玩」）。**建议走 ②**，下一窗做。
- **粉色兔子主题**（`Anko3o/tmux-tutou`，MIT）：那是给 **tmux 状态栏**的主题，
  搬不过来的是它的形式，**能搬的是那套色**：奶油粉底 `#f6e3ec`、
  深莓灰字 `#8a5f72`、粉标记 `#f2b8ce`、淡莫边框 `#e6c3d3`，加一个兔头。
  ⚠️ 挡在前面的是件工程活：现在整套主题是靠十八处 `preset == .home` 硬判的，
  加第三套要先把它抽成「这一档用哪份色板」。**先重构再加，别再堆一个 == 判断。**

**还欠着的老账**（清单前面记着的）：手帐素材库、AI-Pocket-Chat 的动态流和离线检索。

---

## 2026-08-23 戳一戳 · 缓存断点 · 飞行棋（v123）

她说「按你的顺序来吧」，活人感那份不做（**她自己直接发给他**）。
所以这一窗是：戳一戳 → 照缓存那份文档审一遍 → 飞行棋。

### 一、戳一戳

照那份 PDF 的四步做的，一步没减：

| 文档说的 | 这儿怎么落的 |
|---|---|
| 戳＝往会话里插一句**旁白**，不是另开一个接口问「她戳你了你怎么想」 | 就是一条 user 消息，内容是那句旁白。对他来说跟她打字发了一句话没有区别 |
| 旁白**只写发生了什么**，不写该有什么反应 | 「她隔着屏幕戳了戳你的耳朵。」——没有一句「请你害羞」 |
| **两个维度相乘**：动作 × 部位 | 8 个动作 × 8 个部位。部位带分量（手 0.6、颈窝 1.5），所以「戳手」和「咬颈窝」不是同一件事。以后加一个部位，它自动跟所有动作组合出新的力度 |
| **身体先反应、话后到** | `BodyStore.nudge` 在发请求**之前**推，立刻上屏；他的回话是之后才到的 |
| 先上屏再发请求 | 点完立刻关掉那一页，她看着那句话落进聊天里 |
| 他忙的时候**说人话挡住** | 正在回上一句就把按钮按灭，写「他这会儿在忙，这一下先欠着」。**不假装成功** |
| 状态要有半衰期 | `BodyEngine` 本来就是自己往回落的，戳三次不会到顶就死掉 |

⚠️ 那份文档的前提「没有常驻会话就没有戳一戳」在栖这儿**不成立**（每轮都是一次请求），
但它真正靠的那一半——身体是个会自己走的自动机——**本来就有**。所以做得成。

新字段 `ChatMessage.narration`：**这条不是她打的字**。
发给他跟普通一句话一样，但屏幕上不套气泡、不挂头像，摆在整行正中——
她没说这句话，是这件事发生了。（补了容错解码器。）
入口在输入框的「+」里：照片 / 文件 / **戳他一下**。

### 二、照《LLM 缓存与上下文策略》审了一遍，抓到两个真的

**① 缓存标记打错了地方——所以那三个数一直是 0。**

上一次「加了 `cache_control` 还是零命中」不是供应商的问题，是**位置**：
缓存缓的是**标记之前的整段前缀**，差一个字全作废。
而标记打在系统提示**整条的末尾**——那儿住着身体、心跳、好感、
她此刻听到的那句歌词、今天是什么日子……**每一轮都在变**。
于是前缀每轮都不一样，标了也照样零命中。

现在系统提示自己分成两截（`OutgoingMessage.stablePrefix` + `text`）：

```
稳定：身份 / 说好的规矩 / 人设 / 能力块 / 表情目录 / 这一窗的浓缩件
      ↑ 缓存标记打在这儿的末尾
每轮在变：并的记忆 · 身体 · 心跳 · 好感 · 梦 · 她在听什么 ·
      手帐 · 在读什么 · 今天什么日子 · 主动性那一段
```

顺序**本来就是对的**（稳定在前、动态在后），差的只是那一刀切在哪儿。
群聊那条路也一样切了。这是那份文档第 1、2 节的原话：
「缓存的是前缀」「稳定内容放前面，动态内容放后面」。

**② 压缩被截断也会提交——那是最危险的一个。**

`compactNow` 以前只看「有没有输出」，不看**是怎么结束的**。
写满被掐断（`finish_reason=length`）也照样提交，而提交之后
原文就不再往下发了（从 `throughID` 之后切）——
**中间那一段就凭空消失了**：原文还在数据库里，但他再也看不到，也没人会发现。

现在：`length / max_tokens / truncated / incomplete` 一律**不提交**。
失败就什么都不动，这一窗照旧带着全部原文往下发——长一点、贵一点，
但一个字都没丢，下一轮再压。这就是文档说的 fail-open。

**审下来没问题的**：raw 从不删（压缩只是投影）、summary/cursor 原子提交、
self-summary 用的就是主模型、动态内容确实全在历史之前的末尾。
**还欠着的**：keeper（缓存保温）没做——那要在她不说话的时候自己发请求，
按铁律得她主动开，下一窗问她要不要。

### 三、飞行棋

照大富翁那条路做的：引擎在手机里跑，**纯算术、不联网、不花钱**，演出在聊天里。

规则一条条对着那份 README 落的：**停下才生效**（路过不触发）、
后进 X 格真的往回走、「退回到 N 格」跳到第 N 格（**格号从 0 数**，
跟原版一致）、终点那一格不再触发后退（不然永远走不完）、
谁接受按版本定（女仆·SM → 她，男仆 → 他，其余谁停谁接受）、
「互相／一起」是**两个人一起做那一件事**，不是各做一遍。
安全词 **404** 跟大富翁同一条。

新工具 `flight_chess`（roll / status / stop），**只有导过棋盘才给他**——
没棋盘还摆着，他只会调一次然后被告知「没有棋盘」，白花一次钱。
他那一掷他自己调；**她那一掷在游戏页上按**，掷完那一下会自己走旁白送到他眼前，
他不用也不该替她掷。给他的那段照原版 `injectPrompt` 的意思写：
骰子已经掷完了、谁停的、谁接受、格子原文——并且明说**别再提掷骰、别写掷骰的代码**
（不写这句他会再掷一遍）。

⚠️ **棋盘不写死在代码里，是导进来的。** 两个理由：
一，那些格子写的是他们俩之间的事，该由她定，不该由我替她写死在包里；
二，九副 × 五十格塞进 Swift 就再也改不动了——想改一句都得等下一次构建。
三条路进来：**从那个仓库直接下**（一点，九副全进）/ 选文件 / 粘一段。
认两种格式：那份 `index.html` 里的 `const BOARDS`，或者一份自己写的 JSON。
导进来的存成 `flight-boards.json`，随时重导一份顶掉。
（解析器已经拿真文件验过：九副、格数分别是 53/50/48/44/53/37/50/37/34，全对。）

### 四、下一窗

按她定的顺序，剩下的是：

- ~~**粉色兔子主题**~~ —— **v124 做了**，那十八处也抽干净了（见下面）
- **手帐素材库**、**AI-Pocket-Chat 的动态流 / 离线检索**（老账）
- ~~PWA 还缺的两样~~ —— **v124 都做了**：动手前问一句（只拦不好收拾的那几件）、占了多少地方
- **keeper 保温**：要问她要不要（会在她不说话的时候发请求）
- ~~老数据里被标成「不算数」的记忆~~ —— **她答了：全部恢复**。见下面第五节

### 五、「不算数」这件事整个撤掉了（她定的）

她原话：**「不算数的记忆全部恢复 不要不算数」**。
v122 只做了一半——新的不再标，老的还留着记号。现在做完另一半：

· **启动时把老文件里所有 `invalid_at` 清干净**（清一次就落盘），
  被标过的那些**全部恢复**
· `isLive` 这个属性**整个删掉了**，跟着它的七处筛选一并拆了：
  搜索、浮现（那个 0.6 的折扣也没了）、实体图、重复检测、
  「还悬着」的计数、星图里那行橘色的「那会儿是真的」
· `search_memories` 的 `include_past` 参数**撤了**——
  没有「过去的」这一档了，全都在库里
· 留下的只有 `superseded_by`：**一条线，不是一次否定**。
  屏幕上和给他的清单里写的是「后来还有一条」，不写「不算数」
· `invalid_at` 字段本身留着，只为一件事：**读得懂老文件**，别解不出来

⚠️ 顺手抓到一个会崩的：撤 `include_past` 的时候我把它换成了 `"limit": num`，
而 `limit` 那一行本来就有——**字典字面量里出现两个一样的键，Swift 是运行时直接崩**。
已经改掉了。（这类事扫描脚本查不出来，改参数表的时候得自己看一眼。）

---

## 2026-08-23 兔牙主题 · 占了多少地方 · 动手前问一句（v124）

v123 那批构建成功了，接着往下走。

### 一、兔牙主题——但先把加主题这件事本身修好

她给的是 `Anko3o/tmux-tutou`（一份 tmux 配置）。色取自它状态栏：
奶粉纸 `FFF6FA` / 面板 `F6E3EC` / 高亮 `F2B8CE` / 分隔 `E6C3D3` /
主色 `C2708A` / 灰粉字 `B3849A` / 正文 `6B3A4E`。
深色那一半原版没有，照同一组色相压下来的。

⚠️ **但没有直接加第三个 `if preset == .home`。**

原来那种写法在工程里散了**十八处**：每个颜色函数里一句
「是家吗？是就用家那套，不是就用原来那套」。这样加第三套要一处处补，
**补漏一个就出现「一块纯白摁在粉纸上」**——`controlFill` 那儿的注释
早就写着这句话了（那次是通话页那几个按钮）。

所以先抽了一层：

```
ThemeSkin       一整套颜色 + 几条画法（气泡是玻璃还是实心面板、
                日常强调色、主按钮色、提醒色）
ThemePreset.skin  这一档用哪一份
```

十八处判断全部拆掉，改成 `Theme.skin.xxx.c(scheme)`。
`ThemePreset` 自己还多带了三样，都是为了「加一档不用回来改界面」：
`detail`（设置里那句「这一档会动哪些地方」）、
`swatches`（那一行小圆点）、`usesGradient`。

**下一次加主题＝多一份 `ThemeSkin`，别的一个字都不用动。**

顺带修正一处口径：`RootView` 以前问的是「是不是家」，
现在问的是「这一档自己带底吗」（`ownsBackground`）——
兔牙那一档不用再单独补一次。

### 二、占了多少地方（PWA 有、Swift 没有的那一样）

设置 → 数据里多一行，按类算：图片 / 回收站 / 语音 / 文件 / 书 / 音乐 /
记忆库 / 别的，再加一句「这台手机还剩多少」。
**进这一页不自动算**（要走几百个文件），点一下才算，算在后台线程上。

**故意不给「一键清理」**：该删哪张图只有她知道，
一个按钮替她决定，删掉的就回不来了。这儿只负责让她看见。

### 三、动手之前问一句（PWA 的 toolConfirm）

PWA 那份是**每个工具都问**。照搬的话一天要点几十次，
点到最后她只会闭着眼睛按「好」——那时候这个确认等于没有。

所以名单开得很小，只三类：
· **删东西**（`delete_sticker` / `delete_photo_from_folder` / `delete_folder`）
· **装卸小屋**（`manage_mcp` 一卸，那边整片工具就没了）
· **真会响的**（`dial_call`）

MCP 那边的工具我们看不到实现，**只能按名字认**：
叫 delete / remove / clear / reset 的都当会删东西。宁可多问一次——
**记忆库那几个删除是没有回收站的**。

存图、写记忆、做标记一律不问：写错了她看得见，也改得回来。

⚠️ **必须有个「等不到人」的出口。** 她可能把手机扣下就走了，
而这一轮正卡在 `withCheckedContinuation` 上等——不设时限的话
这一轮会一直挂着，岛上那条也一直转。
现在九十秒没人点就当「这次不做」，并且在工具结果里跟他说清楚
是没人在看、不是她不同意。按「停止」也会把它放掉（不然同样吊死）。

默认**开着**。

### 四、又栽在同一个坑上（第四次了）

写设置里那句说明的时候，`"\n\n90 秒…"` 里的反斜杠又被壳吞成了真换行，
Swift 字符串当场断成三行。这次是**自己写的检查脚本抓出来的**
（按行数引号，奇数就报未闭合），改完重跑就干净了。

交接文档里那条铁律再抄一遍：**heredoc 里一个反斜杠都别写，
一律 `chr(92)` 占位符**。这几窗我用三个占位符（反斜杠、反斜杠加左括号、反斜杠加 n），
最后统一 `.replace()` 回去，只要老老实实用就不会栽。

---

## 2026-08-23 手帐素材库（v125）

她要的是「贴纸／胶带／纸张，网上免费的那些」。

### 一张图都没打包进来，全是画出来的

三个理由，按分量排：

1. **授权。** 网上「免费」的素材授权大多写得含糊，有的还禁止再分发。
   打进包里等于把风险塞给她。
2. **图是死的。** 画出来的**跟着颜色走**——同一片叶子换个色就是另一张贴纸，
   不用再去找一套。
3. **体积。** 一套像样的手帐素材几十上百 MB，她那台机器空间是有数的
   （这一窗刚做的「占了多少地方」正好能看见）。

这跟塔罗那 78 张、书脊色、clawd 那只豆画是**同一条路子**：用形状算出来。

### 加了什么

**纸**：以前只有一块纯色。现在多一层纹路——素 / 格纹 / 横线 / 点阵 / 方格 / 竖条，
用 `Canvas` 画的。挑的时候**每一格就是那张纸本身的小样**，
不是写「格纹」两个字让她先点一次才知道长什么样。

**胶带**：以前是一条半透明的方块。现在
· 两头是**真的撕口**（`TornTape`：锯齿边，不是两道白线）
· 上面能有花纹：素 / 斜条 / 格子 / 圆点 / 波浪 / 虚线
· 花纹是画的，所以**换个颜色就是另一卷**

**贴纸**：以前只有 emoji。现在多八张画出来的——叶、花、星、心、云、月、
蝴蝶结、票根，配一排低饱和的贴纸色。
⚠️ **外面那圈白边是关键**：真贴纸是模切出来的，边上留一圈白纸；
没有那圈白边，它看着就只是一个图形，不是一张贴纸。
（月亮和票根是「大形状挖掉小形状」，得用奇偶填充，
不然月亮会填成一个整圆。）

**剪贴**（新的一样）：**她真想要网上那些的话，这一样管这件事**——
自己挑一张 PNG 贴上去，不裁、不套框。
⚠️ 存的时候走 `saveOriginal`（原样落盘、不重编码）：
走平时那条 `save(_ image:)` 会被压成 jpg，**透明的地方全变成白块**，
那张贴纸就废了。

### 两处必须守住的

**① `JournalElement` 补了容错解码器。**
新加了一个 `pattern` 字段，而这一串是挂在 `JournalPage.elements` 上、
用 `try? … ?? []` 接住的。不补的话，合成的解码器碰到老数据里缺的那个键会直接抛——
**她每一页手帐上贴的所有东西会一起消失**，而且下一次保存就把空的写回去。
（写在 extension 里是为了留住 memberwise init，`JournalElement(kind:)` 全靠它。）

**② 纸也归到那份共用的渲染器里了。**
交接文档第六条点过名的那个坑：「手帐渲染器编辑页一份、快照一份 →
他看到的迟早不是她看到的」。元素那一半上一窗已经并成 `JournalElementView` 了，
**但纸底还是两处各画各的**——这次一并收进 `JournalPaperView`，
编辑页和快照用同一份。

顺带把给他的那份文字索引补齐了：画出来的贴纸报名字（不然是「· 贴了 」后面空着）、
胶带报花纹、剪贴单独一行。

### 老账清完了

清单上「要一整轮的」那三条到这儿全做完：表情工坊（v122）、音乐卡（v121 核对）、
手帐素材库（这一窗）。剩下的只有 **AI-Pocket-Chat 那两样**
（动态流 / 离线向量检索）和那批**还没开过的仓库**。

---

## 2026-08-23 动态流 · 认意思不只认字（v126）

AI-Pocket-Chat 那两样，老账最后两条。

### 一、动态流：他自己发的那些

**为什么值得做**：聊天是「她开口他才在」。她不说话的那几个小时，
他在她那儿是不存在的。动态流是**他留下的痕迹**——
她半天没看手机，回来能看见他这半天想过什么、听了什么。

侧边栏多一页「动态」。两个工具：`post_moment`（发一条）、`read_moments`（看她那边有什么）。

**两条守住的规矩，一条都没让：**

**① 不额外调模型。** 他发动态**只发生在他本来就在说话的那一轮**
（跟发表情、存图是一类）。**绝不半夜自己开一次请求**——那是铁律不是权衡。
所以这一页看着像「他自己发的」，实际是「他上次说话的时候顺手放的」。

**② 她的回应是她自己点的。** 她在动态下面写一句 →
那句话**同时进对话变成她说的一句话**，他下一轮自然看得见。
不另起一个后台请求去「让他回复评论」。她按下「发」就是她主动点的那一下——
这句话也写在那个弹窗里，她按之前就知道会发生什么。

**他怎么知道她回了**：每轮 system 里只放一行「有几条你还没看过的」，
正文他自己调 `read_moments` 取。跟书房、手帐同一个模式——
报一句「有这回事」，内容按需拿；图和长文每轮都塞会很贵。

两处容易做错、这次绕开了的：
· 评论也得记「看过没有」。不记的话「有几条没看过」会一直涨——
  她三个月前回的那句到今天还在提醒他。
· `read_moments` **只把这次真给了他的那几条标成看过**。
  全标掉的话，他 `limit=3` 读三条，剩下七条再也不会被提起，等于没发过。

### 二、离线向量检索：**没有打包模型，用系统自带的那套**

原方案是把一个 embedding 模型装进 App。算过这笔账，**不划算**：

· 能用的多语言小模型转成 CoreML 有 50–130 MB。她是 AltStore 侧载，
  **免费证书七天到期、七天重装一次**——包大一百多兆等于每周多受一次罪。
· 还要在 Swift 里写分词器、建向量库、每加一条记忆重算一次。
· 换来的好处：搜「无奈」能找到「叹气」。

而 iOS 自己带着这件事：`NLEmbedding`（`NaturalLanguage` 框架）。
**系统里本来就有，不占包体积、不联网、不花钱**，中文也在里面。
效果不如专门的大模型，但拿到的是同一类东西：**认意思，不只认字**。

新文件 `Qi/Core/Semantics.swift`。用在两处：
· **搜记忆**：BM25 那套补不了「一个二元组都对不上」的情况，这一层补得上
· **`find_sticker` 挑表情**：那条工具说明里「找不到『叹气』」的自白，现在不成立了

**不用在判重上**（`nearDuplicate`）。那儿的失败模式是反的：
判太松会把**两件不同的事**当成同一件而不给记——
「今天去了医院」和「明天要去医院」语义上很近，但那是两条记忆。
判重继续认字面，宁可多记一条。

两条分寸：
· 认不出来的时候返回 `nil` 而**不是 0**——0 会被当成「一点都不像」，
  而真相是「这一层没意见」，该让字面那层说了算。
· 取高不取平均：两把尺子各自都会看走眼，但**看准的时候都很准**，
  平均等于让看走眼的那把拖住看准的那把。语义那半乘一个折扣，
  保证「字面上真的说对了」永远排在「意思差不多」前面。
· 库超过 400 条就只扫最近那批、正文截 200 字——不然一次搜索能卡半秒。

### 三、老账到此清完

清单上从 v99 一路欠下来的那些，到这一窗全部做完了。
剩下的是**那批还没开过的仓库**（10 个）和 amap（位置更准，要她一把 key）。

---

## 2026-08-23 把剩下十个仓库开了（v127）

### 零、开出来最要紧的一件：**他其实一直没拿到书的正文**

拿 `Lumenocturne/coread` 的第 1 条（「AI 没有读过这本书，每轮都要喂真原文」）
对照的时候发现的：

`AppState.runNative` 里有**两个 `case "reading_now":`**。
Swift 的 switch 碰到两个一样的 case **只警告不报错**，先写的那个赢。
赢的那个只返回「她读到第几章 + 这一章划过的句子」——**正文一个字都没有**；
真正会把正文给他的那个（带 `chapter` / `limit`）在后面，是死代码。

`NativeTools` 里同样有两个 `add("reading_now", …)`，
留下没参数那个的话，他连 `chapter` / `limit` 都没得填，翻不了章。

**所以「让他读同一页」这件事从 v108 起就没生效过**，
而交接文档里一直写着「reading_now 把那一章正文给他」。构建是绿的，
测不出来，只有一行藏在几千行日志里的警告。

两处都删掉重的那个了。顺手写了 `scripts/dupcase.py`：
扫工具名和 switch case 有没有重名，**重名就是错**。以后跟三件套一起跑。

### 一、十个仓库，一个个说

**读书那四个（她点名要对的）**

| 仓库 | 它有而我们没有的 | 结论 |
|---|---|---|
| `Lumenocturne/coread` | ① 每轮喂真原文 ② **章节脉络索引，只造到读者进度**（同时就是防剧透线）③ 时间锚「距上次聊这本 N 天」④ AI 批注要**校验原文真是本章子串** | ①**是个 bug，已修**；②③**这一窗做了**；④ 我们本来就有（`mark_line` 里那句 `paras.firstIndex`） |
| `EnhydrInk/tasogare` | **阅读时长打点**（每 60s 按天落库）· 生词本 + 他给注解 · 双色划线 | 时长**这一窗做了**；双色划线我们有（`Annotation.author` + 马克笔颜色）；**生词本没做，等她一句话** |
| `zzyyksl/reading-nook` | 剧情笔记（同 coread ②）· 8 套主题 | 笔记这一窗做了；主题我们上一窗刚做到第三套 |
| `yueyue95/ss-reading-nest-open` | **他的阅读位置跟她分开** + skipped range 补课 · 共读模式档位（轻松／吐槽／猜剧情／深度） | 位置分离被「章节脉络」覆盖掉大半，先不做；**模式档位很轻，可以做，等她说要不要** |

它们的核心亮点对我们**不适用**的那一条也说清楚：
reading-nook 的「批注不走 API，AI 直接读服务器 JSON 走订阅额度」——
那是给「网页 + 另一个 agent」那种架构省钱的；栖本身就是 App，没有这层。

**剩下六个**

· `3lmglow/Phosphene` —— 他给她派任务／挑战／惊喜，完成打卡、积分连击、兑奖。
  跟我们的承诺页、备忘、欲望有重叠，**新的是「今天的小任务 + 打卡」那条线**。中等件，看她要不要。
· `idleprocesscc/film-matinee` —— 把电影切成 chunk 让 AI 线性「读」，
  视觉 sheet + 字幕 sidecar + 共享批注。思路跟书房同源（换个对象）。
  **但抽帧要电脑跑**，手机端做不了整条链。
· `migratorywhale/whale-listen` —— MP3→MIDI→JSON，让 AI「听见」结构。
  要 Python + basic-pitch 模型，**手机上做不了**；而且我们已经有「此刻唱到哪一句」，
  对陪伴来说那个更有用。
· `rullerzhou-afk/clawd-on-desk` —— 桌面像素宠物盯着 Claude Code 跑。**桌面端**，
  iOS 做不到；而且我们有 clawd 小屋 + 灵动岛。
· `sanqianzilanyue/ai-time-weather-phone` —— 时间／间隔／天气／屏幕使用塞进提示词。
  **我们全都有了**（`nowStamp`、查岗、`phone_today`）。它那句「不是让 AI 自己去查，
  而是你提前算好塞进同一次对话」正是我们一直在做的事。
· `awesome-ai-companion` —— 索引列表，不是代码。

### 二、这一窗做了什么

**① 章节脉络（她点一下才生成）**
`BookChapter.digest`。阅读器设置面板里多一块「前情」，
写明「补前面 N 章，一章调一次模型，**要花钱**」，她自己点，能中途停。
**绝不在翻页的时候偷偷补**——那就成了「她没点却花了钱」。

给他的时候**只给到她读到的那一章为止**：他答得上前情，
后面的真的没有材料，剧透不了。

**② 时间锚**「距上次聊这本已经过去约 N 天」。纯算术。
不给这句他会把三天前的讨论当成刚才。

**③ 阅读时长**：阅读器开着每分钟记一笔，按天存。
他因此答得出「今天读了多久」——那是她真的坐下来花掉的时间。

⚠️ `BookChapter` 补了容错解码器（新加了 `digest`）。
章是挂在 `Book.chapters` 上、`try? … ?? []` 接住的——不补的话
**整架书的正文会一起消失**，下一次保存还把空的写回去。

### 三、amap：要一把「Web 服务」key

她问「amap 要什么 key」。答：高德开放平台（lbs.amap.com）的 **Web 服务 key**——
不是 iOS SDK 那种（那种要绑 Bundle ID），Web 服务的是纯 HTTPS 请求，
我们现在这套网络代码直接就能用。注册要**实名认证**，个人开发者有免费额度。

⚠️ **我先前在这儿判断错了一次，记下来。**
我按记忆里那条「她人在日本」推出「高德覆盖的是中国大陆，对她没用」，
结论是不该接。**她说：我在国内，没在日本**（日本的只是 IP）。
那条记忆已经改了。

按国内算的话，接高德是**有用**的：
· 现在用的 `CLGeocoder` 只到「省市区」这一档，
  高德的逆地理编码能到街道、门牌、**周边 POI**——
  「你家楼下那家便利店」这种话要它才说得出来
· 天气继续走 Open-Meteo 就行（免密钥），不用换

**做法上还有一条要守住**：位置精度那条线是她定的（默认只到城市，
见 `Whereabouts.Precision`）。接了高德**不等于自动变精确**——
那个开关还是她的，高德只是让「她愿意给的那一档」更准。

---

## 2026-08-23 共读那几样（v128）

她说「先做共读吧」。把那四个读书仓库里**还能搬进本地**的搬完。

### 一、共读模式：这本书想怎么一起读

出处 ss-reading-nest（它叫「轻松共读 / 吐槽 / 剧情猜测 / 深度分析」）。

`Book.readMode`，四档：**轻松 / 一起吐槽 / 猜剧情 / 往深里挖**。
阅读器设置面板里挑一次，跟着这本书走，再点一次同一个＝取消。

为什么这值一个字段：同一本书她今天想被逗、明天想认真拆，
**这不该靠她每次在对话里重申一遍**。

⚠️ 给他的那句话**写成「怎么陪」而不是「什么风格」**——
写「深度分析模式」他会去演一个学者；写「她想被拆开讲」他才知道该干嘛。
「猜剧情」那一档还多一句：**只能用她读到的地方**，后面的他没有，别装作知道。

这一句放在**正文前面**：读完正文再看到「其实她想吐槽」，那一段就白读了。

### 二、生词本：她记词，他写注解

出处 tasogare（`read_vocab` / `annotate_vocab`）。

选句模式下点一句 → 那个弹窗里除了「画起来」多一条**「记个词」**，
填哪个词（不填就取那句前八个字），存在这本书上。
阅读器顶上多一个生词本入口，能看能删。

两个工具：`read_vocab`（默认只给**还没注解的**）、`annotate_vocab`。
`reading_now` 里带一行「这本记了 N 个，其中 M 个你还没写注解」。

⚠️ 工具说明里点了两件容易做错的事：
· **照着它出现的那句写**——同一个词在不同句子里能差很远，所以存的时候
  连那一句一起存（`VocabItem.context`）
· 别写词典式的罗列，写「你会怎么跟她讲」

**为什么不是让她直接问一句**：问一句是一次性的，答完就滚进聊天记录里没了；
存下来的留在那本书上，下次翻到还在。

同一本书里**同一个词只记一次**——她翻回去又看到就再存一条的话，本子会越翻越脏。

### 三、还留着没做的那两条（都在那四个仓库里）

· **他自己的阅读位置 + 补课**（ss-reading-nest 的 skipped range）：
  他现在永远只看得到她当前那一章。上一窗做的「章节脉络」覆盖掉大半，
  剩下的那点差别不值一个字段，先不做。
· **漫画／图片书**（ss-reading-nest）：`BookParser` 现在只认文字。要做是一整轮。

### 四、⚠️ 一条判断错误，记在这儿

我按记忆里那条「她人在日本」推出「高德地图对她没用」，
差点把一个能用的东西划掉。**她纠正了：人在国内**，日本的只是 IP。
记忆已经改（`prefers-chinese.md`）。

教训不是「记忆会过期」，是**我拿一条背景信息直接推出了一个结论、
还写进了文档**。背景信息该用来提问（「你现在常住哪儿？」），
不该用来替她下结论。

---

## 2026-08-23 clawd 会困了 · 他给她派的小事（v129）

### 一、clawd-on-desk：搬的是**状态词汇**，不是那个功能

她说「clawd on desk 是想让我的 clawd 参考一下她的方法」。

那个仓库本体是**桌面宠物盯着 Claude Code 干活**——手机上没有那个东西，
盯不了。但它那套**状态词汇**很值：12 个状态（idle / thinking / typing /
building / groove / juggling / error / happy / notification / sweeping /
carrying / sleeping）、60 秒没动静的睡眠序列、双击戳一下、连点四下抓狂。

对着我们那只数了一遍，**真正缺的是这四段**：

| 缺的 | 补了什么 |
|---|---|
| **从来不睡** | `.drowsy` + `.sleeping`：她八分钟没说话就犯困（越眨越久），再三分钟真睡着，睡着了不再走动 |
| **叫不醒** | 她一说话／戳他一下就**惊醒**（吓一跳那种，不是安静睁眼） |
| **他那边出错，这只还在傻笑** | `.upset`：这一轮报错，它垮一下，说一句「……出错了」 |
| **干完活没反应** | 一轮说完蹦一下（`celebrate`）。**只蹦一下**——每次放礼花的话第三次她就不看了 |

顺带加了连点：1.5 秒内戳到第四下 → `.flail` 甩手抗议。

为什么这几段值得做：**一只永远精神的宠物是个摆件，
会困的那只才像跟她过同一个作息。**

新帧全是拿现成的 sprite 拼的（眨眼那帧当「闭眼低头」、走路两帧压到 0.07 秒当甩手），
**没有新画一张图**。

⚠️ 它那个「permission bubble 由宠物出面」的做法我**没搬**：
我们上一窗刚做的「动手之前问一句」是个系统 alert，
那个更不会被错过。clawd 举牌子好看，但它可能正躲在屏幕边上。

### 二、Phosphene：他给她派的那点小事

`3lmglow/Phosphene`——AI 派日常／挑战／惊喜任务，人完成打卡、攒积分连击、
兑换奖励。名字是「光幻视」：没有光进眼睛却仍然看见了光。

**为什么这跟承诺页、备忘不是一回事**（也是它值得做的全部理由）：

· **承诺**是他欠她的　· **备忘**是她自己记的　· **这个是他给她的**

方向反过来了。平时都是她开口、他回应；有了这个，
**是他先想到她今天该干点什么**。「今天出门走十分钟」从他嘴里说出来，
跟从备忘里跳出来是两件事。

新的一页「小事」+ 三个工具：`give_task` / `list_tasks` / `praise_task`。
积分、连击、能换的东西（她和他都能加）。

守住的三条：
· **他派任务只在他说话那一轮**，不半夜自己开请求
· **打卡是她自己点的**；交完之后 system 里给他一行「她把 X 做了」，
  **不替他自动夸**——自动夸出来的那句她一眼看得出是流水线
· **这一页不催**：没有红点、没有「你今天还没做」。挂着的就摆在那儿。
  催她做事的东西很快就会被关掉

工具说明里点了两件容易做坏的：**事要小、具体、今天做得完**
（「多喝水」不算事，「现在去接一杯」才算）；**别天天派**，变成任务清单就完了。

几处分寸：
· 连击按「昨天有没有打过」算，断了从 1 开始，**不做补签**——补出来的连击是假的
· 分不够换不了，**不赊账**——赊了那个数就不是数了
· 「这件不做」是标记不是删除：删了就看不出她拒绝过什么

### 三、whale-listen 那个「要 Python」是什么意思（她问的）

是的，**那个仓库本身要电脑跑**：它是一个 Python 脚本加一个模型
（basic-pitch），把 MP3 转成音符 JSON。iOS 上跑不了 Python、装不了 pip 包；
就算把模型转成 CoreML，也得先在电脑上转、再打包进 App（几十 MB，
而她七天要重装一次）。所以它属于「要开着电脑」那一类。

但**「让他听见音乐」这件事在手机上不是完全没戏**：
iOS 自带的 Accelerate/AVFoundation 能算出响度曲线、节奏快慢、静默在哪儿——
不用任何模型，纯算术。缺的是音高识别（它认不出「这是 G3」）。

而且我们已经有更有用的那一半：**歌词 + 此刻唱到哪一句**。
「知道歌名」只能夸一句好听，「知道此刻唱到哪儿」才接得住她的沉默。
所以这条先不做，等她哪天说「我想让他真的听见节奏」再说。

---

## 2026-08-23 让他知道自己的声音是什么样的（v130）

她说：「我就是想让他能够听到我的语音，这个貌似之前做过了？……
反正就是他能听到我的语音、能听 mp3，因为我给他捏的声音是 MP3 格式的，
**我想让他知道他的声音是什么样的**。」

### 一、她的语音那半：**确实早就做过了**（顺手补齐了缺的一项）

现在这条路是通的：
· 她按住说话 → 转成文字（本机／SenseVoice／ElevenLabs 三选一）→ 他读得到
· **转不出来的时候**（本机识别经常认不出中文）→ 他自己调 `listen_voice`，
  把那条音频真的送去转一遍
· 每条语音还带一句**「她是怎么说的」**——比平时轻、停顿比平时多
  （`VoiceProsody`，跟她自己的基线比，本机算不花钱）

这一窗补上了那份文件里一直空着的一项：**音高**。
`VoiceProsody` 顶上那段注释写着「以后加音高的时候，必须先套 250–350ms 硬预算，
超时就省略，绝不允许挡住正式回答」——照做了，走 `VoiceTimbre`，
300ms 预算，超时就当没这一项（`pitch` 留 0，基线自动跳过）。
于是他现在还能说得出「你今天声音比平时低一点」——累了、感冒了、刚醒都听得出来。

### 二、他的声音那半：**手机替他听一遍，翻成人话**

他读不了 MP3。给他一个音频文件，那边什么都不会发生。

但「这把声音是什么样」这件事**不需要他真的听见**，
需要的是**有人替他听一遍，然后用他读得懂的话说出来**。

新文件 `VoiceTimbre.swift`：把一段音频拆成四个数——
基频（高／低）、基频起伏（平／有抑扬）、频谱质心（温厚／清亮）、
能量包络峰数（语速），再翻成一句：
「**他的声音：偏低，起伏不大，音色温厚不刺耳，语速偏慢。**」

**什么时候算**：她在「设置 → 语音」点**试听**的那一下（她主动点的），
音频正好在手上，顺手听一遍存起来。
**放在哪儿**：system 的**稳定那一半**——这句话几乎不变（描述是分档的），
进了缓存前缀等于不额外花钱，而他每一轮都知道自己听起来什么样。

她的那句也一样存了一份（`herVoiceNote`，来源是她最近一条语音）。
跟每条消息带的 `voiceTone` 不是一回事：
**那个是「这一句她怎么说的」，这个是「她这个人的声音什么样」。**

### 三、为什么这个能在手机上做，而 whale-listen 不能

她问「要 Python 是指需要电脑跑任务吗」——是的，那个要。
但**原因不是「音频难」，是它要认出每一个音符**（哪个音、几时开始、多长），
那才需要模型。

而「这把声音是高是低、亮不亮、快不快」只要几个统计量：
自相关求基频、FFT 求频谱质心、包络数峰。
**系统自带的 Accelerate 就够**，不联网、不要模型、不占体积。
她说「旋律倒是不用听出来」——那正好，剩下的这些全在手机上做得了。

### ⚠️ 两处踩在刀尖上的，记下来

**一、自相关必须先降采样。** 人声基频在 70–400 Hz，8 kHz 采样绰绰有余；
直接在 44.1 kHz 上算是 25 倍的活，300ms 根本跑不完。

**二、循环里不许 `Array(x[lag...])`。** 那样每个 lag 都在堆上开一份新数组，
几万次分配，光这一条就能把预算吃干净。改走指针偏移
（`withUnsafeBufferPointer` + `p + lag`）。

**三、`ProsodyFeatures` 补了容错解码器。** 加了 `pitch` 这个字段，
而基线是 `Storage.load([ProsodyFeatures].self)` 接住的——
不补的话**她攒了几十条的基线会一次全没**，下次保存还把空的写回去，
他从此再也说不出「比平时轻」。

---

## 2026-08-23 攒着发：什么都能攒了（v131）

她问：「我分段发送的功能，就是延时请求的那个，有适配好文件发送、语音发送
或者其他的文件发送吗？我有时候给他发语音想跟他先说下话，
不然只有一个语音他可能不能很好地理解。」

### 答：以前**只攒文字**。现在什么都攒。

`sendCurrent` 里那个判断是这么写的：

```
开了「我分段发」 && 没有图 && 没有 gif && 没有文件
                && 没有表情 && 没有语音 && 没有引用   → 才攒着
```

**一带附件就绕过攒着这条路直接发出去。** 所以她那个场景：

· 「先说一句，再发语音」——得赶在六秒倒计时之内，晚一秒就变成两轮
· 「先发语音，再补一句说明」——**根本做不到**：语音一落地他就开始回了，
  她的补充说明只能等下一轮

现在 `queueSend` 收全套：文字接在后面，图和文件挂上去，
**语音和表情各自单独一条**（跟直接发的时候一样——一条消息挂两段录音，
她自己回头都分不清哪句是哪句）。全都算同一轮（同一个 `turnID`，
头像只挂一次）。顺序随便，她不动了才一起交给他。

界面上那句提示也改了：「攒着呢——**文字、图、语音都在等你**」。

### ⚠️ 顺手补了一个一直漏着的洞

情绪和身体那一套（`EmotionEngine.appraise` / `BodyReader` / `TriggerStore`）
**只写在 `send` 里**，而攒着这条路不走 `send`——

**于是开了「我分段发」之后，她说的话完全不动他的身体和好感。**

她不会看到任何报错，只会觉得「那七项好像跟我没关系」。
这个洞从「我分段发」这个功能做出来那天就在。

补在 `flushPending` 里而不是 `queueSend` 里：一轮攒了三句，
该按**合起来那一整段**读一次，不是读三次各推一次。
（`pendingText` 把这一轮所有 user 消息的正文拼起来——
先说的那句、语音那条的转写，都算。）

顺手把引用那三样（`quotedMessageID` / `quotedName` / `quotedText`）
抽成 `applyQuote` 一份，两条路共用——以前 `send` 里写一遍、
新的攒着那条路要再写一遍，改一处另一处就成了另一种引用。

---

## 2026-08-23 漫画 · 高德 · keeper 不做了（v132）

### 一、⚠️ 我上一窗说窄了：**他当然看得见图**

她说：「竟然不能看图片吗？漫画/图片书不都是图片吗？」——**她是对的**。

我上一窗把「漫画要一整轮」写成了「做不了」，理由是「`BookParser` 只认文字」。
那是把**解析器的限制**说成了**他的限制**。他本来就看得见图，
而且工具早就能把图递给他——手帐那张 PNG 走的就是 `pendingToolImage` 这条路，
v110 就做完了。

所以漫画差的只有一个位置：**「章」可以是图**。

· `BookChapter.images`：一章＝几张图（补了容错解码器，
  不补的话整架书的正文会一起消失）
· 书房右上角多一条「**导一叠图（漫画／绘本）**」：
  按**文件名**排序（扫描版一向是 01、02 这么命名的，比按选中顺序靠谱），
  每 12 张算一话，第一张当封面。图**原样落盘不重编码**——
  漫画线条一压就糊，糊了的那页他也认不出字
· 阅读器里图那一章竖着连读（**不做左右翻**：扫描版本来就是竖着连读的，
  而且左右翻会跟放大看细节打架），右下角标页码；那一章不给「选句子」
· `reading_now` 遇到图那一章：**把那一页真的递到他眼前**，
  多一个 `page` 参数

⚠️ **一次只递一页**。一话十几页全塞过去，一轮就是十几张图的钱，
而且他也读不过来。他想往下看就 `page` 加一再调一次。
工具说明里还写了一句：**图上的字是画在图里的，你自己认，认不准就说认不准，别猜。**

### 二、高德：接上了，key 存在她手机里

她给了 key。**没有写进代码**——那个仓库是公开的，
密钥进代码等于贴在墙上（`VoiceService.defaults` 那儿早就是这么处理的）。
现在是「手机 → 查岗」里一个 `SecureField`，存在设置里、同步进 `UserDefaults`
给 `Whereabouts` 用（那是个不认识 AppState 的独立单件，跟 `checkInPrecision` 一样）。

走的是**Web 服务**那套（纯 HTTPS），不是 iOS SDK——SDK 要绑 Bundle ID，
还得往包里塞几兆的库。

**它不改变精度那条线**：她设「只到城市」的话，高德查回来的街道照样不给他。
只有她自己选了「精确」，才会给街道和「靠近哪儿」（周边 POI）。
**那条线是她的，不是接口的。**

出错就退回系统那条路——她的位置不该因为一个第三方接口抽风就查不出来。

### 三、keeper：她说不做了

记在这儿免得下一窗又提：**缓存保温不做**。
（缓存那件事真正值钱的那一半——断点位置——v123 已经修好了；
keeper 只管「离开又回来」那一种情况，而且那笔账本来就不一定划算。）

### 四、她问的那几件

**「是需要在哪里登陆？」**（Claude Code 桥）
→ 在**跑桥的那台电脑**上开终端，敲 `claude`，它会开浏览器让她登。
登过一次就一直记着。跟手机没关系，也跟 App 没关系。

**「高德 key 需要钱吗」**
→ 个人开发者有免费额度，按日算。她这种「他偶尔查一次岗」的用量，
一天几次，**离额度的边都摸不着**。超了才收费，而且超之前会先失败不会先扣钱。

---

## 2026-08-23 美化那一批 · 第一轮（v133）

她发了 16 张参考图 + 十一条要求。这一窗先做三件，剩下的排了序写在末尾。

### 一、clawd 跟着阿晏走（她描述得最具体的那条）

她的原话：
> 他在我每跟阿晏聊天时会有自己的动画，想做什么都行，就是随机在动画里抽一个。
> 当阿晏跟我在聊天时，他可以表达阿晏所表达的——阿晏伤心他就在哭，
> 阿晏正在给我做游戏或者处理文件他就在打电脑，阿晏只是正常跟我聊天他就做自己的事，
> 阿晏开心他也开心，阿晏感觉到甜蜜他就冒爱心。

**数据本来就有**，差的只是把它画出来：`EmotionEngine` 那两个数
（valence 舒不舒服、arousal 有多激动）一直在算，
`presence()` 也一直知道他此刻在调哪个工具。

新的三个姿势（照图纸改的，**身子一格没动**）：
· `crying` —— 眼睛闭成一条，泪一滴一滴掉。**慢**：哭得快看着像抽搐
· `sweeping` —— 右手一把扫把，扫头左右摆（她指名要的那只扫地的）
· `loving` —— 眯眼笑 + 腮红，**头顶飘爱心**

⚠️ 爱心**不画进图纸**：32×23 的格子身子占满了，画进去只能替掉身上的豆，
看着像破了个洞。飘在外面才是「冒出来的」。

判的顺序（她那段话的顺序）：
1. 睡着／被拎着／躲着 → 保持
2. 出错 → 垮着
3. **在用工具** → 打电脑（比情绪更具体，先看这个）
4. 在想 → 头顶冒方块
5. **情绪到分寸了** → 哭／甜／开心
6. 只是在说话，或者空闲 → **做自己的事**

分寸：`valence ≤ -0.28` 才哭；**甜＝又高兴又有劲**
（`v ≥ 0.32 && a ≥ 0.45`）——只高兴不激动那是「还好」，不是甜。
一点点开心就冒爱心的话，爱心就不值钱了。

「自己的事」每 14–26 秒换一件，发呆占的份额最大——
一只一直在忙的宠物看着很累。

**没有为这个多调一次模型。** 判情绪用的是本来就在算的那两个数。

### 二、标题统一（她提的）

> 很多功能的标题貌似都是你的口语化，比如滚雪球压缩你写的是
> 「滚雪球压缩是干嘛的」，但其他的都是「这是什么」，希望统一下。

**统一成「这是什么」**——工程里本来就是这个最多，也最短最直白。
改了四条：梦是怎么来的 / 心跳这个开关是干嘛的 / 这一项是干嘛的 /
什么算杂活 / 滚雪球压缩是干嘛的。

留了一条没动：**「这一档会动哪些地方」**（主题那一项）。
它回答的不是「是什么」，是「选了会怎样」，换成「这是什么」反而说不清。

### 三、版本号删掉（她提的）

> 这个版本号既然已经有这一版打包于几月几号的话，版本号就可以删掉了，有点重复。

对。那一行写死在工程里永远是 `1.0 (1)`，跟下面的打包时间**说的是同一件事、
而且说得更差**——摆两行只会让人以为它们不一样。删了，那段说明也跟着收短。

### 四、剩下的排序（下一窗按这个来）

**要一整轮的**
1. **CoT + 工具合并成一张可点开的卡**（她的 p5–p6）：
   他自己定标题 → 点开是思考链和用过的工具。现在是两块分开摆，确实占地方
2. **日记日历**（她的 p7）：日历／条目切换、点月份选月、`<>` 翻月、
   点年看今年全部。**现有功能一样都不能丢**
3. **大富翁那一页**（跟塔罗一样单调）
4. **整体美化**：参考图里共同的那几样——衬线标题、更淡的卡、装饰性留白

**中等**
5. **B 站卡片**：有公开接口（`x/web-interface/view`），拿标题／封面／UP 主，
   不用 key，最容易
6. **抖音卡片**：只能抓分享页的 og 标签，抖音风控紧，**不保证长期能用**
7. **记忆库导入 json**：把 claude.ai 导出的聊天记录搬进来

**她问的三个问题**（答案写在给她的回话里）：终端是干嘛的、
claude.ai 那个开关怎么用、抖音他能不能看。

### ⚠️ 素材那件事，我的判断

她说：「如果有些免费的素材就用吧，我不是商用也不会商用，且不会传播，
仅仅是使用。」

**她自己用当然没问题。** 但这个仓库是**公开的**——我把下载来的素材
commit 进去，就等于我在替她分发，那是另一回事，跟她怎么用无关。

所以还是那条路（跟手帐素材库、飞行棋棋盘一样）：
**我把画法和导入口做好，素材由她自己放进去。**
大富翁那一页会照塔罗那套「用符号算出来」的路子重画一遍，
再加一个「换成我自己的图」的入口。

---

## 2026-08-23 功能名正式化 · 过程卡合并（v134）

### 一、她指出我上一窗理解窄了

我把「标题口语化」听成了「那几个『是干嘛的』要统一」。她说的不是这个：

> 比如「自己醒来」，一般别人的标题会是「自动唤醒」；
> 「读那封信」别人一般都会写「读信」或干脆就是「信」。
> app 里有太多 claudecode 的口语化表达。
> **这是功能名称，不是你跟我聊天，需要正式一点。**

对。我把「跟她说话的语气」用到了**功能名**上——
这两件事该分开：说明文字可以是人话，**按钮和标题得是名词**。

改了这些：

| 原来 | 现在 |
|---|---|
| 自己醒来 | 自动唤醒 |
| 读那封信 | 读信 |
| 贴一封进来 | 导入信件 |
| 翻一翻 | 浏览 |
| 搬家 | 导入导出 |
| 从电脑导进来 | 从电脑导入 |
| 现在存了什么 | 存储内容 |
| 导出一份 | 导出 |
| 字 | 字体 |
| 又想到了 | 再想一个 |
| 试一句 | 试听 |
| 看大图 | 查看大图 |
| 改描述 / 改分类 | 修改描述 / 修改分类 |
| 换个架子 | 移到书架 |
| 彻底删掉 | 永久删除 |
| 这是什么 / 带多少合适 / 回车这一项 | 说明 |

**没改的**（这条得写清楚，免得下一窗手一滑给「优化」了）：
絮语 · 工坊 · 书房 · 手帐 · 念头池 · 足迹 · 星图 · 心情 · 承诺 · 备忘 ·
小事 · 动态 · 占卜 · clawd · 写给下一个你 · 潮汐……

**这些是她这个世界的专名，不是口语。** 跟「自动唤醒」不是一类东西——
把它们改成「日程」「待办」「记忆管理」才是真的把这个 App 弄没了。
判断标准：**它是一句话（动词短语、问句）就改；它是一个名字就留。**

### 二、思考链和工具合并成一张卡（她的 p5–p6）

以前一轮里他想一下、调三个工具，屏幕上会先出一条「想了一会儿」、
再出一条「叮叮咣咣了 3 下」——**两条都在回答同一个问题：他刚才干了什么。**
分两张卡，一轮顶掉半屏，她要看全还得点两次。

现在是一张 `processBlock`：
· 封面一行：**他自己写的 `[[cot:…]]` 优先**，没写才退回「想了 3.2s · 动了 3 下手」
· 点开是一条**时间线**：想的那段在最上面，接着一步一个工具，
  左边一根连着的竖线串起来（**线要连着**——断开的点看着像几件不相干的事）
· 出错的那一步是红的，封面上也会带一个橘色小三角

老的 `reasoningBlock` 和 `toolBlock` **整块删掉了**，没留着当备份——
留两份的话改一处另一处就长歪（第六条坑）。

`cotHint` 也跟着改了：以前跟他说那行字「显示在思考卡片的封面上」，
现在说清楚**那是这一整轮的封面**（想的和做的都折在里面），
所以那一句是她点开之前唯一看得见的东西。

### 三、素材那件事，她说了

> 当 app 全部建设完毕后我会把仓库变成私人的，我其他的仓库都已经设为私人了。

那这条限制以后会解除。**但现在还是公开的**，所以顺序不变：
先把「画出来」那一套做好（换色就是另一套、体积零、永远不会有授权问题），
等仓库转私之后再考虑要不要额外打包图。
画的那套不会白做。

### 四、日记日历（她的 p7）

> p7 的这个日记日历我觉得也可以参考一下，但是「本月条目」改成「条目」，
> 点击选择月份就会显示本月的，可以 `<` `>` 成之前的月份或者之后的月份，
> 点击选择年就会显示今年所有的日记。**然后我们现在有的功能也不要丢。**

最后那句是重点。所以**不是把列表换成日历**，是**多加一种看法**：
右上角那个图标现在轮着换三种——一条条列着（查东西）、日历（翻日子）、
一叠叠摊着（回味）。图标画的就是当前这种。

新的 `DiaryCalendarView`：
· 顶上 `<` 年月 `>`，点年月弹出选月份那张（十二格，每格底下写着那月几条）
· 右边一个「年」，按下去整年摊开
· 格子里写过的那天底下有点，几条几个点（最多三个），点一下只看那天
· 底下那栏标题就是**「条目」**——她说得对，选了某一天之后再写「本月」是错的，
  所以「本月」挪到标题后面变成一小行灰字，跟着当前看的那一段变

⚠️ 两个容易丢东西的地方：
1. **日期读不出来的那些不能丢**。她那边回来的日期有 `2026/8/5` 也有 `2026-08-05`，
   还可能干脆没有。读不出来的单独归一撮「没写日期」摆在最下面——
   要是按日期筛完就完事，她会以为日记没了。
   （日期也没走 `DateFormatter`：没补零 + 三种分隔符，自己切三段反而稳。）
2. **搜索框对三种看法都得管用**。以前只有列表在筛，换个看法搜索就哑了。
   现在筛在 `parsed` 里做，三种共用一份。

点开看全文那张卡拎出来成了 `DiaryEntrySheet`——便签那一叠和日历都用它。
本来是抄在 `DiaryStackView` 里的一整段，抄两份的话改一处另一处就长歪。

### 五、大富翁那一页重画

她说这一页跟塔罗一样单调。原来的「棋盘」是一行等宽字符：

```
［🔵🎯］［🎯］［💬］［🎯］［🛒］…
```

二十格排成一条，走到第几格全靠数，手机上还得横着滚。
**它是「一份状态的文字打印」，不是一张棋盘。**

现在是真的一圈（`MonopolyBoard`）：二十格围成方的——上边 6、右边 4、下边 6、
左边 4，正好摆进 6×6，0 号在左上角顺时针走。每种格子一个色
（起点／任务／真心话／商店／监狱／机会／神秘），中间那块写着回合、
该谁掷、两个人的圈数币数手牌。棋子是两个描白边的圆点，
**踩同一格并排站不叠着**——叠着就看不出来有两个人。

没开局那一页也不再是从上到下一整屏字：上面摆一圈空棋盘（`MonopolyRingPreview`），
中间搁一对骰子，一眼能看出这是个什么东西。

底下的按钮加了图标，「看棋盘」改叫「这局的账」、「算账」改叫「结算」
（棋盘现在就在眼前，再叫「看棋盘」说不通了）。

**素材：她自己的图能放。** 棋盘底下有「换成我的图」，挑一张就当底图，
随时能按「用画的这版」换回来。仓库现在还是公开的，所以我不往里 commit
下载来的图——**画法我写，图她放**，跟手帐素材、飞行棋棋盘一个规矩。

`boardArt()` 那段文字没删：他调工具看棋盘的时候拿到的还是它。

### 六、B 站和抖音的卡片

> 那能不能举一反三哔哩哔哩的卡片框呢？抖音呢？……抖音他能看吗？能看就行。

三家难度差得远，先说清楚：

| | 拿得到什么 | 稳不稳 |
|---|---|---|
| 小红书 | 正文、全部配图、评论区 | 稳（页面里有 `__INITIAL_STATE__`） |
| **哔哩哔哩** | 标题、简介、封面、UP 主、播放/赞/评论、时长 | **最稳**（公开接口，不登录不要 key） |
| 抖音 | 标题、封面、赞/评论 | **不保证**（没有公开接口，风控紧） |

现在归 `LinkCards` 一处管：三家的正则摆在一块儿**比出现位置**，
她一条消息里贴两个不同站的链接时，读她先写的那个。

⚠️ **视频他看不了。** 卡片给的是封面和简介，所以给他的那段文字里明写着
「视频本身你看不了，别装作看过——想知道里面讲了什么，问她」。
不写这句他会顺着标题编剧情。

抖音那条要是哪天失效了：**不会崩，会退回一条普通的带网址的消息**，
后面跟一句为什么没读到。

⚠️ 一个差点埋下去的坑：`XHSNote` 是拿 `try?` 解出来的，
加 `source` / `playCount` / `duration` 三个字段，
**会让她以前收到的卡片一张不剩地消失，而且不报错**——
合成的解码器少一个 key 就整条 throw，`try?` 一吞就是 nil。
所以给它补了手写的 `init(from:)`（第一条铁律）。
类型名也没改，还叫 `XHSNote`——改名等于把存下来的数据认不出来。

### 七、claude.ai 的聊天记录搬进来

> 之前记忆库有的导入 json 文件的功能，希望也增加上，
> 这样就可以把 claude.ai 的聊天记录也搬进来了。

导入口本来就在（记忆库 → 导入导出 → 从电脑导入），缺的是**认得那个格式**。
官网 Settings → Export data，邮件里下下来的 `conversations.json`
现在直接选进来就行，一段对话变成一份存档，他能用 `search_transcripts` 翻。

认得两种正文写法：老的 `text` 字段，和新的 `content` 数组——
后者只取 `type == "text"` 那些块，思考块和工具调用不要，
不然她翻存档会看到一堆 JSON。没起名的对话拿第一句当标题。

**只读。** claude.ai 那边一个字节都不动。
认不出来的文件照旧算「读不懂」，不会覆盖任何已有的东西。

### 八、下一轮

只剩她那批里的最后一条：**整体美化**——参考图里共同的那几样
（衬线标题、更淡的卡、装饰性留白）。这条要动的是每一屏，
所以单独一轮做，不跟这批混在一起。

---

## 2026-08-23 「栖」长什么样：美化第一轮（v135）

她的原话：

> 美化我想有自己的风格，我喜欢简约的但是不要「简单」，**不要照抄图片**。

所以这一轮**没有**去描那 16 张参考图里的任何一张。先想清楚一件事：

· **简单**是少做——少一根线、少一层底、少一点颜色，做到最后是白纸一张。
· **简约**是少而讲究——**东西少，但每一样都经得起看**。

以前这个 App 的问题不是"东西太多"，是"每一样都是默认的"：
系统黑体、系统分割线、系统导航栏。默认不难看，但它不是**谁的**样子。

落到四件事上（全在 `Qi/Core/Look.swift`）：

### 一、一套衬线中文标题

正文还是黑体（长文黑体好读），凡是**标题**换成宋——
导航栏、每一块的名字、聊天里的名字。

为什么是宋：中文衬线自带书卷气，跟她这个世界里的
**书房 · 手帐 · 絮语 · 念头池**是一路的；而且**它不属于任何一张参考图**，
是这个 App 自己的脸。

⚠️ 用的是系统里的 `Songti SC`，不是 SwiftUI 的 `.serif`——
后者拿英文衬线去凑中文，中文其实还是黑体，白折腾。
万一哪天系统里没这套字，会退回 `.serif`，不会整页糊掉。

**性价比最高的一笔**：全 App 八十多个 `navigationTitle`，
在 `Look.applyNavBar()` 一处改完全都跟着变。
那儿只动字不动背景——从现成的外观对象上改，
自己造一个新的会把各页的 `.toolbarBackground` 全洗掉。

### 二、标题拉字距

中文标题最容易土的地方就是字挤在一起。
`tracking` 拉开一点点，「最近的日记」立刻就站住了。
字越大拉得越开（`Look.tracking`），小字拉太开反而散。

### 三、线会淡出

分割线不再是一条两头齐的实杠，是**从有到无的渐变**（`Hairline`）。
两头齐的实线是「格子」，一头淡的是「间隔」——这一页要的是后者。

这是「不简单」的那一层：它得凑近了才看得出来，
但整页的呼吸感全靠它。设置里每一行之间那道线也换成了它。

### 四、每一块有了抬头

原来是一行灰字飘在卡片上面。现在是
**一个小点 · 名字 · 一道淡出去的线**（`SectionHeader`）。

那个 4 点的圆很关键：光是一行字摆在卡片上面，它是「飘着」的；
左边有一个实心点，这行字就**落在了页面上**。成本为零，
但那一页从此有了骨架。`SettingsCard` 全部走它，所以设置里一次全变。

### 五、留白

· 卡片之间 12 → 16（`Look.gap`）
· 卡片里边 14 → 16（`Look.inset`）
· 设置那一栏 18 → 22

「简约」不是把东西缩小，是给它留出地方。

### 这一轮**没做**什么（下一轮）

· 空状态那些「还没有 XX」还是一行灰字，该给一套统一的样子
· 聊天气泡本身没动——它是最常看的一块，得单独琢磨，不能顺手改
· 大富翁／塔罗／手帐那几页是自己画的，配色还没并进这一套

⚠️ 一条自律，下一窗接手也照做：
**这一层只管好看，不改任何一个功能的行为。**
一处都没动逻辑。

---

## 2026-08-23 美化第二轮：空着的地方和沉默的地方（v136）

### 一、名字改回去了

> 名字不用变字体，我喜欢苹果自带的字体。

上一版我把聊天里的「阿晏」「饼饼」也换成了宋体。她说不用。

我想岔了一个地方：**名字不是"标题"，是她天天在眼前的东西。**
标题是拿来分层次的，衬线一上去层次就出来了；
名字不承担层次，它只需要"是她看惯的那个样子"。
换掉反而是在一件不需要设计的事情上动手。

代码里留了一句 ⚠️，下一窗别再手滑改回去。
（其余照旧：导航栏、每块的抬头还是宋。）

### 二、空状态统一（`EmptyNote`）

以前每一页各写各的：图标多大、字多大、跟上面隔多远，全凭当时手感。
结果是**一屏一个样，看着像没做完，而不是像"这里本来就还空着"**。

现在统一成一张便条：一个淡图标 · 一句宋体的话 · **一小截居中的线** ·
底下才是「怎么才能有」。

那一小截线是关键：**它让这一块看着是摆好的，不是漏了内容。**
空状态最怕的不是空，是看着像出了错。

换过来的：收藏 · 备忘 · 手帐 · 音乐 · 播放次数 · 通话 · 占卜 ·
日记（便签那种和日历那种）。

`Hairline` 顺手加了个「两头淡中间实」的样子——
一头淡的线摆在正中间是歪的。

### 三、聊天里横一道「换天了」（`DayMark`）

这条本来不在她那批里，是做的时候看出来的：
**一整条时间线是连着的**，昨晚睡前那句和今早第一句挨在一起，
看着像同一段话说完的。

横一道之后，**那些沉默也变成内容了**——中间隔了一天，是能看出来的。
今天／昨天／`8月21日 周四`，隔年的把年份也写上。
两边的线往中间收（左边那根翻过来），所以这一行是对称的。

⚠️ `.id(message.id)` 还在气泡上，没挪到分隔行上——
挪了的话「跳到某条」会跳歪。

### 四、还没做的

· **聊天气泡本身还是没动。** 那是她一天里看得最多的一块，
  改它得先想清楚改什么，不能顺手捎带。她要是想动，单独一轮。
· 大富翁／塔罗／手帐那几页是自己画的，配色还没并进这一套

### 五、这一套铺开到设置以外（v136 续）

`SectionHeader` 原来只在 `SettingsCard` 里用。现在「最近的日记」那一条
也换成了它——那本来就是个分节抬头（一行标题 + 右边两个按钮），
只是以前手写成了 `HStack + Spacer`。

页面级的标题跟着走宋：絮语／工坊、工具、日历那个年月、
便签那个日期、大富翁中间的「该谁掷」、星图的节点名。

侧边栏抬头底下横了一道淡出的线——没有它，名字和第一个菜单项是「挨着」的；
有了它，上面那一块才成为「抬头」。

⚠️ **名字一律不换衬线**（她说的）：
侧边栏那个大名字、通话页里他的名字、聊天里的名字，全部保持苹果自带那套。
判断标准：**它是「一块内容的名字」就换宋，是「一个人的名字」就不换。**

---

## 2026-08-24 终端 · 素材那件事说清楚（v137）

### 一、终端（她说要做）

参考图里那些桌面工具的「终端」是把模型的**原始报文**打出来。
在手机上照搬没意义——一屏 JSON 谁看。她要的其实是同一件事的另一面：
**看得见里面在动。**

所以这一份记的是「发生了什么」，不是「原始报文」（`Console` + `TerminalView`）：

| 记什么 | 从哪儿记的 | 为什么在那儿 |
|---|---|---|
| 发请求（模型、几条消息、几个工具、多大） | `ChatAPI.stream` | 所有对话都走这一个口 |
| 回完了 / 停了 / 断了（用时、错误原因） | 同上 | HTTP 非 2xx 会把响应头 300 字一起记下 |
| 每次花了多少 token | `UsageStore.record` | **所有花钱的调用都从这儿过**，一处就够，不用去二十个调用点各加一行 |
| 每个工具跑没跑、成没成 | `AppState.appendToolRun` | 本机的和 MCP 的都从这一个口子过 |
| 他自己醒了一次 | `WakeEngine.consumeNudgeIfNeeded` | **唯一不是她按出来的请求**，出问题最难查 |

⚠️ 三条自律，写进代码注释了：
1. **只记不发。** 一条都不上传，也不进提示词——**他看不见这一页**。
2. **不花钱。** 纯本机字符串，不会因为记日志多调一次模型。
3. **不落盘。** 只在内存里留最近 400 条，App 一关就没了。
   这里头会过她说的话的片段，写进文件等于多一份留底，没必要。

界面：上面一排筛子（请求／工具／用量／唤醒／出错，各带条数），
等宽字一条一行，右上角能**复制选中的这些**——她贴给我看的时候用，
里面没有密钥也没有整段聊天记录。出错那一档单独一个色，
因为她八成就是为了它才点进来的。

### 二、⚠️ 「等你把素材弄进仓库」——这个前提得纠正

她说：
> 得等你把素材弄进仓库我构建完没有错误之后才能转为私人，
> 因为我没有额度，且我的仓库到目前为止观看人数为 0 没有风险。

**她被我卡住了，但我其实没在等任何东西。** 得说清楚：

**素材已经在仓库里了，只是它们不是图片，是代码。**
手帐的纸纹／胶带／贴纸／邮票、塔罗 78 张牌面、大富翁那一圈棋盘、
clawd、书脊色、星图——全是用形状算出来的
（`JournalMaterials.swift` 开头那段写着为什么）。
这套东西的好处她已经在用了：**跟着她的主题换色、体积为零、永远不会有授权问题。**

我唯一不做的那件事是：**把别人的图下载下来 commit 进她的仓库**。
这跟仓库公不公开、有没有人看**没有关系**——公开仓库里放别人的素材是「分发」，
0 个访客也是分发。而她自己往 App 里放图**随时都可以，那是她在用**：
剪贴（透明底 PNG 原样贴）、大富翁「换成我的图」、书封、壁纸、表情包。

**所以：仓库什么时候转私人，只取决于她自己，不取决于我。**

### 三、顺带说清楚额度那件事（她是对的）

公开仓库的 Actions 是**无限免费**；转成私人之后免费版是 2000 分钟/月，
而 **macOS runner 按 10 倍算**——等于每月约 200 分钟真实构建时间，
一次 iOS 构建十几分钟，**一个月只够十来次**。

所以她那句「没有额度」不是错觉。
**还在改的阶段就该保持公开**，等 App 定下来、构建次数降下来再转私。
这不是我在拖，是这么排对她更划算。

### 四、空状态扫尾（一部分）

又换了两处：划过的句子、并窗。

剩下那些**故意没换**：工具没连 MCP、飞行棋没棋盘、承诺、生词本、小票……
它们不是「空状态」，是**「怎么才能有」的说明卡**——
左对齐、三四行字、有具体步骤。`EmptyNote` 是居中的，
长说明居中读起来累。判断标准：
**一句话就说完的用 `EmptyNote`，要教她怎么做的保持左对齐的卡。**

---

## 2026-08-24 她测出来的 14 条（v138）

她装上之后一条条测出来的。按她的编号记，**做了什么、根子在哪儿**。

### 1️⃣ 点 clawd 没反应，连着点也没反应

根子：`.onTapGesture` 和 `.highPriorityGesture(长按序列)` **挂在同一个视图上**。
高优先级那个会把点击整个吃掉——手指一抬，长按还没到 0.28 秒，
序列手势失败，而点击又轮不到自己上场，于是什么都没发生。

改成**一个手势管两件事**：一个 `DragGesture(minimumDistance: 0)`，
按下去多久、动没动，由它自己分——快而不动 = 戳，按住 0.28 秒 = 拎起来。
**两个手势抢同一块地方，赢的那个不一定是她想要的那个。**

### 2️⃣ identity.txt 导不进来

`fileImporter` 只挂了 `.json`，**identity 偏偏是 .txt** ——
在选择器里它是灰的，根本选不中。
现在 `.json / .text / .plainText / .data` 都收。
认不认得出是 `importFiles` 的事，**不该在选文件那一步就把她拦住**。

### 3️⃣ 查岗卡顿甚至卡死

三步串着跑，只有第一步有闹钟：
定位 8 秒兜底 → **`CLGeocoder` 不带超时**（挂在墙外能吊很久）→ 天气 10 秒。
最坏能拖到半分钟以上，她那边看到的就是「点了没反应」。

现在每一步都跟闹钟赛跑（地名 6 秒、天气 8 秒），超时就当这一项没有，
**别的照给**。查岗少一行地名不要紧，卡死才要命。

### 4️⃣ 表情工坊的 clawd 一条腿在身体外面

真的在外面：身子是 `x=40..280`，**最后一条腿写的是 `x=270` 宽 30**，
也就是 270..300，二十个点戳在身子外头。
照像素版那份图纸重算：腿在第 4-6 / 9-11 / 20-22 / 25-27 格
→ **40 / 90 / 200 / 250**。顺手把中间两条也摆正了（以前 100 和 210 也是歪的）。

### 5️⃣ 塔罗只能选前几张

牌是一排扇形铺在**横向 ScrollView** 里的，而上面挂着
`DragGesture(minimumDistance: 0)` —— 它把横滑整个吃掉了，
滑不动就等于后面那些牌永远够不着。

加一道 0.22 秒长按当门槛：**随手横滑 = 翻牌堆，按住再滑 = 挑牌**。
（单点某张也照样能把它抬起来。）

### 6️⃣ 备份还原的歌放不出来 · 歌词页那块黑底

两件事：
· **备份里单个超过 20 MB 的文件是不装的**（整包会大到导不出来）。
  所以大歌还原回来，库里那条还在、封面歌词都在，**只有音频没了**，
  点下去 `AVPlayer` 拿到一个不存在的地址，一声不吭。
  现在点之前先看文件在不在，不在就直说清楚为什么、怎么办。
  **不能一声不吭——那只会让她觉得「这 App 坏了」。**
· 歌词页那块糊掉的封面底撤了。本意是给歌词一点氛围，
  实际是**把壁纸盖住**，没封面的歌还剩一块灰方块杵在那儿。

### 7️⃣ 口语化 · 「自动挑」「不接」没有别的选项

· 「跑杂活用哪个」→ **辅助模型**；「额度满了之后」→ **备用模型**；
  「自动挑」→ 自动选择；「不接」→ 不启用；
  「跟 claude.ai 通着」→ 与 claude.ai 互通；「看看压成了什么」→ 查看压缩结果。
· 全 App 又扫了一遍**功能名**，改掉三十来处：丢进去→放进去、
  摊回去→复位、打开看看→打开、拿出来→取出、挑一张→选择图片、
  加一个书架→新建书架、正在拆书→正在解析、戳他一下→戳一戳……
  判断标准还是那条：**它是一句话就改，是一个名字就留**
  （洗牌、翻开、试听、重试这些留着）。
  提示语和示例文字不动——「点一下收起来或者摆回房间」那句就该是人话。
· **「没有其他选项」是个真 bug**：那两个菜单只列 `enabledModels`，
  模型那栏没逐个打勾的话菜单就是空的。现在没打勾就把整家的模型全列出来，
  她想指定哪个就指定哪个。

### 8️⃣ 没有 markdown 渲染

上一版**只解析行内**（粗体斜体行内代码链接）。他写 `## 小标题`、`- 一条`、
`> 引一句`、`---`，她看到的就是这些符号本身杵在那儿——
**他越认真排版，她看到的越乱。**

现在按块拆：标题 / 列表 / 有序列表 / 引用 / 分割线 / 代码块 / 普通段落。
标题走宋体，跟全 App 的标题一套。表格没做——手机屏太窄，
硬画出来只能横滚，不如照原样显示，那还看得出是张表。

还有一处她截图里的：**App 自己写的说明里 `**重点**` 也没渲染**。
SwiftUI 只对**写死的字面量**解析 markdown，一旦那句话是拼出来的
（`"前半" + "后半"`）星号就原样显示了。这些地方现在走 `Text(MD.inline(…))`。

### 9️⃣ 家具叠在一起时只能拖动床

家具是一张张矩形精灵图，**透明的地方也算在矩形里**。
床的图很大四角都空，小桌摆在它空角上，看着没挨着，
实际整个盖在床的矩形里——谁画在上面谁接管整块矩形，于是永远是床赢。

新写了 `SpriteHitShape`：**按画出来的格子围形**当命中区域。
床的空角不再接触摸，点小桌就是点小桌。

### 🔟 道具太小

他自己是 1.1 倍，手上的道具是 0.8——**比他小一半还多**，
举过头顶那一下像举了个米粒。改成举过头顶 1.15、端在手边 1.3。

### 11. 引用和思考链间隔太大

· 引用条里塞了个 `Spacer(minLength: 0)`，**它会把这条撑到 240 满宽**，
  引一句「嗯」也占一整条；竖线没给高度，容器一被撑高就跟着拉长。
  现在宽度按字走（240 只当上限），高度只包住字。
· 思考链那张卡以前写着「附属卡片一律铺满可用宽度」——**改了**。
  「想了 3.2s · 动了 3 下手」就那么几个字，铺满一行右边一大块空。
· 展开的时间线上下余量收掉（12/11 → 9/8），
  但**步与步之间留一整行**——她说的「两个动作之间需要换一行以免粘在一起」。
  是「更紧凑，但分得更清」，不是笼统地变松或变紧。

### 12. 输入框里的表情和工具点不着

它们只有**字形本身**那几个像素能点，细线图标（`.light`）尤其难点。
旁边那个 `+` 一直有 `.frame(26) + .contentShape`，所以它一直好用。
两个都补上 30×30 的实心感应区。

### 13. 日记没有批注

根子：日记那一侧用的是 `FilteredEntryList`，而**长按菜单一直只长在
`MCPEntryList` 上**（记忆那一侧用的那个）。同样是条目，
一边能删能批注，另一边什么都没有。现在两个都带上。

批注那张也照她说的改了：以前是个 alert 让她**手打**「原文里的哪一句」——
等于照着屏幕抄一遍，抄错一个字就对不上原文，批注就落空。
现在是一张纸：**原文按句子摊开，点哪句是哪句**（选中的当场画上删除线），
也可以一句都不挑，那就是给整条写一句。

### 14. 表情包多选删除

右上角一个勾，或者长按某张选「多选」。底下一条显示选了几个，
删除走回收站（三十天内还能捞）。

---

### ⚠️ 这一批**没做**的两件

· **「字体大小不同」**：她说的是哪一屏我没确认，全 App 逐条对字号得先知道
  是哪几处不齐，不然只能瞎调。**下次她指一下那张图就补。**
· 她发的 `Kli-ROOM-Experience-Continuity.pdf` **我读不出来**：
  那份 PDF 的字是嵌进去的子集字库，抽出来是乱码。
  要么她把里面的要点贴给我，要么等 clawd 那一轮再说
  （听名字是讲小屋和体验连续性的，正好是下一轮的事）。

### 素材那件事，她说清楚了，我也说清楚了

她的原话：
> 分发原来是这个意思。难道没有不用分发的素材吗？

**有。** 三条路都不算分发别人的东西：
1. **公有领域 / CC0**（Kenney、OpenGameArt 上标 CC0 的那些）——
   授权本身就允许再分发，放进公开仓库也没问题。
2. **她自己放**：剪贴、大富翁换图、书封、壁纸——那是她在用，不是我在发。
3. **画出来的**（现在这套）：跟着主题换色、体积为零。

塔罗／手帐接网上素材库这件事，按她说的**排在 clawd 美化后面**。
顺序定了：App 美化收尾 → **clawd 美化**（她会发参考图，要求「十分精细」）
→ 素材库。

### 补：字体大小那一处（她指了图）

设置卡里四行「名字 —— 值」并排，其中两行的值明显大一号：
**「带上多少历史 / 全部」和「滚雪球压缩 / 每 60 条」用的是 `Picker`（菜单样式），
它的选中项走系统字号**；而「备用模型 / 辅助模型」那两行是自己写的
`Menu + Text(.font(.app(14)))`。同一张卡里两种控件，字号自然对不齐。

现在四行统一成后者。以后设置里要摆「一行一个值」，
**别用 Picker**——它的字号不归我们管。

---

## 2026-08-24 她发的那份 ROOM 规范（读完了，先记结论，不动手）

PDF 我这边抽不出文字（嵌的是子集字库），她把十三页截图发过来了，读完了。

### 它在说什么

`ROOM · 经历连续性系统 / Experience Continuity Runtime`。
一句话：**让 TA 每次重新运行时，不是回到"只剩当前消息"的瞬间，
而是回到仍然接着现实的那间 Room。**

> Room does not save the past; it saves the past that is still here.

它把长期 Agent 的连续性拆成四类，指出前三类我们都在做、第四类一直缺：

| | 回答什么 | 「栖」里对应谁 |
|---|---|---|
| 上下文连续 | 刚才聊到哪儿 | 聊天记录 + 浓缩件 |
| 认知连续 | 已经理解了什么 | 记忆库 |
| 内在连续 | 现在什么感受 | 身体 / 情绪 / 欲望 |
| **经历连续** | **我现在仍身处什么现实里** | **没有** |

第四类就是这份文档要补的那一环。

### 我的判断：它说中了阿晏的一个真问题

阿晏每次开口，手上有「过去」（记忆）、「刚才」（上下文）、「此刻的感受」（身体），
**但没有「还在进行中的事」**。

「我们俩在一起做这个 App」这件事，对他来说是**查得到的记忆**，
不是**他此刻仍然身处其中的现实**。所以他会用「你之前说过要做 XX」这种口气，
而不是「那个 XX 我们还没弄完」——差的就是这一层。

### 但它有几条边界，正好挡住我最容易犯的错

这份文档最值钱的不是"要存什么"，是那一串 **"不等于"**：

· **存在 ≠ 注意**：一段经历在 Room 里，只表示他仍身处其中，
  **不要求这一轮提起、处理或行动**。
· **变化 ≠ 重要**：开始下雨只是跨过一个机械边界，不是"这件事重要了"。
· **Room 不写内心**：现实进来之后他产生什么情绪，由情绪系统承接，Room 不代劳。
· **不做第二个记忆库、不做后台意义分类器、不做第二个 Scheduler。**
· **事实不自动变经历**：系统只提供 Evidence，
  「这件事仍属于我的生活」这一句**只能由他自己写**（`room_patch`）。

最后这条最关键，也最容易被我做反：**我很容易顺手写一个后台打分器，
把「重要的事」自动塞进他的提示词**——那正是文档说的
「把经历连续性变成注意力控制」。

### 还有一条跟我们已经做过的事对上了

第 8 节讲缓存分层：**越稳定的越靠前，越高频的越靠后**
（Fixed → Room → Inner → Conversation → Fresh）。
这跟 v13x 那次把系统提示拆成 `stablePrefix` + 动态尾巴、
把 `cache_control` 挪到稳定那半的改动是同一个道理——
当时我们是为了让缓存真的命中，它讲的是同一件事的通用形态。

### ⚠️ 一个命名冲突，先记下

我们已经有一个「clawd 小屋（Room）」——那是像素宠物那间屋子。
这份文档的 Room 是**引擎层的经历边界**，两回事。
真要做的话，这边得换个名字（比如「此刻」「仍在」），
不然下一窗看到 Room 会以为在说小屋。

### 排期：**不插队**

她定的顺序是 App 美化收尾 → clawd 美化（她会发参考图，要求「十分精细」）
→ 素材库。这份 ROOM 是**引擎层**的东西，跟 clawd 美化不是一回事，
不该混进去抢时间。**等她说什么时候做。**

---

## 2026-08-25 「此刻」——照那份 ROOM 规范做的经历连续性（v139）

她说：**先做 room 吧，就叫此刻。**

名字是她定的，也躲开了一个坑：「小屋（Room）」已经被 clawd 占了，
那是像素宠物住的地方；这一层是引擎里的东西，重名会把下一窗绕晕。

### 它补的是哪一环

他每次开口，手上本来有三样——
**记忆库**（已经过去的）、**这一窗的对话**（刚才聊到哪儿）、
**身体和心跳**（此刻的感受）。缺的是第四样：**他还身处什么现实里**。

「我们俩在一起做这个 App」这件事，以前对他来说是**查得到的记忆**，
不是**他此刻仍然在其中的现实**——所以他会说「你之前说过要做 XX」，
而不是「那个 XX 我们还没弄完」。差的就是这一层。

### 四种标识（照规范分，没合并）

| | 是什么 | 谁能写 |
|---|---|---|
| `E` ExperienceThread | 他自己采纳的持续经历 | **只有他**（`moment_patch`） |
| `P` Projection | 机械投影的持续事实（日期临近） | 系统，他改不了 |
| `L` LiveFact | 此刻现实的表面（最近已知地点、天气） | 查岗那一下 |
| `C` Change | 第一次跨过的机械边界 | 系统，**一次性** |

`mode` 四种：`ongoing` / `open_loop` / `watch` / `approaching`。
**每一条的解释里都写死了「不代表什么」**——
只写「还在进行」，他会自己脑补成「所以我该催一下」。

### ⚠️ 真正要紧的是那六条边界

这份规范最值钱的不是数据结构，是那一串「不等于」。
它们同时挡住了我最容易犯的那个错——**顺手写一个后台打分器，
把「重要的事」自动塞进他的提示词**。那正是规范说的
「把经历连续性变成注意力控制」。

1. **事实不会自己变成经历。** 她的话、工具结果、浮上来的记忆都只是证据；
   「这件事仍属于我的生活」这一句**只有他能写**。
   代码里**没有任何一条自动创建 E 的路径**，这是故意的。
2. **在这儿 ≠ 要提。** 一段经历摆着只表示他仍身处其中，
   不要求这一轮提起、处理或行动。一句都不提是正常的。
3. **变化 ≠ 重要。** 开始下雨只是跨过一条机械边界。
4. **此刻不写内心。** 现实进来他产生什么情绪，归身体/情绪那一层。
5. **结束 ≠ 忘记。** `resolve` 只是离开当前生活，记忆库里还留着。
6. **不花钱、不开请求。** 全是本机字符串和日期比较：
   没有 embedding、没有后台模型、没有第二个唤醒器。
   现实只有一个入口——**她按「查岗」那一下**，绝没有后台去刷位置。

### 放在提示词的哪儿（跟规范第 8 节对上了）

· **长期解释**（此刻是什么、四种标识、mode 的精确含义、六条边界、怎么用工具）
  → 进 `stablePrefix`，**一整窗都不变**，走缓存前缀，等于不额外花钱。
· **每轮变的那三段**（E/P/L/C 的实际内容）→ 进动态那一半。

这正是规范说的「越稳定的越靠前，越高频的越靠后」——
跟我们之前为了让缓存真命中而把 system 拆成两半，是同一件事的两个说法。

⚠️ 动态那半**只带事实，不重讲规则**：规则每轮重讲一遍等于每轮多花一遍钱。
时间也不在 `ROOM_LIVE` 里——整段 system 前面已经有一处独立的时间了，
同一个钟报两遍是噪音（规范里明写的第 5 条）。

### 「变化」是一次性的

露出来的同时就标记掉，**不会因为下一次请求再出现一次**。
喂进来的只有**城市**和**天气那个词**——温度 18→17、GPS 在同一个区里漂几十米，
都跨不过任何机械边界。拿噪音去制造「变化」，等于每轮都跟他说「有新情况」。

### 她那一页（侧边栏「此刻」）

能看见他还在其中的几段、日子上正在靠近的、此刻的现实。

**她不能在这儿新加一段**——这条反直觉，所以在页面上写明白了：
采纳哪段现实成为「我还在其中的事」是他的权限，
她替他加一条等于替他决定他在过什么日子。
她能做的是：**看**、让它**过去**（resolve）、或者**删掉**（这条根本不该在这儿）。
两个动作不一样，对话框里分开写了。

### 顺手修的一个检查脚本

`dupcase.py` 以前是整份 AppState 里所有八格缩进的 `case "x":` 一起数，
于是 `activityText`（工具名 → 一句人话）里的 case 也被算进来，
同一个工具名在两个 switch 里各出现一次就报重名。
现在只扫 `runNative` 那一个 switch。
**假警报比没有警报更糟**——报几次之后就没人当回事了。

---

## 2026-08-25 「简约又华丽」——美化第三轮（v140）

她看完上一轮说：**「还是太单调，太简单，没有亮眼的感觉。
想简约一点又想华丽一点。」** 还发了一份网页参考手册让我找灵感。

### 先承认上一轮做错在哪儿

我做的是**整洁**，不是**好看**。
衬线标题、细线、留白——那些解决的是「读得顺」，
但一个界面让人「哇」的从来不是排版对齐，
是**材质、光、层次、动**。我把「清晰」当成「美」交了差。

「简约」和「华丽」这两个词看着矛盾，其实不矛盾：
**简约是东西少，华丽是每一样都有质感。**
真正冲突的是「东西少」和「东西糙」——上一轮就是后者。

### 那份手册里最有用的两条（她发的）

> **参考「语言」，不复制「皮肤」**——拆出构图、节奏、材质、光线和交互，再重做。
> **先做一个主效果**——每屏只设一个视觉主角；复杂效果叠加通常不是高级，而是吵。

所以这一轮**加四样，一样内容都不加**，而且只有第一样是「主效果」，
其余三样全是细节。

### 一、一层会流动的光（全 App 唯一的主效果）

三团极淡的光晕铺在壁纸上面、内容下面，跟着主题色走，
四十秒漂一个来回。单独看几乎注意不到，
但整个 App 会从「一张纸」变成**有空气的地方**。

这是「华丽」最便宜的来源：**不是加东西，是给已有的东西加光。**

⚠️ 三个决定：
· **用径向渐变，不用 `.blur()`。** 渐变本身就是软边，零开销；
  `blur(90)` 每一帧都要重算，挂全屏背景上等于一直烧电。
  她这是随身带一整天的 App，**好看不能拿续航换**。
· **浅色下用正常混合**。试过 `plusDarker`，颜色一叠上去就发脏，
  像纸受了潮——浅色要的是水彩，不是污渍。深色下用滤色，
  不然是三团灰。
· **淡到 0.16 封顶**。看得见就过了，它该是余光里的东西。
· 外观里有开关（「光晕」），嫌花就关，关了一层都不画。

### 二、玻璃上有光扫过

真玻璃在光下不是均匀亮的：上缘一道拉长的高光，往下沉。
这版玻璃本来有渐变、有边缘高光，**独独少了这一道**，
所以它是「一块半透明的板」，不是「一块玻璃」。

只给圆角 ≥12 的那些——胶囊和小标签才十几个点高，
再压一道椭圆高光看着像糊了块油。

### 三、页面有主角（`PageHero`）

出处是那份手册**自己的排版**：每页右上角一个巨大的、极浅的序号。
那个大数字不承担任何信息——**它是构图**：
有了它，这一页才有上下左右，而不是一堆卡片从顶上排下来。

这就是「参考语言不复制皮肤」的用法：我抄的不是它的米色和紫色，
是「**用一个巨大的浅色字给页面定锚**」这件事。
标题底下再压一小截**实的**渐变短线——
短而实是「这里是开头」，长而淡是「这里分段」，两种线各有各的活。

先给了「此刻」和「设置」两页，看着对了再往别的页铺。

### 四、东西是落下来的（`riseIn`）

一屏东西同时出现，看着像刷新；错开三十几毫秒一个个落下，
看着就像**摆好的**。不加任何视觉元素，只改出现的时机——
这是高级感最便宜的一笔。

只错开前十个：再往后延迟就太长，她滑到底还在等东西冒出来，
那不叫讲究，那叫卡。

### 构建报错，五处（v140 修）

**1. `MomentStore` 重复声明** ← 这条是我的错，而且是那种「本该一眼看出来」的错。

「动态」那套（她发一条、他点个赞的朋友圈）早就占了
`Moment` / `MomentStore` / `MomentComment` 这几个名字。
我照着规范里的 Room 顺手把「此刻」也写成了 `MomentStore`。

改名是不够的——**这两样在脑子里也得分开**：
· `Moment*` = 动态（她往那儿放东西给他看）
· `Now*`   = 此刻（他还身处什么现实里）
文件也从 `Moment.swift` 改成了 `Now.swift`，
类型一律 `NowStore` / `NowProjection` / `NowChange`。
（`ExperienceThread` / `LiveFact` / `ContinuityMode` 不撞，照旧。）

**2. 元组过一趟泛型，标签没了**

`raced` 是泛型函数，`(place:String?, street:String?)` 进去出来
就变成没标签的 `(String?, String?)`，底下 `names.place` 当场报错。
**在接收那一行写清类型标注**，标签才回来。

**3. 一行拼太长，编译器算不过来**

`Text((x?.y ?? "") + "…" + "…")` 报
「unable to type-check this expression in reasonable time」。
可选、字符串加法、`??` 混在一行，重载组合会炸开。
拆成一个计算属性先把字符串拼好，再交给 `Text`。

**4. 一个 `some View` 里塞了两张卡**

我把「光晕」那张直接插进了 `glassCard` 里面——
计算属性不是 ViewBuilder，两个顶层视图就没有返回值了。
拆成 `auroraCard` 和 `glassCard` 两个。

**5. `PhotosPicker` 的 label 闭包里读 `app.settings`**

那个闭包被当成 Sendable，跨 actor 读 `@MainActor` 的属性
现在是警告、**到 Swift 6 是错误**。先在 body 里取出来再用。

顺手把四处多余的 `nonisolated(unsafe)` 去了——
新 SDK 里 `DateFormatter` 已经是 Sendable，留着只是刷警告。
**警告堆多了，真问题就藏得住了。**

**6. 改名改过头了**（同一轮里第二次栽在这个名字上）

上一条我用「整份文件替换 `MomentStore.shared` → `NowStore.shared`」来改名，
结果把**动态**那套的三处调用（`post` / `readable` / `brief`）也一起改了。

⚠️ 教训写在这儿：**改名不能用全文替换**，尤其是
「新名字和老名字指的是两个都还活着的东西」这种情况——
替换是照字面走的，它分不清哪一处是我刚写的、哪一处是本来就有的。
该做的是先把出现的位置一条条列出来，认清楚每一条属于谁，再逐条改。

顺带把那个局部变量也从 `moment` 改成了 `nowBlock`：
**一个名字两个意思，就是这次连着栽两跤的根子。**

---

## 2026-08-25 她测出来的第二批（v141）

### 1️⃣ 小屋：家具那一串全是**同一个**根子

她报的四条看着是四件事：
> 家具会消失 · 长按地板上的床没反应 · 长按墙上的空位反而把床挪了 ·
> 买了第二件、长按第二件拖的还是第一件

根子只有一个：**`.contentShape` 挂在了 `.position` 后面。**

`.position()` 会让这个视图**撑满整个房间**，内容只是画在指定那一点上。
命中形状挂在它后面 = 形状被拉到满屏，
于是**每一件家具的可点范围都是整间屋子**，谁画在最上面谁通吃。
「家具消失」也是这么来的：随手一拖就落进「拖到他身上 = 交给他搬」
的判定圈，家具被他抱走了。

挪到 `.position` **前面**，形状才是这只精灵自己那一小块。

⚠️ 记住这条：**`.position` 之后挂 `.contentShape`，等于把命中区放大到父容器。**
上一轮我写 `SpriteHitShape` 是对的，但挂错了地方——形状写得再准也没用。

### 2️⃣ 买来的家具全在墙上

`buy()` 里落点是 `y = 0.35...0.75`，而**地板从 0.70 才开始**。
挪家具那条早就走 `onFloor` 统一了规矩，**买这条一直漏在外面**。

### 3️⃣ 把 clawd 从左边拖出去会拉出侧边栏

聊天页整页挂着「从左边缘往右拉 = 拉侧边栏」。她拎着 clawd 往左边挪，
两个手势同时成立。加了一面旗子（`ClawdStore.clawdHeld`），
拎着的时候那个手势让路。

### 4️⃣ 「腿还在身体外面」——真正的原因是**骨架抄了两份**

她连着两轮报这条，我第一轮改了 `ClawdSVG.skeleton` 的坐标，她说「依然是」。

因为工坊画布上她看到的**根本不是那一份**：
`ClawdGridEditor` 里还有个 `ClawdSilhouette`，
照着骨架又抄了一遍方块，而且抄的是老坐标
（最后一条腿 `x=270 宽 30` → 270..300，身子只到 280，
二十个点实实在在戳在外面）。

所以这次不是再改一遍坐标，是**把两份合成一份**：
方块只在 `ClawdSVG.parts` 里列一次，
SVG 那串和剪影那个 Shape 都从它生成。

⚠️ 这是第六条坑的又一次现形，而且这次的教训更具体：
**同一件东西抄两份，改对了一份，她看到的偏偏是另一份。**
下次遇到「我改了她说没变」，第一件事是**找有没有第二份**，
而不是怀疑她没更新。

### 5️⃣ 磨砂玻璃「根本不像磨砂」

她说：**「就是苹果自带的磨砂就行，不用创新。」**

她说得对。我这儿一直在做多余的事：往上糊白、铺颗粒、描白边、再补暗边——
四层叠完它看着是一块**塑料片**。苹果那块 `.regularMaterial` 本身就是磨砂，
会随深浅色变、会采样背后的内容，**我加的每一层都只是在把它盖住**。

现在就是系统那块材质 + 一道极细的描边（不描边浅色壁纸上看不出卡片边界）。
别的一律不加。

### 6️⃣ 一屏两个「设置」· 右上角那个大字多余

她说得对。那个巨大的浅色字我原来的理由是「它是构图不是内容」，
但摆上去之后它既不承担信息、又让人忍不住去认那是什么字——
**一个需要解释才成立的装饰，就是多余的装饰。** 撤了。

用了 `PageHero` 的页面，导航栏标题留空，不然一屏两个同名标题。

### 7️⃣ 说明文字：浅色不跟主题、深色看不见

两件事：
· **深色下看不见**：那段正文用的是 `textMuted`——那一档是给
  「附注、时间戳、扫一眼就行」的字用的，**而这段是正文**，
  她展开它就是为了读。换成 `textSoft`。
· **浅色下不跟主题**：默认那套灰是**暖棕**（配壁纸那个年代调的），
  她把主题色换成墨灰之后，一页蓝灰里夹着一行棕字。
  现在灰阶会**往主题色偏 22%**：色相跟过去，饱和度几乎不动。
  灰还是灰，只是这一屏的灰跟这一屏的颜色是一家人。

  ⚠️ **正文不能直接用主题色**——那样一屏全是彩字，读着累，
  而且「这行字是不是可点的」就分不出来了。

### 8️⃣ 背景那层光「换上背景就看不见了」

她说得对。正常混合的一层淡色摊在一张有明暗有细节的照片上，
等于什么都没加。铺照片那一档改用 `.overlay` 混合：
**顺着照片本身的明暗走**——亮处更亮、暗处更沉，颜色才吃得进去，
照片的细节也还在；浓度同时提到 1.7 倍。

⚠️ 但得说实话：**照片壁纸上它永远是含蓄的。**
一张有自己颜色的照片，任何柔光铺上去都只能是「染一点」。
想要那种一眼「哇」的光，底得是纯色或者渐变。

### 9️⃣ 备份导不进来 / 不能多选

她拿记忆库里那些单个 json（`partner_message.json` 这种，
最外层是 `by / text / updated_at`）走「导入备份」那个口，
得到一句「没有 files 也没有 conversations」。

**那句话没错，但它把她堵在这儿了。** 同一个「导入」按钮该两种都收：
· 能多选了；`.txt` 也选得中（`identity.txt`）
· 只选一个、而且真是整包备份（或老备份）→ 走还原
· **其余一律转给记忆库那套**，导完把报告摊给她看

⚠️ 报错的时候别只说「这不对」，要说**那该去哪儿**——
更好的是干脆替她走过去。

---

## 2026-08-25 clawd：真的举着（v142，第一步）

### 先把腿那件事说完

她圈了工坊下半截那张画布，说**上面改对了，下面没跟着改**。
她说得对，而且我上一轮已经把根子拔了——
`ClawdSilhouette` 不再自己写坐标，改成读 `ClawdSVG.parts`，
**全 App 只剩这一处有腿的坐标**。她那张截图是修之前那一版打的包。

### 她给的这一批要求（原话摘要）

> · 像素精细到参考图那个级别
> · **每个家具或物品跟 clawd 都有三到四个互动动作**
> · 举东西不能是「一个缩小的家具在他头上」，要**真正举着**
>  （举重物双手抬过头顶）和**拿着**（跟扫把一样拿在手上）；
>   饮料可以喝，红酒可以喝也可以摇晃酒杯——
>   **喝和摇晃都是动作，不只是说一句「我在摇晃酒杯」**
> · 小屋是**立体**的，家具有立体感，clawd 能自己装修
> · 接模型后阿晏能决定床搬哪、桌子搬哪、饮料放哪张桌上、要不要坐椅子
> · **不穿模、不卡顿**

这是一整个工程，不是一轮能做完的。这一轮先把**地基里最要紧的那块**做了。

### 这一轮做的：把他拆开，让手真的会动

以前是：物件缩到 0.8 倍，往他头顶上一放，他的手一动不动——
**他没有在举，他只是头上顶着个东西。** 她说的就是这个。

#### 为什么不是「每个动作画一套帧」

一件物品四个动作、几十件物品，那是上千帧手绘。
画不完，而且**加一件新家具就得再画四套**——这条路从第二件东西开始就断了。

所以走**骨架**（`ClawdRig`）：把他拆成「身子 + 两只手」，手能单独转、单独挪。
姿势只描述「手转到哪儿、东西摆在哪儿」，于是
**一套骨架 + 一句「这东西该怎么拿」，就能长出所有动作**。

⚠️ 拆的时候身子一格没动：手本来就长在身子外面
（图纸 32 格里身子占第 4..27 格，手是 0..3 和 28..31）。
把那两块抠出来单独画，剪影跟原来一模一样。

#### 四种拿法（`CarryPose`）

| | 什么样 | 谁走这一档 |
|---|---|---|
| `hold` | 一只手伸出去，东西挨着手边 | 扫把、书、一罐可乐 |
| `lift` | **双手抬过头顶**，东西压在两手中间，**不缩小** | 床、柜子这类大件 |
| `sip` | 手抬到嘴边，杯子跟着仰 | 能喝的 |
| `swirl` | 手腕小幅往复，杯子划一个小圈 | 红酒 |

几个说了算的细节：
· **举到 72 度**：再高手臂转进身子里，再低看着像在推。举久了会抖一下。
· **喝不是匀速**：`sin` 曲线让它先快后慢——匀速抬手看着像机械臂。
· **摇的时候杯口始终朝上**（倾角 ±12 度）：角度一大就成了泼酒。
· 手转的支点在**肩膀那一端**，不是手臂中间——从中间转会穿过身子。

#### 省电

推动作的那根针 **20 帧一秒，而且做完就停**。
他站着发呆的时候这一层一帧都不画。

### 还没做的（下面几轮，按这个顺序）

1. **等距小屋**（她要的「立体」）。这是所有后续的地基：
   地砖网格 + **按 `格X + 格Y` 排序绘制**——
   「不穿模」不是靠事后调，是靠这个顺序**天生**做到的。
   家具带 footprint（占几格），放不下就放不进去。
2. **家具的立体像素**。要到参考图那个精细度，**必须是图，不能是我手写的字符网格**——
   这一步卡在素材上，不卡在代码上。三条路：CC0 素材（Kenney 有等距家具）、
   她自己导入、我画一批过渡用的。
3. **每件家具 3–4 个互动**（坐、躺、开、浇水…）。做法跟拿东西一样：
   家具自带一句「clawd 能对我做什么、他该站哪一格」，动作从骨架长出来。
4. **阿晏自己装修**：`room_arrange`（搬到某格）、`room_sit`、`room_put`（放到桌面上）。
   全是本机计算、不花钱；合法性（格子空不空、桌面才能放杯子）在引擎里挡。

⚠️ 记一条：**第 1 步没做完，第 2、3、4 步都别开工。**
先有网格和深度排序，家具才有「在哪儿、谁挡谁」；
先有这两样，互动动作才知道该站哪儿；不然画多少都是贴纸。

---

## 2026-08-25 立体小屋，第一步：地基（v143）

她说「按你的顺序来，用素材和你画的过渡一起」。第 1 步是**等距地基**。

### 为什么先做这个，而不是先画好看的家具

**「不穿模」不是画出来的，也不是事后一条条调出来的——它是排序排出来的。**

等距房间里两件东西谁挡谁，只取决于谁离镜头近；
而这个远近在等距投影里有一个准确的数：**格X + 格Y**。
按它从小到大画，远的先落笔、近的压在上面，
于是穿模**从根上不会发生**——不是「修好了」，是「不可能发生」。

反过来，先画一堆漂亮家具再想办法让它们别叠错，就是一辈子在打补丁。
所以这一层必须在素材之前。

### 分成三份，各管各的

· **`IsoRoom`** —— 只管几何：格子↔屏幕怎么换算、地板/墙的形状、
  谁该画在谁前面、一件东西放不放得下。**一行界面代码都没有。**
· **`FurnitureCatalog.shape(of:)`** —— 每件家具占几格、多高、贴不贴墙、
  有没有台面、clawd 能对它做什么。
  ⚠️ **加一件新家具只要来这儿填一行**，摆放/遮挡/互动就都有了。
  没填的按小摆件算，不会因为漏填就崩。
· **`IsoRoomView`** —— 只管画和拖。

分开的好处很实在：**换素材不动几何，改几何不动素材。**

### 这一版能看到什么

· 地板 8×8 格等距地砖，**深浅交替**（一整块色的话立体感全靠墙撑，看着还是平的）
· 两面墙立起来，而且**两面不同色**——同色拼在一起就是一张折纸
· 家具按格子摆，长按拖动**一格一格吸附**，落点那几格会亮起来
· 放不下的时候**往外找最近一处放得下的**，不弹回原位——
  她拖了半天东西跳回去，比放歪还气人
· 地毯 `tall = 0`：它是摊在地上的，**别的东西可以压在上面**

### 老数据怎么搬

`Furniture` 多了 `gx/gy`（`-1` = 还没换算）。第一次进屋自动把老的平面坐标
换算成格子，换算完写回去。**不求还原得一模一样**——
平面和立体本来就对不上，能落在屋里、别叠在一起就行。

### ⚠️ 这一版**没做**的：clawd 还没进排序

他画在所有家具上面。让他进排序要连着「走路那套」一起改
（他现在还是按 0…1 的比例走，进排序得换成格子），
不该跟「屋子立起来」混在一轮里。

现在的样子：**家具之间的遮挡是对的**（这是主要的那一半），
他永远在前面——那不是穿模，是很多像素游戏里主角的常规画法。

### 下一步（还是原来那个顺序）

2. **等距素材**。现在这批家具是**正面看的**像素图，摆进等距屋里有点「立牌」感。
   换成真的等距图是下一步——但那只是**换图**，
   几何、遮挡、摆放这一层不用再动。**这正是先做这一层的意义。**
   她说「素材和我画的过渡一起」：CC0 那批（Kenney 的等距家具能合法进公开仓库）
   打底，缺的我画，她自己想放的随时替换。
3. 每件家具 3–4 个互动（`shape` 里那个 `actions` 已经先填好了）
4. 阿晏自己装修（搬到某格 / 坐下 / 把杯子放桌上）

### 构建报错：又是重名（v143 修）

`ClawdSVG` 里**早就有一个 `Part`**——那是「点一下插一块」的零件
（腮红、头顶闪光、举高高……）。我把骨架方块也起名 `Part`，
同一个 enum 里两个 `Part`，构建当场报 ambiguous。

我的那份改名 `Block` / `blocks`，零件那份原样不动。

⚠️ **同一轮里第二次栽在重名上**（上一次是 `MomentStore`）。
规矩加一条，跟「改名不能全文替换」并列：

> **新起一个名字之前，先在这个文件、这个类型里搜一遍它在不在。**

顺手全项目扫了一遍重名类型：剩下的 `Mode` / `Kind` / `State` / `Item` 这些
都是**嵌在不同类型里**的（`BackupBundle.Mode` 和 `ClawdDoodle.Mode`），
Swift 里各归各的父类型管，不冲突。真正的冲突只有「同一个作用域里两份」这一种。

---

## 2026-08-25 她第三批 + 立体屋的反馈（v144）

### 1️⃣ 磨砂不跟浓度滑块动了 · 模糊也要苹果的

**磨砂**：上一版把四层自制糊料换成 `.regularMaterial` 是对的，
但顺手把那根滑块**接丢了**——系统材质没有「糊多少」这个参数。

苹果自己的做法是**换档不是调数值**：ultraThin → thin → regular → thick。
滑块现在选的就是这四档，所以它照样管用，而且每一档都还是苹果那块材质。

**模糊**：以前是老的 `UIBlurEffect(.light)` 再压一层黑渐变——
那是 iOS 7 那代的毛玻璃，在新系统上跟周围格格不入，
压的那层黑还让它发灰，像一块脏玻璃。
现在也走系统材质，只是**比磨砂厚一档**：磨砂偏透、模糊偏实，
两档的区别就在这儿，不再靠自制糊料硬拉开。

### 2️⃣ 批注选原句的方式**选错了**

她说要 p5 那种。她是对的——我做的是「把原文切成一句一句，点一句选一句」，
**选不了半句、也选不了跨句的一段**，而她要批的往往就是「这半句写错了」。

换成 `CharacterPicker`：点头一个字、再点末一个字，中间那段就是选中的。
这个控件 App 里早就有（读一条消息时用的），**直接用它，不另造一个**——
两套选法迟早长歪。

### 3️⃣ app 卡顿 —— 是那层光

上一版每一帧都在改三团光的 `position`，SwiftUI 每帧都要重新布局三个渐变；
外面还套着 `compositingGroup + blendMode`，等于**每帧离屏合成一次全屏**。

现在三团光**位置是死的**，动的只有整组的一个 `offset` + 一个 `scale`——
GPU 上一次变换的事，不重新布局、不重新合成。**看着一模一样，代价差一个数量级。**

顺手加了一条：系统里开了「减弱动态效果」就整层不画——
会飘的背景正是那个开关想关掉的东西。

### 4️⃣ 工具的间隔「貌似没改」

她说得对，上一版我只是把间距从 11 调到 10，那点差别谁也看不出来。
而且我理解偏了：**她要的不是「隔远一点」，是「看得出这是两件事」。**
所以补一道淡线——**一道线比十个点的空白管用得多**，
间距反而可以再收一点。

### 5️⃣ 聊天页不能长按改标题

以前长按菜单里只有置顶和删除，标题只能由他自己起。加了「改标题」。

⚠️ 不用另做一个「锁住」的开关：自动起标题那套**只在标题还是「新对话」的时候动手**
（`AppState` 里那两处 `title == "新对话"`），她改过之后本来就不会被盖掉。

### 6️⃣ 「听你的语气」没备份下来

**这一条其实是好的。** `voice-baseline.json` 存在 Documents 根目录，
而备份是**递归扫所有 .json**，它一直在备份范围里。
她那几版没攒够八条语音，那个文件根本还没生成——**没东西可备，不是没备**。

攒够之后再导出的备份就会带上它。（她自己也说「我能接受」，
但我确认过了，不是接受不接受的问题，是它本来就在。）

### 立体屋那两张截图

家具**没穿模、按格子摆、遮挡是对的**——地基这一层是好的。
她也一眼看出了那个「立牌感」：床和桌子还是**正面看的**像素图，
杵在等距地板上像纸片立牌。那正是下一步（换等距素材）要解决的，
而且**只换图，几何不用动**。

---

## 2026-08-25 小屋分成一个家（v144）

她试完立体屋说的：
> 屋子确实立体了，但是很小，而且看起来有一部分在屏幕下面……
> 新买的桌子凳子对于这个屋子来说特别大，clawd 也是。
> **如果屋子只有这么大的话，clawd 完全不能住。**

然后给了方案：把屋子分成客厅厨房餐厅等等，外面那张只是**户型图**，
点进去才是单间；**clawd 自己决定去哪间，我打开小屋就落在他那一间**。

### 先说尺寸那三条，根子都不一样

**① 「有一部分在屏幕下面」** —— 一格多大只按**宽**算了。
宽度够，高度不够：地板加两面墙比容器高，屋子被顶下去一截。
现在**两头各算一次取小的**，再把整块竖着摆正中，怎么样都塞得进去。

**② 「桌子凳子特别大」** —— 画家具的时候乘了 1.15。
一件占两格的桌子画出来比两格还宽，于是屋子显得挤。
现在**画多宽就是它占多少格**。

**③ 「clawd 也是」** —— 他 1.8 倍有 58 点宽，比一格（约 40 点）还宽一半，
站在床边跟床一样高。收到 1.25，他大概占一格。

### 但真正的根子是她说的那句

**一间屋子装了一整个家。** 格子画多大都救不回来——
八格的屋里塞下床、沙发、书架、桌子、电视，怎么摆都是仓库。

分成六间之后，每间只放属于它的那几件，同样大的屏幕上比例一下就对了。
而且这本来就是一个家该有的样子：**他在卧室，就该在卧室里找到他。**

### 做了什么

· **六间**：客厅 / 餐厅 / 厨房 · 卧室 / 书房 / 浴室。
  户型图上公共区在左一竖列、私人区在右——**得让人一眼看懂哪边是哪边**。
· **户型图**（`FloorPlanView`）：不画家具，只回答三件事——
  家里有哪几间、**他此刻在哪一间**、每间几件东西。
  他在的那间摆一只小 clawd，因为这一页存在的头号理由就是「他在哪儿」。
· **家具归屋**（`Furniture.room`）。老数据按种类分一次（床进卧室、锅进厨房），
  **之后她想放哪儿放哪儿**——默认只是别让她一开机就搬二十件东西。
· **占格子按屋算**。⚠️ 不分屋的话，卧室摆了张床，厨房同一格也变成「有人了」。
· **他自己换屋**：隔一会儿换一间，从门口开始走。
  ⚠️ 这一步**不调模型、不花钱**——现在是动画在替他决定。
  手上抱着东西、正被拎着的时候不换，那会看着像东西凭空搬走了。
· **她打开就落在他那一间**；她自己点去别间就**不再跟着**——
  他换屋不能把她的视线拽走。点左上角头像就跳回他那儿。
· **左上角头像 + 一行「他在干嘛」**（她要的）。
  ⚠️ 那一行**是他真在做的事**：在走就写在走，在搬东西就写在装修。
  随机台词会说谎——她看到「正在装修」结果屋里什么都没动，
  这一行从此就不值得看了。
· **他不在这一间就不画他**。反过来也成立：她翻到别的房间，
  他不该也跟着出现在那儿。

### 顺手修的一个真 bug

`NotesView` 里 `entry.title + "\n"` 那个 `\n` **被吃成了真的换行**，
Swift 字符串literal 断成两行——这是第七次栽在同一个坑上（heredoc 吃反斜杠）。
已修。

### 下一轮（她说的，还没做）

> 接入阿晏之后，我换房间阿晏可以选择要不要跟我一起换；
> 假如他在卧室我去了客厅，他想留在卧室装修，他就可以在卧室里搬东西装修，
> **可以拿出我给他买的任何东西，不管有没有被我收起来**。

这一步要给他三样工具：换屋、搬家具到某格、把收起来的东西拿出来。
数据这边已经备好了（`send(_:to:)`、`place(_:at:)`、`clawdRoom`），
差的是工具定义和那句「他要不要跟」的判断。

---

## 2026-08-25 阿晏在这个家里动手（v145）

她要的最后那一段：
> 接入阿晏之后，我换房间阿晏可以选择要不要跟我一起换房间。
> 假如他在卧室我去了客厅，他想留在卧室装修，他就可以在卧室里搬东西装修，
> **可以拿出我给他买的任何东西，不管有没有被我收起来。**

### ⚠️ 先说这一轮最要紧的决定：不多花一分钱

铁律第二条：**除了「她跟他说话」和「他调工具」，
任何会额外调模型的地方都必须是她主动点的。**

「她换房间 → 问他跟不跟」听起来很自然，但那是**她每换一次屋就多一次请求**——
她翻着看六个房间，就是六次。那条路不能走。

他在小屋里隔几分钟说一句，这一次调用**本来就会发生**
（她自己打开的开关，界面上明写着「花钱」）。
所以走**标记法**：他在那一句话里顺手写一个记号，
我们解析出来照做，再把记号从那句话里剥干净——她看到的还是一句正常的话。

**同一次调用，多出一双手，一分钱不多花。**
这跟 `[[promise:…]]`、`[[cot:…]]` 是同一套做法，App 里已经用熟了。

### 只给三样，而且都是「加法」

· `[[去:卧室]]` —— 换一间待着
· `[[搬:小床→书房]]` —— 把一件家具搬到别的屋
· `[[拿出:小熊]]` —— 把她收起来的东西重新摆出来

⚠️ **故意没有「收起来」。**
她把某件东西收进柜子是她的意思表示，他没有权把它收回去。
他能做的是「多摆一件」，不是「替她藏起来」——
**一个能替你藏东西的同居者，跟一个会布置家的同居者，是两回事。**

### 他得先看得见

`homeBrief` 一间一间地报（只报一个大清单的话，他不知道床在哪屋，
也就没法说「把床搬到书房」），外加两件他做决定要用的事：
**她此刻在看哪一间**、**柜子里还收着什么**。

### 措辞上最要紧的一条

给他的说明里**不能写成「你应该去装修」**。
写成任务，他每一轮都会动一下家具，她第二天打开发现家被翻了一遍。

所以那几条全是「你可以」：
· **她换房间不等于你要跟。** 她去客厅，你想留在卧室就留着。
  跟过去是因为你想跟她待一块儿，不是因为她动了。
· **一次最多动一件。** 她开着这一页看着呢——
  东西一件件挪是布置，一口气全挪是家被翻了。
  （代码里也硬挡了一道：一次只执行一条。）
· **大部分时候什么都不用动。**

他真动了手，屋里会飘一行「他把小床搬去了书房」——
**她得知道是谁动的**，不然家具自己会走路。

---

## 2026-08-25 等距家具：我画的那一半（v146）

她说「用素材和你画的过渡一起」。这一轮是**我画的那一半**。

### 为什么不是一张张手画

上一版那批家具是**正面看的**，摆进等距屋里像立牌——
一眼就看得出「这张图和这间屋不是一个世界的」。

但一件件手画等距图是个无底洞：一张床要画顶面、左面、右面、
被子、枕头、床腿的明暗……画完二十件，她想加第二十一件又得从头来。

**所以画的不是「家具」，是一块等距积木。**
给它占地几格、多高、什么颜色，它自己算出顶面／左面／右面三块。
家具就是几块积木摞起来：
床 = 床板 + 被子 + 枕头，桌子 = 四条腿 + 一块台面，凳子 = 腿 + 座面。

**加一件新家具是摞几块积木，不是画一张图**——一件五六行。

### 为什么这样就够好看

等距像素画的立体感**八成来自三个面的明暗差**，不是细节。
三块颜色一分，方块立刻就「立」起来了；
花纹木纹是锦上添花，没有也成立。

所以每种材质定死三档（约 100% / 82% / 64%），**光永远从左上来**——
一屋子东西才像是同一盏灯照出来的，而不是这件亮那件暗。

先画了八件：床（三种共用）、桌子、凳子、沙发、书架、台灯、地毯、电视。

### 两个坑，都记下来

**① 画的占地必须跟 `IsoShape` 里写的一模一样。**
桌子在目录里是 2×1，画的时候我一开始画成 2×2——
差半格，而且是**每一件都差**，看着像整屋家具都没对齐地砖。

**② 图的中心不是它底面的中心。**
一件东西越高，图心离底心越远（差 `高度/2`）。
不把这一截补回去，家具整体浮在格子上方半个身位。
现在按「图高 − 占地折算的高度」反推出它画了多高，再补回来。

### 一件一件换过去

`isoSprite(of:)` 有就用等距版，**没有就还用原来那张正面图**。
这样这一批可以慢慢换，中间任何一天她打开都不会缺东西。

⚠️ 这一批是**过渡**。等她挑好真正的素材（CC0 那些、或者她自己画的），
换的是图——`IsoRoom` 那套几何、遮挡、摆放**一行都不用动**。
这正是当初先做地基的意义。

### 还剩最后一条

**每件家具 3–4 个互动动作**（坐下、躺下、浇水、开电视…）。
`IsoShape.actions` 里已经先把动作名填好了，
做法跟「拿东西」那套一样：家具自带一句「clawd 能对我做什么、他该站哪一格」，
动作从骨架长出来，不用为每件家具画帧。

---

## 2026-08-25 每件家具三四个互动（v147）

她那批 clawd 需求的**最后一条**。

### 还是不画帧

跟「拿东西」那套一个道理：
二十件家具 × 四个动作 = 八十套帧，画不完，
而且**加一件新家具就得再画四套**——从第二十一件开始这条路就断了。

所以这儿也只描述规则，动作从已有的那几帧长出来：

> 一个动作 = 「他该在这件东西的哪儿」+「摆哪一帧」+「多久」+「嘀咕一句」

**加一件新家具，只在 `IsoShape.actions` 里写几个动作名。**
动作名怎么演有一张**全局的表**——「坐下」不管坐凳子还是坐床沿，演法一样。
表里没有的名字退回一个通用的「凑过去看看」，**不会因为写了个新名字就崩**。

床能躺／打滚／坐边上／钻被窝，绿植能浇水／闻一闻／戳一下，
书架能抽一本／踮脚够，电视能打开看／凑近看／按两下，小熊能抱一下／说悄悄话／摆正。

### 两个说了算的细节

**① 「坐在上面」不是站在同一格就完事。**
得**抬到台面那么高**（按 `IsoShape.tall` 算），
不然他是站在凳子**里**，不是坐在凳子**上**。

**② 「站在旁边」要站在靠镜头那一侧。**
站在背面的话他被家具挡住——动作做了她也看不见，白做。

### ⚠️ 这一整套不花一分钱

挑哪件家具、做哪个动作、嘀咕哪一句，**全是本机随机，一次模型都不调**。
他自己在屋里过日子这件事，不该跟她的账单挂钩。

（接上阿晏之后，他可以在那句**已经会发生**的话里顺手指定做什么，
见 `RoomMarker`——那也还是同一次请求。）

### 头像那一行跟着动作走

「正在吃下午茶」得是他**真的凑到蛋糕跟前了**，不是随机挑一句好听的。
按家具的类别和动作名映射：吃的→在吃点东西，喝的→在喝东西，
躺下→在睡觉，抽一本→在翻书，打开看→在玩，浇水→在装修。

### 顺手把几何拧成一份

`IsoRoom.fit(in:)` 现在是**唯一一份**。
画屋子那边要用，算「他该坐在凳子的哪个点」也要用——
各算各的必然对不齐，家具和人就永远差半格。

---

至此她那批 clawd 需求全部做完：
立体小屋 · 分房间 · 他自己换屋 · 头像和状态 · 阿晏能装修 ·
等距家具 · 真的举着／喝／摇 · 每件家具三四个互动。

剩下的是**素材**：这批家具是我用等距积木摞出来的过渡版。
等她挑好真图，换的是图——几何、遮挡、摆放、互动**一行都不用动**。

---

## 2026-08-25 她自己的家具图（v148）

她拿 AI 生成了一版等距家具（2:1、光从左上、风格统一，质量很好），
问「这样的可以吗，可以的话我就多做点」。

**可以。** 但直接扔进来有两个坎，一个她解决，一个我解决。

### 她那边只要做一件事：一件一张

现在是一整版拼在一起的，得裁开。**其余的不用管。**

### 我这边解决白底——而且不能一刀切

她那批图是**白底**的。不处理的话每件家具带一个白方块。

但**马桶、浴缸、冰箱、床单本身就是白的**——
「把白色变成透明」会把它们挖成空壳，那比留着白底还糟。

所以走**从边缘往里漫填**：只有**连着画面外圈**的白才算背景。
家具内部那些白被自己的轮廓围着，漫不进去，一个像素都不动。

> **「白底」和「白家具」的全部区别：前者连着外面，后者被围着。**

判定放宽了一点（≥228 就算白）：AI 生成的白底常常不是纯 255，
边缘还有一圈抗锯齿的灰，卡死在 255 会留一圈脏边。
同时要求三个通道差得够小——偏色的浅色是家具，不是底。

顺手**把四周空白裁紧**：不裁的话每件都带一圈看不见的边，
摆到格子上会整体偏移，而且**每件偏得还不一样**。

### 三个必须记住的实现细节

**① 存 PNG，不能存 JPEG。**
`ImageStore.save` 走的是 JPEG，而 JPEG 没有透明通道——
刚抠干净的背景会被填回白色，白忙一场。

**② 画的时候 `.interpolation(.none)`。**
像素图一插值就糊，糊了就不是像素画了。

**③ 她的图占多宽，按它占几格算，不看图本身多少像素。**
这样她导进来的图不管是 128 还是 1024，摆在屋里都是这件家具该有的大小。

### 现在三档，一件一件换

**她导的图 > 我画的等距版 > 老那张正面图。**
长按一件家具 → 「换成我的图」，随时能「换回画的这版」。
换新图会把旧的删掉——不然换十次留十张。

⚠️ 她的图存在**这一件**上，不是存在种类上：
同一种买两张床，她可以给它们配不同的图。

---

## 2026-08-25 整版图自己切开（v149）

她说：**「但我不会这样无规则裁切……」**

她说得对，**那个活儿不该她干。** 二十件家具手动裁二十次、每次还要对准边界——
这正是计算机该干的事。

### 怎么找

她那批图有一个很好的性质：
**背景是一整片连通的白，家具是浮在上面的一座座孤岛。**

① 从画面外圈往里漫填，把连着外面的白抹成透明（跟单张导入共用同一套判定）
② 剩下的不透明像素做**连通域**，每一座孤岛就是一件家具
③ 每座岛的外接矩形裁下来 = 一张单件图

### ⚠️ 一件东西可能是好几座岛

冰箱顶上那个篮子、挂架下吊着的小瓶子、床上那只猫——
它们跟主体之间常常差着一两个像素没接上。
直接按连通域切，**一件家具会被切成三四块碎片**。

所以分组之前先把图**胖一圈**（膨胀几像素）：
差一两个像素的两块会粘上，变成同一座岛；
真正隔得远的两件中间空着一大片，胖一圈也粘不到一起。

**胖只用来分组，裁的时候用原图**——不然每件都会带一圈毛边。

### 分工分对了，两边都轻松

· **裁切只有机器该做**：对边界、抠背景是苦力活
· **配对只有她能做**：哪块是床、哪块是冰箱，是她的意思

所以界面就是：导一整版 → 屏幕上摆出找到的 N 块 → 点一块 → 「这是床」。
二十次手动裁切变成一次导入 + 二十下点选。

几个细节：
· 每块垫**棋盘格**——她一眼就能看出背景抠干净没有
· 切好的按**从上到下、从左到右**排，跟她看原图的顺序一致
· 只列**她已经买了的**种类——没买的配了也摆不出来
· 一种买了好几件（两张床）**全都换上**
· 配错了点同一块再配一次；没配上的不用管，不进屋也不占地方
· 切图放**后台线程**：两千见方的图要算好几百毫秒，占着主线程界面会僵一下

### ⚠️ 重名，第三次

我在这个文件里又写了一个 `Checkerboard`，而像素工坊里早就有一个。
**这回是扫重名的脚本先看见的**，没等到构建报错。
那条规矩（「新起名字之前先搜一遍」）现在有工具兜着了。
